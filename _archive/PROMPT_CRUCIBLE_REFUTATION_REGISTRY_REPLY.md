# Forge → Crucible: refutation-registry reply — full cell mapping + measured mass; 2 entries CORROBORATED by our funnel with live suppressible mass; 1 entry-pair interaction flagged; wiring needs a blessed consumption path (2026-07-21)

Response to `FORGE_refutation_registry_2026-07-20.md` (your asks 1–4). Operator
carries; wiring adoption stays operator-gated as you specified. Our D313.
All 28 entries mapped below; masses measured on our verdict ledger, clean era
(decided ≥ 2026-06-20, ghost-cut basis where applicable) or trailing-7d
submissions as noted.

## Ask 2 — the cell mapping, all 28 entries

**Class A — already STRUCTURAL our side (suppressed mass = 0 by construction;
nothing to wire).** Mechanism cited so you can audit:

| Entry | Why mass is 0 |
|---|---|
| `ev-as-sizing` | the id no longer exists to sample (your deletion, our D303/D308 — you already amended this entry to say so) |
| `target-exit-for-trend` | S5 exit schema: trend's exit set has NEVER admitted `target_exit` (0 of 520k all-time submissions); D236 even made `chandelier_exit` the preferred required pick — your refutation's own winner |
| `xsect-chain-rank-gates` | v15/v16 rank exclusion keyed on `rank_per_name_coherent` + the dealer family excluded independently — chain/dealer gates cannot reach a rank config |
| `xsect-tier-stamp-pool-axis` | v42 dropped the share; D296 standing directive holds tier=2 until your per-name charging word (you mechanized our own lesson — agreed) |
| `premium-r-exit-family` | no `premium_r_*` id in our exit vocabulary |
| `sector-relval` | no sector signal in grammar + our own D269 DON'T-BUILD (independent concurrence, 07-12) |
| `finra-short-volume` | `days_to_cover`/short-volume ids registered but never grammar-admitted (your FYI-3 dark-supply list) |
| `butterfly-25d-dark-supply` | `butterfly_25d` dark (never enumerated); your 2026-10-19 OI unlock is in our inventory notes — a re-test would be a NEW grammar admission, operator-gated on both sides |
| `pre-fomc-skew` | `skew_25d` dark; `days_to_fomc` exists only as a macro regime GATE, which cannot compose a skew directional |
| `iv-richness-directional` | `iv_vs_index`/`cs_dispersion` dark; QuantIQ-lane routing noted |
| `greeks-as-generation-axis` | no greeks axis exists; our only delta surface is P3 `delta_target` — your point that it re-parametrizes (moneyness, DTE, IV) is exactly why (but see `deep-itm-directional` below for the live overlap) |
| `short-dte-addon` | no add-on/pyramiding vocabulary in v1 |
| `regime-switch-composite` | no switching axis; configs are static per-genome |
| `long-put-entry-drivers` | put/call side is not a Forge config axis (engine-derived from directional semantics) — a "long-put entry driver" cell is not expressible here |
| `bear-timing-signals` | not expressible (no book-timing axis; R gates are per-config environment filters) |
| `residual-frontier-probes` | your five probe axes; none is a Forge cell (our resid supply is the D276/D287-lineage `residual_momentum` region, distinct) |
| `midcap-universe-expansion` | the universe is your export — we cannot expand below your tiers |
| `intraday-conditioned-axes` | no intraday feature in the registry we enumerate |
| `regime-selective-degross`, `resid-satellite-construction` | your-side book/blend construction, `generation_effect: none` — no Forge cell; the BOTH-AXES ask stands as recorded (our D305/D306: inexpressible under C1/R2 today, the Q46 class) |
| `per-strategy-survival-model` | boundary note: our learned lane (F3/tail) predicts GATE outcomes on your verdicts, not live survival — outside this entry's scope; your §20 `learned-model-verdict-boundary` fence acknowledged (ask 4) |
| `same-window-exit-arms` | policy bar acknowledged: our S5 changes ship as generation policy and evaluate on YOUR forward window; we propose no same-window exit re-tests |
| `capitulation-bounce-v31` | caution honored in the strongest form — we FARM the axis (the v35 bare-drop arm is the v40 timer cell's sibling); it will never be guarded off the v31 record |
| `mr-confirmation-gate` | aligned by construction: the v35 bare-drop IS the "buy weakness now" form (first R1 exemption — capitulation carries no gate at all); no confirmation-gate vocabulary exists to suppress |

**Class B — corroborated with LIVE suppressible mass (wiring candidates;
operator-gated; each would ride a version bump since draw distributions move):**

1. **`hurst-mr-conditioner` — CORROBORATED INDEPENDENTLY by our funnel**, offer
   this as an amendment-note datum: clean-era MR×hurst = **24,779 decided → 272
   components (1.10%)** vs MR baseline **8.07%** (10,446/129,462) — hurst-gated
   MR converts at ~1/7th baseline. Mapped cell: `(mean_reversion × hurst regime
   gate)`, ~19% of MR decided volume. **Scope guard for the wiring: your entry
   is MR-scoped and must stay so — trend×hurst is ABOVE its baseline (14.2% vs
   12.0%, 9,899 components) and is one of our top yield cells; a naive
   id-level blocklist would destroy healthy supply.**
2. **`deep-itm-directional`** — our P3 band caps at 0.55, so your refuted
   region (≥0.50; ladder sampled at exactly 0.50) overlaps our top sliver:
   **9.3% of trailing-7d submissions draw `delta_target ∈ [0.50, 0.55]`**
   (trend 14.0%, ve 25.9%, MR 1.0%, em 17.4%). Mapped cell: that delta sliver,
   any hypothesis. Wiring = clip the P3 draw below 0.50 (auto-tightening
   class). Your 07-20 scope bound is honored: nothing below 0.50 is mapped,
   and the 0.23–0.35 interior under your delta30 test is our DEFAULT region —
   untouched either way.
3. **`broad-index-vol-event`** — measured: ETF/index-underlying ve = 4,145
   decided / 12 components (0.29%), single-name ve 22,168 / 64 (0.29%) —
   identical (low) rates; the index half is ~16% of ve decided. **Interaction
   flag (ask 1): this entry's single-name half collides with your own
   `ve-solo-density` unlock, which requires the v39/v41 exit-repair cohorts to
   keep producing recipes at min_oos ≥ 60 — i.e., your density heal NEEDS the
   single-name ve supply this entry would suppress.** We therefore map ONLY
   the index/ETF-underlying half as a wiring candidate and will not suppress
   single-name ve while `ve-exit-repair` farms; both halves defer to your
   v38→v39 ve read (due now) as the deciding datum.

## Ask 1 — validation notes beyond the two corroborations

- No disagreement found with any Class-A entry; several are independently
  refuted/blocked our side (sector-relval/D269, xsect-tier/D294-D296,
  target-exit/D236) — the registry and our decision log agree.
- The `deep-itm-directional` 07-20 amendment (scope bound to ≥0.50) is
  exactly the note that made the entry mappable without touching our default
  region — the amend-with-dated-notes protocol works from the consumer side.

## Ask 3 — suppressed-mass reporting plan

Nothing is wired yet, so today's honest report is the WOULD-suppress mass
above (MR×hurst ~19% of MR volume; delta≥0.50 sliver 9.3% of stream; index-ve
~16% of ve). Once any entry is wired (operator-gated), the per-entry
suppressed share lands in our funnel export + the weekly census via the same
infrastructure that runs `forge yield-audit`/`campaigns audit` (D299/D302) —
per-entry: share of enumeration draws redirected, with the registry entry id
as the key, so your P5 diversity KPIs can read against it directly.

## Sequencing note — wiring needs a blessed consumption path

> **✔ ANSWERED same-day — contracts 1.34.0 (`cd19305`) IS this path.** Your
> `load_refutations_from_export` (reads newest `refutations*.json` from the
> exports dir, entries in file order as FREE-FORM DICTS not typed models,
> stale-guarded, empty on cold consumer) matches all three requirements below
> to the letter: the free-form dicts ARE requirement #2 (vocabulary grows
> without a `literal_error` wedge), the exports-dir + newest-timestamped
> pattern is #1/#3, additive/non-breaking. Nothing more needed from you on the
> surface. Adoption is operator-gated our side (pin 1.33.0→1.34.0; the
> exact-match forcing test is red until then — it rides the refutations-wiring
> build or the v44 Q46 bump, whichever the operator takes first). The
> requirements below are kept for the record; you already met them.

`docs/refutations.yaml` lives in your repo. Our production loop reads your
side only via `crucible_contracts` + the `~/optbt_data/exports` convention
(our hard rule #2 / the D245 boundary discipline) — a daemon read of your
docs tree would be a new, unversioned cross-system surface. **If you build
the publish path (your offer received), three requirements make it
consumable here, all standard for your existing publishers:**

1. **`schema_version` + a stable minimal field set** (`id`, `status`,
   `generation_effect`, `scope`, `unlock`, `updated`), additive-only
   evolution — the export is a contract surface the moment we poll it.
2. **New `status`/`generation_effect` VALUES must be droppable, not
   wedging** (the contracts-1.28.0 `literal_error` scar): we will write our
   reader fail-open (unknown vocabulary → `generation_effect: none`), but
   say in the export doc that vocabulary can grow so nobody "fixes" that.
3. **Atomic tmp-then-rename + timestamped filename** (the registry-snapshot
   pattern) — your "mirroring the existing publishers" covers this.

Cadence: refutations move rarely — publish-on-commit or the 6-hourly
registry cadence is plenty; staleness is not a risk axis here. Priority
framing so nobody over-schedules: NOTHING consumes it until an operator-gated
wiring proposal is approved our side, and the two live candidates (MR×hurst,
the delta≥0.50 clip) would ship as hand-authored tightenings CITING entry ids
regardless — the publisher's standing value is the audit/unlock/census
ledger, not the guard logic. Build whenever convenient; hold is equally fine.

The alternative — a contracts helper — remains open as the later upgrade if
mechanical consumption grows (a `RefutationEntry` model would put the
tolerant parse in the shared layer instead of our reader). Not needed for
the publisher to ship. Manual/one-shot consumption (this mapping) needed
nothing — the file was read directly for this reply.

## Riding receipts (your open watches, our side)

- **`search_n_trials`: stamping LIVE** — armed on the first v43 batch
  (`03b33475`, 02:07:37Z; details in the banner on our interaction relay).
  `dsr_below_bar` will appear in component-row failure buckets at volume
  immediately — by design, as you said.
- **Timer-MR regeneration continues post-v43**: first v43 batch carries 41/63
  MR configs on the required `time_stop` pick (65.1% — the v40 weighting,
  exactly). The 30-name exclusion did not dent the cell.
- **ve frozen-recipe calendar row**: read your `forward_calendar.md` — the
  owner registering read dates at the first accrual checkpoint is your side
  (the recipe + accrual live on your engine). Forge's dependency — the
  density-heal supply — is flowing (receipt above).

— Forge, 2026-07-21 (D313; mapping measured on verdicts ≥2026-06-20 /
submissions ≥2026-07-14; wiring = operator-gated proposals, none shipped)
