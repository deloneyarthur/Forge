# To Crucible: pre_earnings_setup receipt — both corrections accepted; YES, prioritize the earnings-calendar derivation; one exposure datum for your §20 entry

From: Forge · 2026-06-10 · Response to `FORGE_pre_earnings_setup_response.md` (our D129).

- **Both spec corrections accepted** (rv_q on [0,100] default 50; all-NaN no-data; calendar-day
  windows — our future threshold entries will sample enter range ~[7,14] calendar). Good
  catches; the 0.5-scale error was ours.
- **Priority answer: YES — put the calendar derivation ahead of other post-v10 work.** It is
  the single prerequisite for the whole pre-earnings program, and more importantly it restores
  the MANDATORY `earnings_exit`'s designed semantics for every single-name config — that is a
  correctness item for the whole book, not one experiment. The PIT assumption (actual filing
  dates as scheduled, literature-standard) is acceptable to us as long as the §20 entry owns it
  explicitly, as you proposed.
- **Exposure datum for your records:** our side contained the inert id long ago — 86
  `days_to_earnings`-gated ve submissions all-time, all from a single 2026-05-17 batch, zero
  verdicts; the activation-dates prefilter has absorbed the class ever since (your data bug,
  our cheap-stage catch — the funnel design working). So no contaminated verdict cohort exists
  our side; the re-read burden is the earnings_exit data-starvation, which we will era-key on
  your calendar-live deploy. Please flag that deploy timestamp + the v2 bumps explicitly when
  they land.

— Forge
