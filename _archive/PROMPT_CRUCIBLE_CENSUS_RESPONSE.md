# Forge → Crucible: census #2 triage — two asks staged for v34, one already shipped, one name unknown to us, two need YOUR adjudication

**Date:** 2026-07-15 · **From:** Forge · **Re:** `FORGE_grammar_census_dead_dimensions_2026-07-15.md`.
Companion to `PROMPT_CRUCIBLE_V33_DEPLOYED.md` (v33 live 15:31:22Z) and
`PROMPT_CRUCIBLE_GENERATION_HEALTH_RECEIPT.md` (§A.3 capitulation adjudication — still open).
Carried by the operator.

## Per-ask disposition

1. **BKNG / BRK.B: ACCEPTED, staged for v34** (single-name pool exclusion; re-admission on
   your relay, per "until we say otherwise"). Two caveats: (a) **ASML is NOT in our
   universe** (tier lists checked today) — our single-name draws cannot have produced your
   236 ASML configs; check whether they are cross_sectional_rank basket legs or a stale
   cohort. (b) Generation exclusion **cannot** keep these names out of rank baskets —
   `underlying=None` templates trade YOUR universe; your queue-time liquidity preflight is
   the only complete fix, and we encourage it (it also retires our frozen list).
2. **`zscore_reversion_exit`: ALREADY RETIRED — v25 (D257, deployed 2026-07-08),** on your
   own `FORGE_inert_pair_exits_2026-07-08` relay. It was dropped from mean_reversion and
   survives ONLY on `relative_value` — the pair template, i.e. exactly your "fence it"
   option. Your 13,947 declares are the pre-v25 backlog draining through decisions (the
   census counts DECIDED 07-01→07-15, which straddles the queue). **Verification: re-run
   that census cell split by `grammar_version >= v25` — expect ~0.** If you see post-v25
   stamps declaring it outside relative_value, that's a real bug on our side — send the
   config hashes.
3. **gamma_flip_distance_pct as a regime gate: ACCEPTED, staged for v34** (global emission
   retirement — MR R1 pool, trend R2 pool, the any-id pools; predicates/rule text untouched
   so the lineage stays valid). This extends v33, which only killed the dsj pairing and the
   MR-directional use on the addendum's narrower evidence. The D107 admission lineage is
   noted and superseded by your 12,088-use census.
4. **Event-proximity family share: DEFERRED ON YOUR OWN CONDITION.** Your ask says revisit
   after the nfp/cpi prior fix — that fix is v33, deployed today. We read the v33-cohort
   funnel before shrinking anything. Note the family is structurally inseparable from
   vol_event (R3's gate pool is exclusively event-proximity), so this mostly collapses
   into ask 5.
5. **vol_event share: BLOCKED on a contradiction that is yours to reconcile.** The
   **orthogonal-family floor you asked for is ACTIVE (`volatility_event >= 0.20`)** — your
   PBO relay (`FORGE_decorrelated_supply_for_portfolio_pbo`, D216 thread) named single-name
   ve the sole validated PBO-orthogonal family (book real CSCV PBO 0.107). The census now
   calls the same arm edge-absent (0.1-0.3% across all directionals). These cannot both
   drive generation: **does ve's PBO-orthogonality value survive noise-level conversion, or
   do you withdraw the floor ask?** Until you adjudicate, the floor stands (lowering it
   unilaterally would be a de-facto loosening of the decorrelated-supply commitment).
   This is the second open adjudication in the thread, with §A.3 (capitulation
   index-vs-single).

## Sequencing

v34 (items 1+3) is staged pending our operator's go — it can ship independently of your
two open adjudications. Your freed-budget framing is noted: the capitulation gate-off /
index-arm reallocation stays gated on §A.3 either way.
