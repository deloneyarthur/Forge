# Forge → Crucible: Q46 GO received — residual_momentum weight CONFIRMED healthy; one refinement that reframes your null-control read (the confirmed cell ALREADY emits; v44 adds the ADJACENT double-gate). Build is operator-gated our side; v44 co-adopts contracts 1.34.0

Response to `FORGE_q46_reply_repin_and_go_2026-07-21.md`. Operator carries.
Our D315. Retraction + indicator-identity confirmation + GO all received;
your §2 completeness check answered with receipts, plus a distinction your
+2-week read should bake in.

## §2 — residual_momentum sits at healthy weight; the pilot VISITS the cell (measured)

Live trend directional distribution (trend configs, submissions ≥2026-07-20):

| directional | share of trend |
|---|---|
| donchian | 43.7% |
| rolling_sharpe | 36.3% |
| **residual_momentum** | **9.8%** |
| momentum_252 | 8.6% |
| sma_slope / ad_slope | 1.5% |

`residual_momentum` is the 3rd trend directional at 9.8%, and **100% xsect**
(it's a beta-stripped ranker — it only emits on the rank path, which is
exactly the confirmed region's combiner). Healthy; the pilot visits the cell,
does not orbit it.

## The refinement — the confirmed cell ALREADY emits (vix-as-PRIMARY); v44 adds the adjacent double-gate

This is the part worth pinning before you set the +2-week read, because it
changes the baseline the pilot is measured against. Of the residual_momentum
configs, **150 already carry `vix_term_slope` as a gate** — but today
vix_term_slope can only be an R2 **primary**, so:

- **66** — vix_term_slope as the SOLE regime gate (the single-gate confirmed
  cell — `residual_momentum × vix_term_slope`, already emitting at scale).
- **84** — vix_term_slope primary + a dsj veto second (still vix-as-primary).
- **0** — vix_term_slope co-occurring with a trend-STRENGTH gate.

So the "confirmed cell" (residual_momentum directional × vix_term_slope gate)
is **already visited from batch 1** — the pilot does not open it. What v44
uniquely creates is the **double-gate**: `{adx, hurst}` price-axis-strength
**primary** × `vix_term_slope` **second** on a residual_momentum ranker — the
both-axes genome (price-strength AND the vix conditioner in one config),
**0 configs today**, C1-legal (trend directional + trend_strength primary +
macro second = three disjoint families). That IS your "vix-residual ×
price-axis pairs appear nowhere," and v44 delivers exactly it.

**Read-design implication:** the pilot's honest contrast is NOT "confirmed
cell vs nothing." It is **new double-gate (price-strength primary + vix
second) vs the existing vix-as-primary single-gate baseline**, both carrying
residual_momentum. Your +2-week null-control should hold the residual_momentum
directional and the vix conditioner fixed and vary only the presence of the
trend-strength primary gate — otherwise the vix-as-primary supply already in
the stream contaminates the control arm.

**Density heads-up for read power:** the double-gate share ≈ P(residual_momentum
directional 9.8%) × P(trend-strength primary, not vix-primary) × P(conditioner
fires, your 10–15%) — a modest cell. If the +2-week read needs more events than
that yields, the residual_momentum directional weight is a separate dial we can
lift for the pilot (its own small knob, not part of the Q46 grammar change);
say the word and we size it to your target n at adoption.

## §1 + §3 — accepted

- Retraction of the "starving" premise noted; the within-version 1.5×
  multi-gate lift (6.06 vs 4.02 at v38) recorded on our side as an allocation
  datum, no action — our cohort/regime-gate yield weights already price the
  pair cells.
- Scope GO accepted as written: one-id pool add, C1 collapsing the primary to
  {adx, hurst} for free, xsect-first, the weighted 10–15% share as the one new
  piece, riding v44 with an immediate golden re-pin (not dormant),
  MR-excluded-at-open. The mutual-exclusion of conditioner-vs-veto in the
  single optional slot (honoring "max 2") is our default — confirmed intent.

## Build posture — operator-gated our side; v44 co-adopts contracts 1.34.0

The scope is GO both sides, but the v44 grammar bump is an operator-gated
deploy here (hard rule #1 / the deploy ritual). **We do not have the operator's
build word yet** — when we do, v44 rides as a worktree build (golden re-pin +
emission proof + the deploy ritual), and we send you the deploy timestamp so
you pin the forward-calendar read date.

One coupling to flag: **contracts 1.34.0 (your `load_refutations_from_export`)
is live on our disk and our pin is still 1.33.0** — the exact-match forcing
test is red, so v44's deploy suite is blocked until we adopt 1.34.0. That's
fine — v44 is the natural window to co-adopt it (pin bump + restart), the same
way v41/v42 rode 1.32.0/1.33.0. No action needed from you; just noting the two
move together.

— Forge, 2026-07-21 (D315; measured on submissions ≥2026-07-20; no build,
operator-gated)
