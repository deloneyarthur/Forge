# D073 / Phase 3 — auto-tightened threshold ranges. RETIRED 2026-06-24 (D206).
#
# This path is retired: the file carries NO tightenings, so
# `forge.enumeration.indicator_thresholds._auto_tightenings()` returns {} and the
# sampler falls back to the D031 audited baselines for every (indicator, role).
#
# Why (2026-06-24 prefilter-tightening review; D206 / OPEN_PROPOSALS c4d68531):
#   - FLAT AXIS: the per-config threshold response is flat on the binding
#     constraint (CPCV-p25) — Q43 -> D171. Tightenings cannot improve
#     promotability (and were not Goodharting — no edge gradient to capture).
#   - WASTE SOLVED: 8,914/10,000 gated runs are high-trade (>=10 trades); the
#     zero-trade waste this path targeted is gone (D076 empirical-prior filter +
#     grammar maturation), not threshold selection.
#   - MONOCULTURE RISK: the active set was a one-shot from 2026-05-27 (~v3-v5
#     cohort, never re-run); re-deriving 5-95% bands off the current rolling 10k
#     export (~3 days, all v21/v22, ~76% trend) would entrench the trend
#     monoculture the pool must diversify away from.
#
# PERMANENT (D298, 2026-07-20, operator-approved): the proposer
# (`scripts/propose_threshold_tightenings.py` + `forge.feedback.threshold_proposer`)
# is deleted from the tree — re-enabling means restoring both from git history
# (commit 85c1df5 has the last 16-entry content, pre-D206) + a deploy. This
# file stays (empty) because `auto_tightenings_fingerprint()` reads it into
# `enumeration_inputs_hash` (H-3 determinism identity).
#
# Loosening proposals still route to OPEN_PROPOSALS.md (hard rule #4).

tightenings: []
