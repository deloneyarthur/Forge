# Forge — Design-level Decisions Log

> **STATUS (2026-06-24):** LIVE pointer, still EMPTY (no entries yet). This is the reserved home for *design-level* decisions that supersede/extend a DESIGN.md §14 choice — NOT the day-to-day ledger. The active decision ledger is `../IMPLEMENTATION_DECISIONS.md` at repo root (its newest D-number is the current state — never mirrored here, per the volatile-facts rule). Note: `learned-ranker.md` named this file as F3's eventual decision home, but F3 shipped via the root ledger ([[D149]]); nothing has been written here.

Append-only. **Design-level** decisions made *after* `DESIGN.md` was finalized — when the operator chooses a path that supersedes or extends a §14 design-time choice.

Day-to-day implementation choices (which the agent makes while building) live in `../IMPLEMENTATION_DECISIONS.md` at repo root.

The original v1 design decisions remain in `DESIGN.md` §14 and are not duplicated here.

---

(No design-level decisions added yet beyond DESIGN.md §14.)
