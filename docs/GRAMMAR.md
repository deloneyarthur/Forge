# Forge — Hypothesis Grammar Narrative

**Status:** Phase 1 deliverable. This file will be written alongside `config/grammar.yaml` in Phase 1 — every rule in the YAML has a corresponding section here, paired by id.

See `DESIGN.md` §3 for grammar structure and §3.5 for the v1 ruleset (21 rules: S1-S5, C1-C4, P1-P4, E1-E3, R1-R3, X1-X2).

A pre-commit hook (Phase 1) will validate that every rule id in `grammar.yaml` has a matching section header here.

---

(Phase 1 will populate this file. Bootstrap placeholder only.)
