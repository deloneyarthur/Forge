# Forge — Status

## 2026-07-21 (later) — `search_n_trials` build LANDED dormant + D310 record completed: their (a) SHIPPED (record-not-bind for forge rows, §20 `dsr-record-not-binding-forge-minimal`) → the D306 hold released; per-slot cumulative stamp rides the v43 restart SELF-GATED — arms itself only when Crucible's `recorded_not_binding` marker appears in our verdicts (NOT yet observed; freshest pre-build verdicts still n_trials=1-detail)

Full record: [[D310]] (the absorbed commit `babb148` + this session's extension — the build session's SHELL died, not the session; the operator's deploy absorbed the on-disk files to honor D104, correctly). Their resolution relay confirmed all three D306 claims verbatim and chose compute-and-record over our exemption-set phrasing (deliberately: the exemption set would demote full-pass runs once stamped); (b) remains available as its own pre-announced boundary. **Expect once armed:** non-reject forge rows may carry `dsr_below_bar` in `failure_buckets` (extra signal, not contradiction — zero Forge readers, verified); journal flips `search_n_trials: dormant` → `stamped N configs (max slot n_trials=M)` with no Forge action — **record the first stamped batch_id when it happens.** Riding confirms recorded (sma_slope GO; resid-vix floor retirement; BOTH-AXES = standing record, Q46 class). In-code D309→D310 renumbers completed (third number race of the day). RELAYS: interaction relay answered same-day.

## 2026-07-21 — GRAMMAR v42 → v43 DEPLOYED (D309): the 30-name yield-audit exclusion rider — first cohort detected, prereg'd (`44a4e08aef4f`), and shipped entirely on OUR funnel (operator "Ship all 30" → "deploy v43"). [deploy evidence + first-batch audit appended below after start]

**Change:** `_STRUCTURALLY_UNTRADEABLE_UNDERLYINGS` 8 → 38 (+AAL ADBE AMZN ARKK BSX DIA DVN EEM EFA GE INTC KO LRCX LUV MS MSFT NEM NKE PEP TXN UNG UPS VZ WFC XBI XLF XLI XLP XLV XOM — each ≥500 decided / 0 conversions since clean era, ghost-cut applied; all verified still-in-universe; 3,092 single-name subs/7d = 4.7% of stream). `rules:` untouched; frozen-list terms unchanged; xsect books untouched (documented limitation — their preflight is the complete fix). **Operator's mid-window "exclude or fix?" answered:** the exclusion is v1-long-options' verdict on these names, not the tickers'; the "fix" is representational (Path C defined-risk spreads — this cohort is fresh evidence FOR that resume case, parked as the operator's call); fully reversible (relay re-admission / preflight retirement / any v2 structure bump revisits).

- **Build (in the 01:39Z down-window):** sampler +30 with D309 terms; census-test literal extended; grammar.yaml v43 + header note + archive `v43.yaml`. **Goldens 7/7 re-pinned environment-matched** (license: OLD code reproduced every constant at HEAD, 2037-green preflight; first divergences: all 7777-seed regime goldens @0 — the v37/v41 pool-shift signature; cohort @2, 5/15 surviving via per-index seeding; first-capitulation landmark 30 → 71, scan widened to 80). d105 fallback tilt bound → inclusive (DIA/MSFT/AMZN leave the 24-name fallback; floored div share lands exactly ON 5%).
- **Emission proof (live registry, 3k cold): v43 stamp, 0/3000 excluded-name draws, 83 distinct names, all 5 hypotheses reachable** (em 616 / mr 608 / ve 605 / rv 601 / trend 570).
- **Relay `PROMPT_CRUCIBLE_V43_DEPLOYED.md` (held for carry):** name list + evidence + row-45 cross-check request + `funnel --compare v42 v43` + boundary cohort-split reminders (the v40 MR and v39 ve reads land near this boundary; excluded names were 4.7% of stream).
- **NEXT: the D307 young-cell floor activation** (`FORGE_YOUNG_CELL_FLOOR=on`, daemon-reload, restart) in its OWN window after the first v43 batch audits in-spec — no boundary carries two changes.
- NB this commit also carries the concurrent session's D308 ledger/STATUS/RELAYS appends (same-file race, absorbed).

## 2026-07-20 (close, later¹³) — EV deletion executed their side, loop CLOSED (D308, docs-only): sequencing honored (soft-fail guard `6bc60f8` → id-less snapshot `222936Z` [72 ids, verified] → engine deletion 22:31–33Z); daemon pickup +1 min (iter 2658, registry_hash → `83e9a01ca0389e0f` = THE split point, grammar stays v42); **kelly dormancy VERIFIED by construction AND count — first post-publish batch `9cca352a` (23:00:45Z) + 3 more = 800 configs, 0 kelly / 0 EV**; 8/19 EV queue-stragglers observed failing soft

Full record: [[D308]]. Their gated-window re-scan reconciled the D303 ledger flag (all-time 25 ours / current-disk zero theirs — both agree). The 8 failed EV stragglers land in the failed-runs export as `error_category: "other"` — NOT the promised `deregistered_indicator` string; contracts field is an open string by design (no D261 wedge), their export writer just doesn't pass it through → **flagged back via the CLOSED banner appended to `PROMPT_CRUCIBLE_EV_DEREGISTRATION_RESPONSE.md`** (cosmetic; hash-join counting works). `~/optbt_data` root: no change, never silent (boundary Q answered). NB the first-batch id was initially recorded as `8cab6359` (a watch-window artifact — that was the second; `9cca352a` slipped out in a brief 22:49Z depth dip); corrected in D308 once the count ran. OPS NOTE (resolved): the triage session's Bash tool died harness-wide mid-verification (even `true` failed, subagent-confirmed; daemon was healthy throughout — box/service never at fault); the count + this commit landed after shell recovery.

## 2026-07-20 (close, later¹²) — D302 decision round CLOSED (D307): corr-to-book CARRIED (operator); **30-name rider APPROVED "Ship all 30"** → prereg `44a4e08aef4f` + `docs/proposals/v43-dead-name-rider.md` STAGED (deploys at your go / rides the next bump); **Theme 2b young-cell floor BUILT flag-OFF** (`FORGE_YOUNG_CELL_FLOOR`, byte-identical, reboot-safe) awaiting its own activation window

**Operator walk-through of the D302 decisions.** Homework ran before the rider call: ALL 30 dead names still in the current universe (1 t1 / 5 t2 / 24 t3) and drawing **3,092 single-name submissions in the trailing 7d = 4.7% of the stream**; the 8→38 frozen-list growth (32% of the union) flagged and accepted.

- **v43 rider (STAGED, awaiting deploy go):** prereg FIRST (`44a4e08aef4f`, cohort cut 07-21T00:00) per D207; build happens in the deploy window (goldens re-pin — the pool shift moves every draw; emission proof 0/3k on all 38; first-batch audit; deploy relay carries the name list + row-45 cross-check request). Candidate window: ride the v39→v40-read bump ~07-22/23 or its own window on your word.
- **2b young-cell floor (BUILT, flag-OFF):** `cell_floor.py` (mature = ≥25 honest-era verdicts per (directional, regime) cell, ghost-cut applied) + diversifier phase 0c (≤2 slots/young cell, cap 10%/batch, pinned cells exempt, bare configs never floor) + queue/main threading behind `FORGE_YOUNG_CELL_FLOOR` (default off = `mature_cells=None` = byte-identical; 4 flag tests + a byte-identity test pin it). With `resid-vix-two-arm` retired (D305) the pin set is EMPTY — this floor is what protects the next campaign cell automatically. **Activation: flip the env on forge.service + restart + first-batch `cell_floor:` journal read; deliberately a SEPARATE window from v43** (no double-change boundary). Sorted-order cap bias documented; revisit on over-subscription evidence.
- **Corr-to-book: CARRIED** (RELAYS.md updated); their move next; telemetry build only on a yes.
- Gates: full suite at commit (below); mypy --strict + ruff clean. MANPAGE env-flag block, architecture.md, proposal headers updated same commit. NB D306 was claimed concurrently mid-round (search_n_trials interaction) — numbering races remain routine.

## 2026-07-20 (close, later¹¹) — `search_n_trials` build HELD at verification (D306, docs-only): **their Q2 "populate + no flip" contradicts their own gate code** — `deflated_sharpe.passed` binds the component verdict via `_verdict_from_gates`, so stamping slot counts (~46k) would flip the component stream to reject = the standing-gate flip + feedback-era boundary they deferred. Interaction relay drafted+held (`PROMPT_CRUCIBLE_SEARCH_N_TRIALS_INTERACTION.md`, asks their (a)/(b)/(c)); today's unset→n_trials=1 behavior continues

Full record: [[D306]]. The D245 both-sides class caught pre-build this time, not post-wedge. The relay also carries the sma_slope re-probe GO + the resid-vix floor-retirement notice. **Open restart window when convenient: D305 (floor retirement) is committed and rides the next restart** — nothing else queued for it.

## 2026-07-20 (close, later¹⁰) — `resid-vix-two-arm` campaign RETIRED (D305, operator "Retire now"): status flip in `campaigns.py`, derived D287 floor now EMPTY (phase 0b reserves nothing), reopening condition on record (their standing BOTH-AXES ask — Q46-class, inexpressible today). Effective at the NEXT RESTART; until then the 4-slot reservation runs harmlessly. ranking 307 + invariants 125 green

Full record: [[D305]]. The `search_n_trials` build (per-slot cumulative, operator-approved same session) is next — lands as D306, also next-restart-effective; the two ride one restart window together.

## 2026-07-20 (close, later⁹) — Housekeeping answers TRIAGED (D304, docs + one live probe): meta-king timer = repurposed, watch closed FOREVER; **sma_slope/ad_slope WIRED — re-probe GO (537/440 max activations), the v24 trend adoption finally carries**; DSR relay was answered 07-08 all along (Q3 basis corrected: daily-n not T=trades; **Forge owes `search_n_trials`, 12-day dropped ball**); **resid×vix two-arm read CLOSED their side → the D287/D299 campaign's retire_on FIRED**. Two builds await operator go

**Inbound `FORGE_housekeeping_answers_2026-07-20.md`, every claim verified (their deleted answers file re-read from their git `dffbb83`).** Full record: [[D304]]. RELAYS.md updated (3 rows → answered). Pending operator go: (1) `resid-vix-two-arm` campaign status flip farming→retired in `campaigns.py` (licensed by the fired retire_on; effective next restart; reopening condition = their standing BOTH-AXES ask, today inexpressible under C1/R2 — the Q46 class); (2) the `search_n_trials` population build (per-slot cumulative at submit; hash-excluded, no idempotency impact). Prereg `098ea730d5f2` still resolves ≤07-21.

## 2026-07-20 (close, later⁸) — D302 SHIPPED (code + ops, no restart): Themes 2–5 round 1 — `forge yield-audit` dead-cell detector (FIRST RUN FLAGS 30 DEAD NAMES ≈21k wasted decided verdicts), campaign-audit wired into the 05:00 timer + a new healthcheck check, `RELAYS.md` ledger, corr-to-book contracts ask drafted+HELD. Theme 2 deliberately deferred (needs a deploy window)

**Operator "Let's continue to the next themes."** Buildable halves executed; operator gates untouched. NB the D-number race is now routine: D302 was reserved via RELAYS.md before the concurrent D303 landed — ledger file order is chronological, not numeric.

- **`forge yield-audit` (Theme 4, TDD 15 tests):** dead names (≥500 decided / 0 conversions) + cold cells (≥1000, <0.25× hypothesis baseline) with ALL the honesty guards (ghost-cut, clean-era window, farming-campaign exemption via the D299 registry, frozen-list names to retire-review, zero-baseline skip). Detection only, writes nothing; prints a staged v34/v37-terms rider draft ending in the prereg step. **First live run: 30 dead names (AAL, ADBE, AMZN, ARKK, BSX, DIA, DVN, EEM, EFA, GE, INTC, KO, LRCX, LUV, MS, MSFT, NEM, NKE, PEP, TXN, UNG, UPS, VZ, WFC, XBI, XLF, XLI, XLP, XLV, XOM — 513–1,139 decided each), 1 cold cell (event_momentum × swing_mid 0/1,359 vs baseline 0.0009 — arguably hypothesis-level), all 8 frozen-list names at 0 conversions.** OPERATOR DECISION AVAILABLE: prereg + stage the rider on the next bump — but cross-check against the CURRENT universe first (July-shrink departures save nothing), and their row-45 cross-check per the drafted terms.
- **Ops wiring (Theme 5c):** `daily_ranker_eval.sh` appends `campaign_audit.jsonl` daily (block dry-run verified on a live snapshot: ratios 1.379 / 1.062 / 1.273, none starved); `forge healthcheck` gains `campaign carriage` (WARN on starved/stale; OK-note before the first fire). Activates at the next 05:00 fire — no restart, no daemon touch.
- **`RELAYS.md` (Theme 5b):** live-relay ledger, one row each, maintained at triage time. A concurrent session adopted it within the hour (their D303 row).
- **Corr-to-book (Theme 3): `PROMPT_CRUCIBLE_CORR_TO_BOOK_ASK.md` HELD** — additive per-gated-config corr-vs-book scalar, telemetry-first; carrying is YOUR call (new-initiative ask). Companion: `docs/proposals/corr-to-book-feedback.md`.
- **Theme 2 NOT built** (deliberate): 2b cold-start floor generalization is not byte-identical → own deploy window; 2a ordinal targets follows 2c label provenance. `docs/proposals/learned-target-and-label-integrity.md` stands with sequencing 2c → 2b → 2a/2d.
- Gates: full suite at commit (below), mypy --strict clean, ruff clean; MANPAGE/architecture/proposal-status updated same commit. Watches unchanged (v39→v40 MR read ~07-22/23; ve read ~07-21; prereg `098ea730` resolves ≤07-21).

## 2026-07-20 (close, later⁷) — EV de-registration ACK'd (D303, docs-only, no build): Crucible retires `expected_value_estimator` → the §3.5 X2 kelly chain goes DORMANT automatically at their next registry publish (per-batch reload + `samplable_sizer_modes` filter, D258 class). Our funnel independently corroborates their NO-GO; one ledger flag relayed back

Triage of `FORGE_ev_deregistration_and_api_withdrawal_2026-07-20.md` → response
`PROMPT_CRUCIBLE_EV_DEREGISTRATION_RESPONSE.md` (held; **the ACK takes effect when the
operator carries it** — on their ack they delete the vertical + 13F plumbing). Full record:
[[D303]]. The numbers: `fractional_kelly` = 1/3 of uniform sizer-mode draws → 6.2% of
last-7d submissions (4,934/79,400, last at 21:13Z today); 30d funnel for EV-carrying:
**1 component / 12,652 decided** (rest of stream ~9.8% non-reject), median 13 vs 431
trades, 27.9% vs 1.1% zero-trade — the deletion frees our worst standing third of the
draw stream at zero cost (no code change, no bump; X2 rule text untouched, vacuously
satisfied). **Ledger flag relayed:** their "zero gated configs reference the id" misses
our 25 all-time EV components (latest `606eea73a5b81609`, v33, 07-15) — re-scan asked
pre-deletion. Sequencing asked: id-less snapshot publishes at/before engine deletion;
in-flight EV configs must fail SOFT (D245 wedge scar). FYI 2 (optbt.* withdrawal)
confirmed — zero Python imports here, `~/optbt_data` paths are layout not API (root-rename
boundary question relayed). FYI 3 (29/73 drift) recorded; the long-premium five
(vol_of_vol, skew_25d, butterfly_25d, realized_skew, iv_vs_index) noted as candidate
signal-add inventory (operator-gated). **Post-carry watch: verify kelly dormancy in the
first batch after their deletion publish (`sizer=fractional_kelly` share → 0; split funnel
reads on registry_hash, grammar stays v42).** NB D302 reserved by the in-flight ops-debt
session (RELAYS.md); this entry took D303; the new relay's row appended to RELAYS.md
(left uncommitted with that in-flight work).

## 2026-07-20 (close, later⁶) — Outbound relay DRAFTED, held for carry: `PROMPT_CRUCIBLE_HOUSEKEEPING_ASKS.md` (meta-king publisher ask + sma_slope writer-status ask + re-flag of the 07-08 charged-DSR relay — carry BOTH files together). The D300 standing watch is discharged into it

Docs-only. The relay bundles every queued cross-system loose end that does NOT depend on their pending reads; receipts/answers to the ve v38→v39 read (~07-21) and the v39→v40 MR read (~07-22/23) will be their own response relays when they land. **Reminder-clock: prereg `098ea730d5f2` (honest standard-basis wall ≤1.479) resolves ≤2026-07-21 — measure + resolve tomorrow; its outcome rides the next read-response.** Held-relay send/park calls still open (operator): FUNDAMENTAL_VALUE_PRECHECK (live door), SECTOR_ETF_XSECT_PRECHECK (recommend archive — D269 DON'T-BUILD), PATHC sizing (strategic, rides the Path-C reopen question).

## 2026-07-20 (close, later⁵) — Bucket B round 2 DONE (D301): the D248 needs-owner manifest CLEARED — ve `|move|` path removed (D235 pre-authorized; shadow-null now dual-null, `flip2` JSONL key gone), D094/D101 reward lane removed (D105-superseded), `is_percentile_emitting` removed, pytest-cov retired. Full suite 2006 green, mypy/ruff clean, daemon untouched

**Operator "ready for it."** Full record: [[D301]]. All three manifest doc bugs turned out already fixed; `TABLE_NAMES` relocate declined. With D295–D301 the entire post-promotion cleanup review is dispatched — no staged cleanup items remain. Standing watch unchanged: next outbound relay carries the meta-king publisher ask.

## 2026-07-20 (close, later⁴) — Bucket C CLOSED (D300, docs-only): DESIGN §12/§14 HISTORICAL banners, 2 stale docs archived (last D248-manifest doc stragglers), **quality-lane-rewire proposal status CORRECTED — it was ADOPTED at D252, the doc was stale** (re-wire clock 21/21 qualifying, latest Δ+0.636; F3 streak on the hygiene incumbent), DECISIONS.md volatile-fact fix, test consolidation declined. The three-bucket cleanup review (D295–D298, D300) is fully dispatched

**Operator answered all four bucket-C questions in-session** (full record: [[D300]]). **STANDING WATCH — next outbound relay carries:** the meta-king publisher ask (their `crucible-meta-king-publisher.timer` still runs daily; Forge's arm retired D190 — still wanted?). Surfaced-not-executed (future bucket-B pass, from the D248 manifest): ve `|move|` flag path (D235 pre-authorized), `compute_hypothesis_reward_weights` (D105-superseded), `is_percentile_emitting` (never wired), pytest-cov config, 2 cheap MANPAGE doc bugs, straddle-proposal banner.

## 2026-07-20 (close, later³) — D299 SHIPPED (code, no restart): campaign registry + region-carriage audit — Theme 1 of the post-promotion process review. The D287 experiment-cell pin now DERIVES from `forge/ranking/campaigns.py` (byte-identical, pinned by test); new CLI `forge campaigns list|audit`; Themes 2–5 drafted as operator-gated proposals in `docs/proposals/`

**Operator: "Let's draft them all up. Let's implement Theme 1"** (the post-promotion process-improvement brainstorm: the loop that produced both promotions — discover → verify-on-our-verdicts → concentrate → guarantee carriage → funnel read — becomes a named, auditable object instead of bespoke pins + watch lines).

- **Registry (`forge/ranking/campaigns.py`):** frozen `Campaign` records with lifecycle (candidate/confirmed/farming/converted/retired), origin, D-refs, funnel_read, retire_on, optional selection cell+slots / hypothesis / explicit `config_json`-dict member predicate. Seeds: `resid-vix-two-arm` (the D287 cell, 4 slots), `mr-timer-duration` (member mirrors the sampler's v40 scoping; converted_note = 65316ca4), `ve-exit-repair` (hypothesis-wide). Registry is CODE — edits are D-entries (sampler-pin convention); `validate_registry` fails malformed records at import.
- **`experiment_cells.py` derives from it** — `EXPERIMENT_CELLS`/`EXPERIMENT_CELL_SLOTS` = `active_selection_cells()/active_selection_slots()`; diversifier/queue/main untouched; **`test_d287_pin_derives_byte_identical` pins the derived values to the D287 constants** — ranking cannot move via a registry edit. Versionless (D193/D252 class), inert-identical until the next operator-gated restart (reboot-safe for the same reason).
- **Audit (`campaign_audit.py` + `forge campaigns audit`):** the D287 failure class standing — per farming campaign, ranked-lane member share vs holdout share (holdout bypasses ranking = unbiased pool-share estimate; the exact tell that caught D287 by hand). STARVED = ranked < 0.25× holdout share at ≥3 holdout members; exit 1 (scriptable); verdict-decision counts included. Read-only, snapshot convention, clock-free module. MANPAGE + architecture.md rows updated same commit.
- **Theme drafts (nothing ships off them):** `learned-target-and-label-integrity.md` (ordinal gate-tail targets, cold-start floor generalization, label provenance/activation-probe tripwires, young-cell holdout weighting), `corr-to-book-feedback.md` (contracts ask; relay text held INSIDE the doc pending operator go), `yield-auditor.md` (self-serve dead-cell detection; farming campaigns exempt), `ops-debt-roundup-2026-07.md` (duckdb write batching, relay ledger, audit-timer wiring 5c, calendar watches incl. prereg `098ea730` ≤07-21).
- Gates: test_ranking+test_cli+invariants 545 green mid-build; full suite at commit (below); ruff + mypy --strict clean. NB D297/D298 landed from concurrent sessions mid-build (D295 race pattern) — this work renumbered to D299; the D297 sampler removal doesn't touch anything mirrored here (verified).
- Watches unchanged (v39→v40 MR read ~07-22/23 → first registry-driven retire/re-scope decision for `mr-timer-duration`; ve v38→v39 read tomorrow → same for `ve-exit-repair`).

## 2026-07-20 (close, later²) — Bucket B CLOSED (D298): D206 made permanent (threshold proposer + script + tests deleted; empty YAML + fingerprint stay — identity-safe, verified), the two 07-18 tighten proposals DECLINED (§8.4 proposer stays armed — it woke correctly post-promotion), §5.5 auto_tune stays DISARMED with a recorded revisit trigger, OPEN_PROPOSALS rotation declined. Daemon untouched

**Operator answered all four bucket-B questions in-session** (full record: [[D298]]). Bucket B is now fully dispatched: item 1 = the D297 dead-branch removal (previous block); items 2–3 = condition-gated, standing (D287 cell pin → retires on Crucible's resid×vix relay; ve ghost-label cut → retires when `CLEAN_ERA_LABEL_CUT` passes 2026-07-18); item 4 = this decision set. Remaining from the cleanup review: bucket C (discussion-class — test consolidation axis, quality-lane rewire scheduling, DESIGN §12/§14 historical markers, the Crucible meta-king-publisher ask) — none urgent, none staged.

## 2026-07-20 (close, later) — Bucket B item 1 EXECUTED (D297, versionless): the capitulation elevated-vol override + 2 orphaned constants removed from `sampler.py` — unreachable since v35/D280; goldens un-re-pinned (byte-identical proven), enumeration+invariants 582 green, daemon untouched. NB second D-number race of the evening caught (this entry took D297; the v42 ack relay holds D296)

**Operator "Let's look at B."** Unreachability verified on both legs before removal (D280's empty regime pool in `_compatible_regimes` + `_regime_signal_params`'s single regime_filter call site; the two constants had zero readers outside the branch). Remaining B items: **D287 experiment-cell pin** — retire condition (Crucible's resid×vix two-arm read concluding) NOT met, stands; **ve ghost-label cut** — condition (`CLEAN_ERA_LABEL_CUT` > 2026-07-18) NOT met, stands; **§5.5 auto_tune self-apply** — disambiguated from the 07-18 proposals (those are the §8.4 gate-failure proposer, D034-guarded, working as designed post-promotion; the §5.5 disarm rationale STILL HOLDS — per-config estimand ≈ 0 under book-level promotion, re-arming today would spam loosen proposals) → decision put to operator; **threshold_proposer.py / D206 permanence** → decision put to operator.

## 2026-07-20 (close) — v42 ack relay CLOSED (D296, docs-only): v42 verified their side (158 decided; the 58-config exposure ledger reproduces to the digit); **xsect HOLDS at tier=2 — standing directive: tier=0 charges the widest ×1.5 class, do NOT emit until their per-name spread charging ships and they say the word**; Q52 ANSWERED (their queue-time annotation, gated on the OPERATOR's reference NAV); fold retirement verified survived

Loop closed on `FORGE_v42_ack_and_answers_2026-07-20.md` — no build. Their v41 read: 58 tier=3 xsect (funnel-isolated dupes) + 80 single-name true-tier stamps (working as intended). The retired-fold export (18:42:45Z, tier_2=20/tier_3=94) landed BEFORE the v42 restart and the 19:41Z daemon booted on it with the 118-name union intact via the tiered reader — pre-adoption code would have collapsed the pool to 24 (the D293 sequencing did its job; verified both sides). Their softener noted: CBOE far-OTM real bid/ask now folds into canonical chain reads, so the synthetic spread table (the stamp's cost meaning) shrinks over time. **Operator item outstanding: Q52's their-side annotation build waits on a declared reference NAV (ties to the live-deposit decision); no urgency — the shadow detects meanwhile.** NB a D-number race with the concurrent sweep (both took D295) was caught and this entry renumbered D296. Watches: their per-name spread-charging word (→ tier=0 rides a bump then); v39→v40 MR funnel read ~07-22/23; ve v38→v39 official read tomorrow; tomorrow's 05:00 retrain = first fit on de-ghosted + v40-fed labels.

## 2026-07-20 (night, later) — Post-promotion cleanup sweep #3 EXECUTED (D295, docs/ops only): 33 answered relays → `_archive/`, 4 completed one-off scripts retired (D241 pattern), STATUS May–June blocks rotated to `_archive/STATUS_2026-05_2026-06.md`, 7 stale May Q-headers bannered, forge_data leftovers cleared. Daemon untouched

**Operator "lets do A"** on the four-survey cleanup review (root files / src dead code / scripts+ops / docs+tests). Full record: [[D295]]. Root PROMPT relays 44 → 10 live/held + 1 research note; STATUS.md 2,522 → ~760 lines; `scripts/` down to the 9 operational entries; `~/forge_data` top level clean (king duplicate cmp-verified then deleted; 46M migration snapshot archived).

- **NOT executed (staged for separate decisions):** the orphaned capitulation rv_rank branch removal (licensed, byte-identical — needs a code commit); D287 experiment-cell pin (retires on Crucible's relay); the **auto_tune re-arm-or-delete call** — its D218 disarm was premised on the 0-promotion regime, and the proposer's own 07-18 append (two PENDING tighten proposals, committed here) shows it waking post-promotion. Operator review of those two PENDING proposals is open.
- **Held-relay pile for a future pass (operator calls):** SECTOR_ETF_XSECT_PRECHECK + SECTOR_VOL_MECHANISM_RESEARCH (companion research = D269 DON'T-BUILD), PATHC sizing relay (parked with Path C), SMA_SLOPE (their fix unverified), VE_PROGRAM/COMBINED_RELAY archive after the v39/v40 funnel reads, ALPHA_BUDGET_DSR/STALE_VOLUME after prereg `098ea730` resolves (≤07-21).

## 2026-07-20 (night) — GRAMMAR v41 → v42 DEPLOYED 2026-07-20T19:41:02Z (D294, commit `7833056`): xsect-union correction — the v41 tier=3 xsect share DROPPED same-day on Crucible's ledger retraction; single-name true-tier + ASML/COST STAND; contracts pin 1.33.0. First batch `514ff188` IN-SPEC (zero tier-3 xsect)

**Relay `FORGE_xsect_union_correction_2026-07-20.md`** — their ledger-verified retraction of their OWN tier-unpin relay's §3, which D292/D293 had adopted. Corrected truth: **xsect has ALWAYS ranked the all-tier union** (`_ALL_TIERS=(1,2,3)` + bulk superset; the promoted trend leg trades 89 underlyings incl. tier-1 + 66 outside curated 1+2; rank_k=20 was always genuine top-20-of-union selection). The tier stamp's ONLY engine effect on an xsect config is the **FillModel spread-table class** (tier=3 = tier_3_base × 1.5) — so v41's 15% share bought duplicate books at strictly worse charged costs.

- **v42:** `_stamp_tier` xsect branch = constant 2 (no rng); `_XSECT_TIER3_SHARE` tombstoned. Exposure: **58 handicapped configs** in the ~5.5h v41 window (`b9e00eff` 28, `9d8d642e` 30) — grammar_version splits them; treat as cost-handicapped dupes. **STANDS:** single-name true-tier stamp (their words: fixed a real mispriced-cheap cost bug), ASML/COST rider (their row-45 read confirms — ASML 100% wf-zero, COST 91%), tiered reader + the fold-retirement license.
- **Contracts pin 1.32.0 → 1.33.0** (their `150e368`: tier ge=0 = explicit union scope, the §20 fix for the emergent-pool defect the correction disclosed). The exact-match pin test was RED at triage (the D267 forcing function) — adopted riding the bump; purely permissive, we never emit 0. **Question back in the relay: should xsect stamp tier=0 once their §20 pin ships, and what cost class does it charge** — never guessed at.
- **Goldens:** verify clean under v41; under v42 the six regime goldens are **byte-identical** (their sequences host no xsect draws) and only the cohort golden re-pins (first divergence = its first rank-combiner config, position 12) — minimal-scope signature. Full suite **2008 green** twice; ruff/mypy clean.
- **Ritual:** stop 19:36:31Z → v42 bump + archive + pin test in the window → uncontended 2008 green → commit `7833056` (the pre-commit stash-conflict was `uv run` re-locking uv.lock to contracts 1.33.0 mid-hook — staged it, belongs in the pin-adopt commit anyway) → start 19:41:02Z (PID 3642136, NRestarts=0; journal v42, manual_bump; NB registry_hash rolled to `09b28bbbd7d79883` at their 18:00Z publish — coincident, unrelated). Window ~4.5 min.
- **First batch `514ff188` (19:50:24Z) audit: IN-SPEC** — v42 ×200, 0 failed; xsect 149 with **zero tier-3 stamps**; single-name tiers {3: 38, 2: 13}, zero mis-stamps; ASML/COST absent.
- **Their relabeled ablation recorded (D294):** trend `6bec53b4` is SPREAD-COST SENSITIVE (dWF −0.206 at 1.5×; far-OTM-wings execution-shortfall thesis), timer-MR `65316ca4` FLAT — 11-bar holds amortize entry costs, a gate-relevant robustness datum for the promoted book. **QuantIQ D418 integer-contract-floor ask → Q52 (LOW)**: needs a per-contract premium source at emission; three shapes relayed back ((a) their premium surface + our prefilter, (b) their queue-time preflight — the D278-principle home, (c) drop).
- Outbound held for carry: `PROMPT_CRUCIBLE_XSECT_CORRECTION_RESPONSE.md` (+ the tier-unpin relay now carries a supersession banner; its adoption confirmation stands). Watches: their fold retirement + §20 pin / tier=0 answer; Q52 answer; v39→v40 MR read ~07-22/23; ve v38→v39 official read tomorrow.

## 2026-07-20 (evening) — GRAMMAR v40 → v41 DEPLOYED 2026-07-20T18:13:22Z (D293, commit `a587296`): tier unpin LIVE — contracts 1.32.0 adopted with wiring, true-tier stamping, 15% xsect tier-3 exploration share, ASML/COST rider. First batch `b9e00eff` IN-SPEC. **Adoption CONFIRMED in the relay addendum — Crucible may retire the export fold**

**Operator "go on building and deploying v41"** (the D292 staging). All TDD; full suite **2008 green** twice (pre-window + uncontended).

- **Change set live:** (1) `_load_underlyings` → `load_universe_tiers_from_export` via one shared cache (`_load_universe_tiers_cached`; the pre-v41 `cache_clear` contract survives via an alias — the dual-cache draft poisoned cross-test state and was rejected); (2) `_stamp_tier` drawn LAST — single-name = true-tier lookup, xsect = tier=3 at `_XSECT_TIER3_SHARE=0.15` with empty-set dormancy BEFORE rng (D258 convention), relative_value keeps 2; (3) `universe_fingerprint` folds the tier split (H-3); (4) ASML/COST join the structural exclusion (v37 terms; row-45 cross-check requested); (5) contracts pin 1.31.0 → 1.32.0 — adopted WITH wiring, unlike the D267/D271 pin-only precedents.
- **Test surface:** `UNIVERSE_TIER3_SNAPSHOT_2026_07_20` (94 names ⊂ the 07-16 union) + the conftest tier pin (third leg of the D274 pattern) + 5 new tests. **Goldens 7/7 re-pinned environment-matched** — onset = the first POOL-TAPPING config (cohort positions 0-1 are relative_value and survive byte-identical; everything else moves at 0 via the 118→116 pool shift, the v37 signature).
- **Ritual:** stop 18:08:37Z → v41 bump + archive + pin test in the down-window → uncontended 2008 green → commit `a587296` (hooks; one formatter round-trip) → start 18:13:22Z (PID 2832927, NRestarts=0; journal: v41, `registry_hash=b3277da2af738788` unchanged — the boundary is ours, manual_bump, zero tracebacks; window ~4.7 min).
- **First batch `b9e00eff` (18:42:14Z) audit: IN-SPEC** — v41 ×200, 0 failed; single-name tiers {3: 41, 2: 8}, **zero mis-stamps**; xsect 151 configs, 28 tier=3 (18.5%, in band at n=151) — the tier-3 xsect cells pass prefilters on the first batch; ASML/COST absent; D287 floor held (4).
- **Relay `PROMPT_CRUCIBLE_TIER_UNPIN_RESPONSE.md` updated with the deploy addendum: ADOPTION CONFIRMED — the fold is no longer load-bearing for us; funnel signatures at the boundary spelled out (new tier=3 xsect cells; config_hash break on tier-3-name single-name genomes; ASML/COST vanish; fingerprint step).** Watches: xsect tier-3 prefilter carriage over the next batches (report like ref_trailing_return); their fold retirement; v39→v40 MR read ~07-22/23 (unaffected); ve v38→v39 official read tomorrow.

## 2026-07-20 (later) — Tier-unpin reply TRIAGED (D292, docs-only): **THE 2-LEG PROMOTED — second portfolio ever, first via the auto-campaign lane**; our tier-3 claim CORRECTED (folded tier_2 key — drawn all along; the pin is the STAMP, the cost is xsect); contracts 1.32.0 verified; **v41 STAGED, awaiting operator go**

**Relay `FORGE_tier_unpin_and_promote_2026-07-20.md`** (their D291 reply). Everything verified before recording (D292):

- **PROMOTION:** trend `6bec53b4` + timer-MR `65316ca4` (spec `b36f49a4fe230f96`) passed ALL 13 §8.7 gates (run `de00e099`: cpcv_p25 1.7236 / WF med 2.3063 / DSR 0.9991 @ charged n=99 / PBO 0.156 / min_oos 840 / leg corr 0.065); sized deflated ~1.3-1.45. The timer-MR leg = the exact cell v40 farms (their words: "v40 is now farming the cell it came from"). v40 rows already in their queue; v38-vs-v39 MR null-control CLEAN (14.4%→15.0% flat) → the v40 read (~07-22/23) attributes to D291. Their census independently confirms the v40 boundary (timer = MR-only edge; worst class in trend swing_long 16.8% vs chandelier 38.5%).
- **Record correction (verified, our error):** "94 tier-3 names never reach our pool" was WRONG — tier-3 is FOLDED into the export `tier_2` key (114 = 20 curated + 94 tier-3, verified subset+subtraction); we've drawn all 94 for weeks. Our funnel: the cited names are drawn-but-DEAD single-name (ASML 641 decided/0 comp, BKNG 1,254/0, COST 1,544/1, LLY 1,372/0, SOXX 1,367/0; BKNG/SOXX/LLY already v37-excluded). The REAL pin = the literal `tier=2` stamp; its cost is CROSS-SECTIONAL — the engine resolves xsect ranking pools from the stamp, so every xsect config ever emitted ranks the TRUE 20-name tier-2 pool (rank_k=20 = take-everything; the census's best band has zero selectivity today) and the 94-name tier-3 xsect pool has NEVER been sampled.
- **Contracts 1.32.0 VERIFIED live** (`0bc45a8` at ../crucible_contracts): `load_universe_tiers_from_export` → 4/20/94 on today's export; old reader returns the identical 118 union (both checked) → the new export is a no-op at our next restart; the 1.31→1.32 pin gap is minor-only (no hard fail). **Fold held until we confirm adoption** — adoption must precede their fold retirement (old reader would shrink 118→24), no time bomb.
- **v41 STAGED (`docs/proposals/v41-tier3-xsect.md`), one operator window:** pin 1.31.0→1.32.0 + reader switch (pool no-op) + true-tier stamping on single-name (config_hash break for tier-3-name genomes at the boundary — flagged) + **15% xsect tier=3 exploration share (xsect-first per their suggestion; single-name shares untouched)** + rider: ASML/COST join the structural exclusion (auto-tightening; row-45 cross-check requested). Deploy relay carries the adoption confirmation so they retire the fold.
- §2 closed cleanly: ONE writer restart at 22:27:21Z (their ~23:30Z was a logging error); our 22:51Z first-carriage batch = +24 min, consistent.
- Outbound held for carry: `PROMPT_CRUCIBLE_TIER_UNPIN_RESPONSE.md`. Watches: operator go on v41; their ve v38→v39 official read (tomorrow; early n=499 underpowered); v39→v40 MR read ~07-22/23; resid-satellite battery on the promoted chassis (their side, today).

## 2026-07-20 — GRAMMAR v39 → v40 DEPLOYED 2026-07-20T15:39:23Z (D291, commit `3901f80`): MR timer cell first-class — required pick weighted to time_stop 0.65 + n_bars U[8,12] bucket-wide (capitulation frozen). First batch `2ee22b66` IN-SPEC. Combined relay §2/§3 also closed: ref_trailing_return heal VERIFIED (the D290 WATCH is CLOSED), tier zero-draw ANSWERED (pinned-not-sampled; contracts gap proposal relayed)

**Operator "let's fix this" on Crucible `FORGE_combined_relay_2026-07-20.md`.** Their §1: the v36/v38-era timer-MR supply CONVERTED — 1,087 components/5d (68 at cpcv≥1.0), head `65316ca4` (11-bar hold) lifting the 2-leg trend+timer book to **cpcv_p25 1.7236 / WF 2.3407 raw** (honest decorr 0.347, selection PBO 0.156, DSR 0.9993 @ N=85, tail ablation passes both ways; frozen spec `b36f49a4fe230f96` accrues forward evidence; sizing deflated to ~1.3-1.45, campaign go/no-go = operator's).

- **Premise correction (in relay + D291):** their "15% timer-share" mis-attributes v38 (that 0.15 = trend/swing_long's OPTIONAL draw); MR's timer is a `required_from_set` pick at uniform ~50%, and the family actually came from the v36 swing_mid U[8,15] duration prior. **Reproduced on OUR verdicts pre-build** (decided ≥07-14, MR excl. capitulation): timer n_bars 8-12 converts **15.0%** vs 13-15 at 11.9% vs param-less default-5 at **5.3%** (worst MR cell, n≈5,000); timer 10.7% vs target_exit 9.9% overall.
- **v40 (both knobs scoped to MR excl. the veto-frozen capitulation directional):** (1) `_pick_required_exit` — the required pick WEIGHTED to time_stop at 0.65 (was uniform; shifts share AWAY from target_exit, the D257-safe direction; membership guard deactivates to uniform if the set changes shape); (2) `_MR_TIME_STOP_NBARS_RANGE` U[8,12] at ALL MR buckets — swing_mid narrowed from [8,15], the param-less default-5 emission retired for MR. Capitulation keeps D270's U[5,15].
- **Goldens:** 7/7 re-pinned environment-matched (OLD code reproduced every constant; every first divergence = the sequence's first MR config, positions 4/4/5/5/6/5/5; non-MR positions byte-identical). Stream re-pin moved two landmarks: first ivol carrier → position 16 (d263 scan widened to 25), first capitulation genome → position 30 (d270's v29-vs-v31 divergence now asserted on a live 40-window). Two v36 tests superseded in place. GRAMMAR.md S5 table repaired for the MISSED v39 ve row (D290 doc drift).
- **Ritual:** stop 15:35:12Z → bump + archive + test pin in the down-window → uncontended **2003 green** + ruff + mypy clean → commit `3901f80` (hooks Passed) → start 15:39:23Z (NRestarts=0; journal: v40, `registry_hash=b3277da2af738788`, manual_bump, clean reconcile, zero tracebacks; down-window ~4 min). Emission proof (live registry, 4k cold): MR timer 0.625, n_bars uniform {8..12}, zero out-of-box; capitulation 0.41 / [5,15] intact.
- **First batch `2ee22b66` (15:56:36Z) audit: IN-SPEC** — v40 ×200, 0 failed; MR non-cap 65 with timer 45 (69%), n_bars {8:5, 9:8, 10:6, 11:16, 12:10} all in-box, zero param-less; 3 capitulation untouched; D287 cell floor held (5).
- **§2 CLOSED — ref_trailing_return heal VERIFIED:** ve veto carriage 0/0/0 (first three v39 batches) → 3-11/batch from `f49c554c` (07-19T22:51Z; their stated writer restart ~23:30Z — carriage flipped earlier, noted in relay), ~40% of submitted ve. The D290 WATCH + writer ask are closed; no dormancy pull.
- **§3 ANSWERED — tier is pinned, not sampled, three ways:** contracted universe read carries tiers 1+2 only (tier_3 absent from the export shape); their tier_1 = exactly {SPY,QQQ,IWM,DIA}, T1.4-excluded by design; `tier=2` stamped at construction. Unpin needs a contracts gap fill FIRST (hard rule #2) — relay proposes (a) additive `tier_3` key in `universe_tickers.json` + contracts reader, or (b) blessing their new PIT `universe/asof_date=*/all_eligible_tickers.parquet` as a contracted surface; D245 both-restart sequencing flagged. Forge stages a 15% tier-3 exploration share + true-tier stamping once readable. Honest frame: their own breadth ablation says coverage axis, not edge.
- **First de-ghosted 05:00 retrain RAN (this morning 05:00 PDT):** F3 343,938 rows / train_auc 0.808 / brier 0.0468; streak **14/3 PASS** on the hygiene incumbent (margin +0.15 dominant); tail oos_r2 **−2.24** (was −16 ghosted), rmse 0.688; re-wire streak 20/3 PASS (fresh delta +0.636). ve per-family probe n=149 Δ=−0.201 FAIL = post-cut small-n — wait for the v39/v40 repair cohort to regrow before reading it.
- Relay `PROMPT_CRUCIBLE_COMBINED_RELAY_RESPONSE.md` held for carry (funnel ask: `--compare v39 v40 --hypothesis mean_reversion`; v38-vs-v39 = clean MR null-control). Watches: their `funnel --compare v38 v39 --hypothesis volatility_event` (repair cohort), the tier-3 contracts decision, the frozen candidate-book's forward clock.

## 2026-07-19 — GRAMMAR v38 → v39 DEPLOYED 2026-07-19T20:28:48Z (D290, commit `50a0337`): ve exit-schema repair + ref_trailing_return veto sampling + iv_term_slope loosening + the ve ghost-label training cut. First-batch audit pending

**Operator "approved, build v39 and deploy when complete."** All four items TDD'd and live:

- **ve exits (S5 set edit, explicitly approved):** event_passed_exit OUT (required→forbidden; it always ran Crucible's fallback mode — a hard cut at entry+n_bars — and the v22/D169 ladder put 60% of ve batches in the cratered region); **time_stop required with n_bars ~ U[4,7]**. The D169 ladder retired with a tombstone.
- **ref_trailing_return ve veto:** SAMPLED never pinned (threshold U[-0.03,-0.02] op ">", reference {SPY,QQQ}, window [3,10]); rides the generic ~0.5 veto share with the per-id C1 macro guard; registry-gated dormancy; horizon entry added. `check-activations` can't probe a regime-role id ([UNCHK] expected) — liveness evidence is Crucible's own certified probe runs.
- **iv_term_slope floor 0.01 → 0.0077** (×1.3 loosening, ceiling kept).
- **ve ghost-label cut (versionless companion):** `VE_GHOST_LABEL_CUT=2026-07-18` enforced at ALL five trainer choke points (hypothesis weights via `_iter_hypothesis_outcomes`, the D105/D106 weighters via `_component_rate_sums`, trade_rate_priors, `build_dataset` (F3+tail), `compute_mature_arms`) — the 34,273 ghost ve verdicts / 657 fictional components (~10% of positive labels) stop feeding the learners; first de-ghosted F3/tail fit at the next 05:00 timer.
- **Emission proof (live registry, 4k):** ve 784 — event_passed 0, time_stop 784/784 n_bars uniform {4..7}, veto 48% SPY/QQQ balanced, iv_term_slope floor reached (0.0080); all hypotheses reachable. **Goldens:** 7/7 re-pinned environment-matched (every first divergence = a ve config on the new stack; two cross-golden prefix asserts tightened to the new mutual split @1).
- **Ritual:** preflight 1999 green → stop 20:25:11Z → v39 bump + header + archive + test pin in the down-window → uncontended **1999 green** → commit `50a0337` → start 20:28:48Z (PID 1411117, NRestarts=0; journal: v39, `registry_hash=a20cdb27dfb92b25`, manual_bump, clean reconcile, zero tracebacks).
- **First batch `f74d10a8` (20:53Z) audit:** v39 stamp ×200, 0 failed; 15 ve configs — **event_passed 0/15, time_stop 15/15 with n_bars {4:3, 5:6, 6:2, 7:4}** — items 1+3 fully in-spec; D287 cell floor held.
- **WATCH — ref_trailing_return veto INERT (their writer):** 0/15 submitted ve carry it; ve `predicted_activations` rejections 1–7/batch (v38) → **182** (v39). Direct writer probe: market_realized_vol → 934 activation dates, **ref_trailing_return → 0** — their evaluator computes it (96317de) but the writer's activation path doesn't serve it (the D254 class; OUR prefilter catches carriers pre-submission, so Crucible sees no dead configs). Cost ≈ 48% of ve enumeration drawn-then-killed (~177/batch of 369 ve attempts ≈ 3.5% of total). SELF-HEALING when they wire the writer — no Forge action; fallback = pull the pool dormant (small bump) if their wiring is far off. Flagged in the relay with the probe evidence.
- Relay `PROMPT_CRUCIBLE_VE_PROGRAM_RESPONSE.md` updated with the deploy addendum + the writer-activation ask (funnel: `--compare v38 v39 --hypothesis volatility_event`).

## 2026-07-19 — ve program relay TRIAGED (D289, docs-only): their §3 bug CONFIRMED in our emission; §5.1/§5.4/§6 ANSWERED with data; **v39 staged, awaiting operator go** (`docs/proposals/v39-ve-program.md`)

**Relay `FORGE_ve_program_relay_2026-07-19.md`:** 23/25 stored-cpcv ve components were GHOSTS (their put_wall/gex/vex/cex staleness blind spot, fixed v3→v4). Key triage results, all verified:

- **§3 (the bug):** every ve config carries `event_passed_exit` (required_always) with NO `event_indicator` → always their FALLBACK mode = hard cut at entry+n_bars. Our v22/D169 ladder {3,5,8,13,21} put **60% of every ve batch in the cratered region** — and our funnel shows the cliff exactly there: ve conversion v21 5.9% → v22 0.7% → ~0 (v24+), archive-verified the ladder was the ONLY v22 ve-cell change. This answers their §5.1.
- **§5.4:** AMD ve-dominance was ghost-yield era (39% at v20, ~20% v19–v21) and already dispersed (~2% since v22; v33+ top name 3%). `factor_cell_discounts` is DORMANT (sampler accepts, production never passes).
- **§6:** no Forge-side cache to re-key (feature reads = live client to their writer, per-iteration). REAL exposure = training labels: **34,273 ve verdicts / 657 fictional components in [06-10, 07-18) ≈ 10% of all positive labels** feeding F3/tail/yield/name-weight trainers → ve ghost-label cut staged (v39 item 4).
- **Memory pillar corrected:** ve mixed-book PBO 0.178 → clean re-validation **0.40** (real-but-marginal; NO solo promotion case).
- **v39 staged (operator-gated; includes an S5 set edit — D236/D257 precedent, needs explicit approval):** (1) ve exits: event_passed_exit OUT, time_stop required with n_bars U[4,7]; (2) `ref_trailing_return` veto SAMPLING (registry-live, macro; threshold U[-0.03,-0.02], window [3,10], ref {SPY,QQQ} — never pinned); (3) iv_term_slope ×1.3 loosening; (4) the ghost-label cut. NOT building: genome ports / stabilization filters / veto pins (their honesty block). Floor holds (their #5).
- Outbound held for carry: `PROMPT_CRUCIBLE_VE_PROGRAM_RESPONSE.md` (the §5.1 table + §5.4/§6 answers + staging).

## 2026-07-16 — GRAMMAR v37 → v38 DEPLOYED 2026-07-16T23:22:27Z (D288, commit `c827783`): trend swing_long exit-mix shift — time_stop optional draw 0.5→0.15 in that cell only. First batch `ab8a8acc` (23:50Z) IN-SPEC: swing_long timer 2/12 (17%) / chandelier-only 5/12 (42%), swing_mid 45% untouched, surviving timers n_bars {10, 9} ∈ U[8,10], v38 stamp ×200, D287 cell floor held (4 resid x vix)

**Relay:** `FORGE_trend_swinglong_exit_mix_2026-07-16.md` (composes with the v36 duration prior — mix share vs duration-given-carried). **Verified on OUR verdicts pre-build** (n=50,906, all strata, same window): timer 46.4% share / 15.1% component rate, chandelier-only 21.6% / 35.5%, other 32.0% / 27.5% — the monotone ordering their census reported, ~1pp off their xsect-only numbers. Mechanism = exactly our S5 draw (time_stop the lone trend optional at p=0.5; chandelier half the required 50/50 pick).

- **Change:** `_optional_exit_pick_p()` — S5 optional Bernoulli, 0.5 everywhere except (trend, swing_long, time_stop) → **0.15**. One knob per their "not prescribing exact weights"; NOT 0 so the surviving ~15% keeps feeding the funnel's read of the v36 U[8,10] prior (their census window mostly predates it). rng consumption identical on every path (#6). Emission proof: swing_long timer 19.8% / chandelier-only 46.9% (n=96); swing_mid 48.0% untouched; every surviving carrier n_bars ∈ [8,10]; MR untouched (required-pick, tested).
- **Goldens:** 6/7 re-pinned environment-matched (OLD code reproduced every constant exactly; first divergences = trend swing_long exit draws, PRE@2 / V27@11); cohort golden hosts no scoped flip — untouched.
- **Ritual:** preflight 1986 green (sole flag = in-flight build) → stop 23:18:39Z → v38 bump + header + archive + test pin in the down-window → uncontended **1986 green** → commit `c827783` (hooks Passed) → start 23:22:27Z (PID 3400736, NRestarts=0; journal: v38, manual_bump, clean reconcile 27/4,816, zero tracebacks). Boundary clean (same universe/registry as v37; v37 lived ~3.2h — flag the short life in the funnel compare).
- Relay `PROMPT_CRUCIBLE_V38_DEPLOYED.md` written (held for carry). First-batch exit-mix audit to follow.

## 2026-07-16 — D287 DEPLOYED 2026-07-16T21:35:36Z (commit `aab54ea`, versionless): experiment-cell selection floor — the resid x vix arm was P-gate-starved at SELECTION after D286 fixed its draw; now 4 reserved slots/batch. First batch VERIFIED: ranked resid mix 4 vix / 11 hurst (was 0/14)

**Finding:** post-v37 ranked-lane resid ran 14 hurst / 0 vix (p≈0.006% under the uniform coin); the lone vix arrival came via the HOLDOUT. Offline diagnosis (6k enumerate + live models): generation balanced (31/32) but only **16% of vix-resid clear the P≥0.02 hard gate vs 87% hurst** (median P 0.011 vs 0.033) — F3 learned from hurst-carried history and gates the experimental arm out at eligibility; below-floor priors pin to 0.0, unreachable for the fill. The D136 arm floor can't scope to the pair (arm key = (role, indicator); vix_term_slope matured within days of v27).

- **Change (operator "lets do 2", TDD):** `forge/ranking/experiment_cells.py` (pin `{(residual_momentum, vix_term_slope)}`, 4 slots ≈ 2% of batch, retire-on-relay) + diversifier reservation phase 0b (deterministic, double-spend-safe, empty-pool-reserves-nothing; `None` byte-identical asserted) + queue threading + main wiring + journal audit line. Versionless ranking change (D193/D252 precedent), full ritual: preflight OK → stop 21:32:18Z → uncontended **1981 green** → commit → start 21:35:36Z (PID 2365796, NRestarts=0, clean journal).
- **First post-D287 batch `823903c2` (22:02Z):** journal `experiment_cell_floor: {'residual_momentumxvix_term_slope': 4}`; DB audit: resid ranked mix **4 vix / 11 hurst**; hygiene 200/200 non-NULL; excluded names 0. The vix arm now gets ~190 submissions/wk — Crucible's two-arm read is fed.
- Relay `PROMPT_CRUCIBLE_V37_DEPLOYED.md` updated with the selection-side finding + fix. Watch: nothing standing; the pin retires on their relay.

## 2026-07-16 — GRAMMAR v36 → v37 DEPLOYED 2026-07-16T20:06:54Z (D286): SOXX/LLY/GS/MSTR exclusion + resid 50/50 coin + Q50 conftest universe pin — AND the D284 hygiene-score recording is LIVE (first batch 200/200 non-NULL)

**Deploy evidence (operator "deploy v37 so the hygiene column starts populating"): commit `a593dd4`, started 2026-07-16T20:06:53Z, PID 1815241, NRestarts=0.** Ritual: preflight full suite **1975 green** (sole flag = the in-flight build, documented v33–v36 pattern; the Q50 pin held — no golden race this time) → stop 20:02:31Z → v37 bump + header + archive + integration test pin in the down-window → uncontended **1975 green** → commit (all hooks Passed; NB hooks need `.venv/bin` on PATH for the `python` entry) → reset-failed → start. Journal: `grammar_version=v37 registry_hash=90a90a592439bd5b`, `recorded manual_bump row for v37`, `registry_loaded_from_export`, clean reconcile (26 batches / 4,729 newly gated), zero tracebacks. **Boundary is CLEAN for the funnel compare** — no universe change rides this restart (the July export was live under v36 since 08:49Z).

- **First batch `9050d39c` (20:29:00Z) in-spec audit:** 200 submitted / 0 failed; grammar stamp v37 ×200; **excluded names 0/200** (SOXX/LLY/GS/MSTR/BKNG/BRK.B all absent); **`shadow_scores.hygiene_score` 200/200 non-NULL, range [0.336, 0.471]** — D284 activation CONFIRMED; gate-tail + F3 + 5% holdout lines all normal.
- **Build (TDD, all licensed):** exclusions frozenset +4 (row-45 guard, v34 terms); resid learned-slice bypass → uniform coin on the D276 pair (test reproduces the starvation adversarially: 4/518 vix pre-fix → ~50/50); conftest `_pinned_universe` autouse pin (07-16 snapshot, 118 names, fp `260321aaaad60241`) + `real_universe_loader` for 13 loader tests — **Q50 CLOSED**. All 7 cold-start goldens re-pinned environment-matched (OLD code reproduced every constant exactly; every first divergence = a scoped single-name draw). Emission proof: 0/3000 excluded-name draws, 114 distinct names, all hypotheses reachable.
- **WATCH (resid submitted mix):** batch 1's 7 resid configs are all hurst-gated. The GENERATION coin is verified 50/50; the submitted subset passes through gate-tail ranking, which can re-skew it (the tail model learned on hurst-carried history). Small n — recount over the next batches; if the submitted mix stays hurst-heavy the two-arm supply ask needs a ranking-side look (exploration holdout rows are the unbiased sample to read).
- **Hygiene clock ETA:** F3 streak rows switch to `margin_source="hygiene"` once decided verdicts on hygiene-scored submissions reach 150/day (~1–2 days, verdict latency).
- Relay `PROMPT_CRUCIBLE_V37_DEPLOYED.md` written (held for carry): version string, funnel-compare ask, resid-arm confirm ask.

## 2026-07-16 — Learned-clock audit: F3 margin slide DIAGNOSED (measurement artifact; model intact) → D284 hygiene-score comparator SHIPPED (`6be2b91`) + D285 §8.6 tail clock RETIRED (`962291d`). No restart — the recording half activates at the next operator-gated deploy

- **Finding (operator asked "dig into the F3 slide / turn down the wf_p25 tail?"):** since the gate-tail flip (D252, 07-06T20:26Z), `shadow_scores.composite_score` records the lane's OWN ordering score (`queue.py`: `composite = prior` under `gate_tail_ordering`) — the F3 clock's incumbent and every §8.6 pairing became self-referential. The F3 slide (+0.35→+0.06, onset 07-12 = verdict latency) decomposes as: stronger incumbent (gate-tail score AUC 0.52–0.65 vs old composite 0.43–0.57) + P≥0.02 range restriction (ranked medP 0.036 vs holdout 0.0007) + stream concentration/mix (P-IQR 0.052→0.024 by v35/v36; MR share 11%→42%; positive rates HOLD ~5%). **Holdout-cohort AUC 0.801 ≈ pre-flip 0.828 → F3 skill INTACT.** The legacy margin will keep compressing toward the 0.05 bar — an artifact, NOT a revert signal; judge F3 on holdout pooled AUC until hygiene rows accrue. Tail model verdict: **KEEP** (abs tail IC 0.31–0.54; re-wire clock +0.44…+0.68, SPRT promote +22.4; the §8.6 FAIL 0/3 was tail-vs-itself).
- **D284 (`6be2b91`):** `shadow_scores.hygiene_score` = model-free §6.2 composite (`ranker.score(report, 0.0)`) recorded per submitted row; `evaluate_shadow(incumbent="hygiene")` (paired rows); `ranker-model eval` hygiene block; the daily F3 streak self-switches to the hygiene incumbent when its fresh window ≥150 (`margin_source` + both margins per row). **Column stays NULL until the next operator-gated restart** (v37 deploy carries it) — verified fallback against a live snapshot.
- **D285 (`962291d`):** §8.6 streak block removed from the timer script; `forge status` clock + SPRT lines removed (tombstone printed), adoption guard arm 2 → re-wire clock Δ (`gate-tail-lane=ADOPT +0.596`); healthcheck "wf_p25 drift" → "gate-tail drift" (rewire `delta` series) — cleared the standing near-WARN; **OVERALL=OK 14/14 verified live**. History JSONL stays; `eval-robustness` stays observational (read its ABSOLUTE spearman, not the delta, until hygiene rows accrue). Timer picks up the script at the next 05:00 fire.
- Gates: **full suite 1972 green** (contended, post-both-commits), invariants 122 green, mypy --strict clean, ruff clean, ranking/submission byte-identical (telemetry-only surfaces).

## 2026-07-16 — GRAMMAR v35 → v36 DEPLOYED 2026-07-16T08:49:57Z (D282): exit-duration priors live — trend swing_long time_stop U[8,10], MR swing_mid U[8,15], capitulation veto-frozen U[5,15]. Q50 BIT MID-DEPLOY (their July universe export → second golden re-pin). Cohort follow-ups triaged (D283): §4 answered with data, 2 asks staged as v37

**Deploy evidence (operator "deploy it"): commit `3f2ca88`, DEPLOYED 2026-07-16T08:49:57Z.** Ritual: preflight #1 **NO-GO — Q50's predicted failure, live**: Crucible's July tier export (`universe_tickers_2026-07-16T082754Z.json`, −FCX/WBD/WDC/VIX/BKNG/XLY/XLB +APH/MDT, 118 names — pre-announced in their same-day relay §2) landed **17 min before the gate ran** and broke all 9 goldens at position 0 (impossible for the v36 draw — the tell). All 7 goldens re-pinned a SECOND time, licensing harness re-run environment-matched (old-vs-new code under the SAME universe: every first divergence == the variant's first scoped attempt; the DSJ-vs-PRE relational split returned to position 3 — the first re-pin's 3→2 was an old-universe word-index coincidence; assert restored). Preflight #2: suite **1969 green**, sole flag = the in-flight build itself (documented v33-v35 pattern) → stop 08:44:50Z (exit 143) → v36 bump + header + archive + test pin in the down-window → uncontended **1969 green** → format→commit (FIRST TRY, all hooks Passed, BEFORE start) → reset-failed → start 08:49:57Z. New daemon **PID 237030, NRestarts=0**; journal `grammar_version=v36 registry_hash=b079eb7934cc5b66`, `recorded manual_bump row for v36`, `registry_loaded_from_export`, clean reconcile (18 batches / 2,924 newly gated), **0** traceback/mismatch lines; §7.3-blocked at start (depth 648 > 600 — normal; watcher armed for the first batch's in-spec audit). **NOTE: the universe shrink activated at THIS restart** (the v35 daemon held the June set to its end) → the v35→v36 funnel boundary carries BOTH changes — flagged in the deploy relay. Q50 ESCALATED with resolution chosen: conftest universe pin (D274 shape) staged as v37 item 3; Q50 closes when v37 ships. **First v36 batch VERIFIED (batch `ffc30152`, 2026-07-16T09:23:45Z, 200/200 submitted, 0 failed; DB-snapshot audited): 200/200 stamped v36; ZERO violations** — trend swing_long 9/9 carriers in [8,10]; MR swing_mid 30/30 in [8,15] (zero floor mass); capitulation 1 carrier in-box (n_bars 12 ∈ [5,15], veto shape); bare cells exactly the untouched ones (trend swing_mid 46, MR swing_short 3, vol_event 9); no unscoped n_bars leak anywhere; **0 draws on the 7 July-export-removed names** (the universe shrink is live in the daemon, as flagged).

**Cohort-read follow-ups (D283, docs-only fold):** (1) their §4 arm-share question ANSWERED with data — capitulation = **23/1,741 = 1.3% of submitted MR since v35 == their decided 1.3% EXACTLY** (the 8.6% was cold-mix enumeration basis; compression = learned-lane composition on a no-history family; nothing eaten; their fair-read timing stands); dte mix 10:13 = structural 1:3 × the D105 learned-bucket fallback, drifts structural as cells mint. (2) §2 ask staged (v37 item 1): SOXX/LLY/GS/MSTR → the v34 frozen-list mechanism (96-99.8% WF-zero at n≈1,000 each; ~4.4k wasted draws/wk). (3) §3 ask staged (v37 item 2): resid gate-mix **50/50 pin** — diagnosis VERIFIED at `_pick_regime` (learned posteriors compose onto the v33-pinned pair → ~94% hurst); fix = per-directional bypass, the D119 precedent. (4) §1 good news recorded: **the v35 bare-drop CONVERTS** (median 13 OOS trades vs 4; WF-zero 70% vs 97.3%; CPCV computing for the first time in the family). (5) §5: **ve-floor decision point re-anchored → `funnel --compare v32 v35 --hypothesis vol_event`** (~1 day; the v33 stamp lived 5.3h — unreachable read); floor stands. **Relays held for carry: `PROMPT_CRUCIBLE_V36_DEPLOYED.md` + `PROMPT_CRUCIBLE_COHORT_FOLLOWUPS_RESPONSE.md`. v37 build (`docs/proposals/v37-cohort-followups.md`) awaits operator go.**

## 2026-07-16 — v36 exit-duration priors BUILT + verified (D282): trend swing_long time_stop → U[8,10], MR swing_mid → U[8,15]; capitulation VETO-frozen at U[5,15] both buckets (Crucible's scoping response, cohort hygiene). AWAITING OPERATOR DEPLOY GO

**Crucible's `FORGE_v36_scoping_response_2026-07-15.md`: readings 1+2 CONFIRMED (strong forms, zero floor mass — n_bars=5 measured -0.382 p25-proxy vs +0.161 at 8), capitulation inheritance VETOED (not merits — the v35 bare-drop pane needs a few hundred configs; [8,15] re-opens after the v34-vs-v35 pane read); "Build on."** Built (full record: [[D282]]): `_time_stop_nbars_range` range table (capitulation resolves FIRST, veto-frozen; then MR×swing_mid, trend×swing_long), `_build_exits` gains `bucket`, TDD 5 new tests + 1 legacy flip. **Key discovery: the enumerate goldens live on ONE shared rng stream (rejected attempts included)** — all 7 re-pinned under a mechanical license (old-behavior monkeypatch reproduces every prior pin byte-exact; every first divergence == the variant's first scoped attempt; healing between scoped attempts via `_randbelow` rejection variance); one relational assert legitimately moved (DSJ-vs-PRE split 3→2, cause pinned in comment). Suite: enumeration 436 green, unit+invariants **1907 green**, ruff + mypy --strict clean. **Emission proof (3000 cold, live registry, 0 violations):** trend swing_long 38/38 in [8,10]; MR swing_mid non-cap 163/163 in [8,15] zero floor; capitulation floor mass PRESENT (veto honored); zero unscoped leaks. **NEXT: operator "go" → deploy ritual (v36 bump in the down-window, archive, test pin; ruff format before commit; commit before start); deploy relay asks `funnel --compare v35 v36`.** Tree dirty with the build until then (single-folder workflow, short window).

## 2026-07-15 — Exit-duration priors relay triaged (D281): both time_stop n_bars asks VERIFIED (trend swing_long default-5 mass 6,775 configs/wk; MR swing_mid 11,489) → staged as v36 (trend swing_long → U[8,10]; MR swing_mid → U[8,15]); operator go needed; docs-only

**Inbound `FORGE_exit_duration_priors_2026-07-15.md` — full fold: [[D281]].** Their probe evidence: the trend swing_long day-5 timer takes 84-88% of exits and cuts WINNERS (n_bars=10 improves cpcv 6/6 + wf 5/6 + maxDD, inside their §6.5.2 [3,10] box; explicit do-NOT-pass-10 tail warning); MR swing_mid's [5,15] box is right but the floor hurts the bounce (plateau 8-20, peak 12). **Verified exact against our live stream (117,400 submissions 07-08→07-15):** trend swing_long w/ time_stop = 6,775 (5.8%), ALL param-less → their registry default 5; MR swing_mid w/ time_stop = 11,489 (9.8%), 11,436 param-less + 53 capitulation at D270's U[5,15]; trend swing_mid (26,325) untouched per their "do not touch other buckets". **Staged `docs/proposals/v36-exit-duration-priors.md`:** emission-side only, ZERO rule surfaces — the D270 tail-draw pattern widened to a (hypothesis × bucket) range table in `_build_exits`. Two disclosed scoping calls (veto window open in the receipt): capitulation swing_mid inherits U[8,15] (their §2 names the BUCKET; [8,15] ⊂ their own capitulation sweep box; the swing_short leg keeps U[5,15]); both asks read in STRONG form (uniform, zero floor mass). Independent of their in-flight champion-side family-PBO check (their §2). **Outbound held for carry: `PROMPT_CRUCIBLE_EXIT_DURATION_RECEIPT.md`.** v36 build awaits operator go; deploy relay will ask `funnel --compare v35 v36`. No code, no grammar byte, no daemon touch this fold.

## 2026-07-15 — GRAMMAR v34 → v35 (D280): capitulation BARE-DROP live — the rv_rank pin dropped via the FIRST R1 per-directional gate exemption (+ the S3 cardinality surface, discovered at build time); swing_short rider. Operator-approved loosening `4d35a046`

**Built + verified (full record: [[D280]]):** the gate requirement turned out to live at TWO rule surfaces — R1's predicate AND §3.5 S3's generic min-1 regime_filter cardinality (the R1 exemption alone left the validator rejecting every gate-less config; the same tuple-keyed exemption now sits in `evaluate_cardinality`, scoped to exactly the S3 shape; rules: yaml text untouched at both — disclosed as a two-surface rule-surface change under the approved intent). Sampler: momentum draws NO regime gate (no regime rng consumed — per-index seeding made this the cleanest bump of the day: **ZERO golden re-pins**, the v31 golden is byte-identical with the carrier still at position 24); vol_target still never hosts capitulation (now explicit policy); kelly may; veto skip stays; k gains 1 → swing_short joins swing_mid (~1:3). TDD 5 new tests + the d270 reachability test re-pinned; suite **1964 green** post-bump; ruff + mypy --strict clean; both hooks Passed. **Emission proof (3000 cold, live registry): 52 capitulation configs, 100% in-spec, ALL gate-less; buckets 42 mid / 10 short; vol_target 0; zero gate-less leaks on other MR directionals.**

**Deploy evidence: DEPLOYED 2026-07-15T21:27:39Z, commit `b274b0e` (D280).** Ritual: pre-stop preflight **1964 green** (dirty-surface flag = in-flight build) → stop 21:23:25Z (exit 143) → v35 bump + archive + test pin in the down-window → both grammar hooks Passed → uncontended **1964 green** → `ruff format` on changed files BEFORE commit (the v33/v34 hook-abort lesson applied — commit landed FIRST TRY, before start; the ritual inversion is fixed) → commit → start. New daemon **PID 3090010, NRestarts=0**; journal `grammar_version=v35 registry_hash=1f38ab4a70796c8e`, **`recorded manual_bump row for v35`**, clean reconcile (160 newly gated), **0** traceback/mismatch lines; the ve orthogonal floor line present (unchanged, per the adjudication). **First v35 batch VERIFIED (batch `b5022d7e`, 2026-07-15T21:36:13Z, 200/200 submitted, 0 failed; DB-snapshot audited):** 200/200 stamped **v35**; **6 capitulation configs, all in-spec** — every one GATE-LESS with the drop trigger in the audited band, buckets swing_short 4 / swing_mid 2 (the rider live on its first batch), no vol_target; **zero gate-less leaks on non-capitulation MR**. **Relay `PROMPT_CRUCIBLE_V35_DEPLOYED.md`: SENT (operator-confirmed 2026-07-15)** (funnel --compare v34 v35).

## 2026-07-15 — Both Crucible adjudications ANSWERED (D279): capitulation gate-drop GREENLIT → v35 loosening proposal `4d35a046` PENDING (operator verdict needed); index-arm ask WITHDRAWN (our refusal validated); ve ≥ 0.20 floor STANDS w/ decision point = the v32-vs-v33 funnel read; their row-45 preflight SHIPPED (v34 frozen list retirable once its rejects appear)

**Inbound `FORGE_adjudications_capitulation_ve_floor_2026-07-15.md` — full fold: [[D279]].** Headlines: (1) §A.3: index reweight withdrawn ("drafting error against our own evidence"; QQQ/SPY bare-drop = book-level no-op); single-name bare-drop KEPT with value hypothesis CORRECTED to **marginal center lever** (first positive slot delta of the program: cpcv +0.0267/wf +0.0794 at the 0.175 slot; bear-block delta exactly 0.0 — 8/13 trades in 2025) — ship the gate-drop, NO replacement gate (market_rv×drop co-fires 2×/8.4y), swing_short rider optional. §A.2: no gate-off cohort exists ANYWHERE until the drop ships (v31's [50,80] gates bind harmfully). (2) **The drop needs an R1 per-directional carve-out** (MR requires a regime gate — bare-drop is inexpressible) = a rule-surface LOOSENING → **OPEN_PROPOSALS `4d35a046` (PENDING, v35): awaits operator approval** (hard rules #1/#4). (3) Bear-block FYI reinforcing D148/D273: NOTHING in v1 moves the bear block without paying more elsewhere (the GLD stress-call candidate = hedge-shaped, net −0.035 → overlay axis); all bear numbers = n=1 episode (2022). (4) ve floor stands — conversion doesn't measure survivor PBO-orthogonality, and 0.1-0.3% is a v32-era number about a retired emission mix; decision point = the v32→v33 funnel compare (noise-level ve AND comparable resid_vix orthogonal supply → they revisit). (5) Bookkeeping our way: ASML withdrawn (stale v22/v24 cohort), zscore D257 retirement VERIFIED (zero post-v25); **row-45 `UnderlyingChainStarvedError` preflight live at their next inbox-watcher restart → watch telemetry, then retire the v34 frozen list on our schedule.** No code/grammar/daemon this fold.

## 2026-07-15 — GRAMMAR v33 → v34 DEPLOYED (D278): census dead-dimension retirements — BKNG/BRK.B out of single-name sampling + gamma_flip_distance_pct retired as a regime gate GLOBALLY. Operator-approved ("go ahead with v34")

**Built + verified (full record: [[D278]]):** both items emission-side (`rules:` unchanged). TDD 6 new tests; 4 search_space tuple tests flipped to retirement pins; the v33 single-gated-cells-alive assumption superseded on the census evidence (test re-pinned to record it); ALL 7 cold-start goldens re-pinned in one licensed sweep (the underlying filter shifts nearly every draw; relational splits re-verified). Suite: scoped 794 green → pre-stop preflight **1959 green** (dirty-surface flag = the in-flight build, expected) → post-bump uncontended **1959 green**; ruff + mypy --strict clean. **Emission proof (3000 cold, live registry):** BKNG/BRK.B 0/2,356 single-name draws; gamma_flip gates 0 everywhere (ve directionals alive, 83); MR 6-gate mix intact, trend 5-gate.

**Deploy evidence: DEPLOYED 2026-07-15T16:51:17Z, commit `eeab189` (D278).** Ritual: pre-stop preflight **1959 green** (dirty-surface flag = in-flight build, expected) → stop 16:47:15Z (exit 143) → v34 bump + archive + `test_v1_grammar` pin in the down-window → both grammar hooks Passed → uncontended full suite **1959 green** → commit → start. **The v33 hook-abort inversion repeated** (ruff-format reformatted 2 test files → commit aborted → the chained start ran ~1 min before the commit landed; content identical — the reformats were applied to disk BEFORE start, whitespace-only). Lesson queued: run `ruff format` on changed test files BEFORE the commit step next deploy. New daemon **PID 2160529, NRestarts=0**; journal `grammar_version=v34 registry_hash=52aa19805bb2e996`, **`recorded manual_bump row for v34`**, `registry_loaded_from_export`, clean reconcile (199 newly gated), **0** traceback/mismatch lines. **First v34 batch VERIFIED (batch `1a8b95cc`, 2026-07-15T16:56:14Z, 200/200 submitted, 0 failed; DB-snapshot audited):** 200/200 stamped **v34**; **zero violations** — no BKNG/BRK.B among 60 single-name draws (45 distinct names), no gamma_flip regime gate anywhere. `regime_supply:` line live on the same iteration. **Relay held for carry: `PROMPT_CRUCIBLE_V34_DEPLOYED.md`.**

## 2026-07-15 — Crucible census #2 (dead DIMENSIONS) triaged (D277): 2 asks staged as v34 (BKNG/BRK.B exclusion + global gamma_flip regime-gate retirement, operator-gated); 1 already shipped (zscore exit = D257/v25 — their window reads queue backlog); ASML not in our universe; 2 blocked on THEIR adjudications (event-family share → v33 cohort; ve share → contradicts their own ACTIVE orthogonal-family floor)

**Inbound `FORGE_grammar_census_dead_dimensions_2026-07-15.md`** (companion to the addendum; 228k configs decided 07-01→07-15). Triage verified against our side before staging — full disposition: `docs/proposals/v34-census-dead-dimensions.md` + `PROMPT_CRUCIBLE_CENSUS_RESPONSE.md` (SENT, operator-confirmed 2026-07-15, with the V33_DEPLOYED + GENERATION_HEALTH_RECEIPT relays). Key verifications: (1) **their ask 2 is already live** — D257/v25 (07-08) dropped `zscore_reversion_exit` from MR; it survives only on relative_value = their "fence to a pair template" option; their 13,947 declares = pre-v25 backlog (census counts DECIDED, straddling the queue) — verification offered: split by `grammar_version >= v25` → ~0. (2) **ASML is not in our universe** (tier lists checked) — nothing to exclude; rank baskets trade THEIR universe, so their queue-time liquidity preflight is the only complete fix (encouraged; also retires the frozen-list approach for BKNG/BRK.B eventually). (3) **The ve-share ask collides with their own D216 orthogonal-family floor, ACTIVE today (`volatility_event >= 0.20`, journal-confirmed)** — PBO-orthogonal-and-required vs edge-absent cannot both drive generation; adjudication asked (the SECOND open one, with §A.3 capitulation). (4) Event-family shrink deferred on their own stated condition (the nfp/cpi prior fix = v33, deployed today — read that cohort first). **v34 build (BKNG/BRK.B + global gamma_flip gate retirement — the latter supersedes v33's narrower single-gated-cells-alive assumption) awaits operator go.** No code, no grammar byte this fold.

## 2026-07-15 — GRAMMAR v32 → v33 (D276): generation-health change set BUILT — resid_vix confirmed-region concentration + 4 dead-cell retirements + nfp/cpi inert-threshold fix. Operator-approved ("go ahead with v33"); capitulation-gate item HELD on Crucible's adjudication

**Built + verified pre-deploy (full record: [[D276]]):** items 1/2/4/5/6/7 of `docs/proposals/v33-generation-health.md` — all EMISSION-side (sampler pools + threshold table + per-directional scoping; `rules:` text UNCHANGED, submitted lineage stays valid). TDD 14 new tests (`test_v33_generation_health.py`); v27 golden re-pinned (divergence exactly at the resid-capable trend positions — licensed-where-changed); v18/v19 activation tests flipped to retirement pins; option_momentum invariant file flipped to a RETIREMENT invariant. Scoped suite **789 green**, ruff + mypy --strict clean. **Emission proof (3000 cold, live registry):** all four retirements at 0 (option_momentum / MR-gamma_flip directional / pre_earnings_setup gate / trend gamma_flip+dsj pair); dsj veto alive on other gates (138); nfp/cpi max threshold 29.9 (n=302); **resid 39/3000, 100% in-spec** — gates {hurst 22, vix_term_slope 17}, dual-gate dsj 20, all monthly xsect-rank, rank_k {5,10}, long_only 72%. Ride-alongs: Q49 custom_predicates relabel (Q49 CLOSED both sides), GRAMMAR.md emission-status notes.

**Deploy evidence: DEPLOYED 2026-07-15T15:31:22Z, commit `7bb6e49` (D276).** Ritual: pre-stop preflight — suite **1954 green**, NO-GO only on the expected in-flight-build dirty surface (documented cause, v32 pattern) → stop 15:27:48Z (exit 143 normal) → v33 bump in the DOWN-window (grammar.yaml version+header note, archive `v33.yaml`, `test_v1_grammar` pin) → both grammar hooks Passed → uncontended full suite **1954 green** post-bump → commit → start. **Disclosed ritual inversion (benign):** the first `git commit` aborted on hooks (ruff-format reformatted 2 files; the two grammar hooks need venv `python` on PATH under plain git) AFTER `start` had been chained — so the daemon started ~1 min before commit `7bb6e49` landed; tree content was identical (yaml final pre-start; reformats whitespace-only) and the tree was clean within the minute. New daemon **PID 1500711, NRestarts=0**; journal `grammar_version=v33 registry_hash=52aa19805bb2e996`, **`grammar_versions: recorded manual_bump row for v33`**, `registry_loaded_from_export`, clean reconcile (199 newly gated), learned-weight lines loading, **0** traceback/SchemaVersionMismatch/GrammarVersionError. NO hot-read hazard (byte-edit deferred to the down-window). Note: `directional_bucket_weights` still lists the retired `trend×option_momentum` cell — a learned weight over an now-unreachable cell, inert by pool exclusion, decays naturally. **First v33 batch VERIFIED (batch `65adcd9f`, 2026-07-15T15:35:44Z, 200/200 submitted, 0 failed; DB-snapshot audited):** 200/200 stamped **v33**; **zero violations** (no option_momentum directional, no MR gamma_flip directional, no pre_earnings_setup gate, no trend dsj+gamma_flip pair, no nfp/cpi threshold >30); **11 resid configs, all in-spec** (gates hurst 10 / vix_term_slope 1, dual-gate dsj 4, long_only 9/11, all monthly xsect-rank) — 5.5% of the batch vs 1.3% cold (the learned trend weights lift the share, as designed). `regime_supply: complement(bear) ... bear=0/0` live on the same iteration. **Relay held for carry: `PROMPT_CRUCIBLE_V33_DEPLOYED.md`** (funnel --compare v32 v33 ask + what-changed + the still-open §A.3 adjudication).

## 2026-07-15 — Generation-health addendum + 3 LATE-PUBLISHED relays folded (D275): ~1,000 dead configs/wk audit ACCEPTED → v33 change set STAGED (operator-gated); resid_vix region CONFIRMED in-book (3 pipeline-native WF-gate passes — the sweep is the headline v33 item); capitulation "reweight to index" ask CONTRADICTS their own 07-13 honest pricing — flagged back, not acted on

**Inbound (operator-carried): `FORGE_generation_health_capitulation_addendum_2026-07-15.md`** + its §C revelation that a 07-12/07-14 relay batch was never received. Verified our side: `FORGE_capitulation_v31_followup_2026-07-13.md` **WAS received + folded** (`ab6a609` — their ledger is wrong on that one); `FORGE_resid_vix_region_followup_2026-07-13.md`, `FORGE_days_to_nfp_cpi_threshold_prior_2026-07-14.md`, `FORGE_earnings_manifest_published_2026-07-13.md` genuinely first-seen 2026-07-15 (zero adoption explained — never carried). All four now read + triaged.

- **Outbound (held for operator carry): `PROMPT_CRUCIBLE_GENERATION_HEALTH_RECEIPT.md`** — receipt table (incl. the ledger correction), the in-place-adoption answer (NO — deterministic enumeration means every ask needs the version-bumped path; stop expecting in-place), and the §A contradiction flag: the addendum asks to reweight capitulation toward the index/broad-ETF arm citing the underlying-bounce probe, but their OWN 07-13 honest-pricing probe found the option-arm index cell NEGATIVE (−0.046/−0.067) with single-name high_vol the lone survivor (+0.036 m2m). Adjudication requested before any emission-share move; their gate-off-split ask (§A.2) is also superseded by their own followup ("the bare-drop arm IS the gate-off cohort … NO injection cohort needed").
- **Staged: `docs/proposals/v33-generation-health.md`** (SCOPING; one v32→v33 bump, items independently strikeable): (1) **resid_vix concentrated sweep — HIGH, the headline**: three generated residual_momentum configs PASS the WF gate in-book (blend WF 2.119/2.103/2.031 vs probe 2.0611; best cpcv carrier 1.4099 = closest-ever to the 1.5 gate; no config carries both axes yet); window 70-160 / skip 7-21 / thr 0.65-0.85 / gates {vix_term_slope 0.1-0.7, hurst p40-50} / monthly xsect-rank / rank_k 5-10 / long_only preferred; density tens-per-neighborhood; solo-reject = EXPECTED for this family (all 3 passes are solo rejects — never feed solo verdicts back as kill signals). (2) days_to_nfp/cpi `regime_range` (7,60)→(7,30) — ceilings 35/34, ~42% of these gates provably inert. (3) capitulation rv_rank gate DROP (their sweep: unhelpful-to-harmful 50-70 clean; our [50,80] band was pre-Q49 miscalibrated; 69/69 decided dead, median 4 OOS trades) — BLOCKED on the §A.3 adjudication. (4) retire pre_earnings_setup×vol_event pairing (~450/wk, 91-100% dead; vol_event conversion 0.1%). (5) drop trend dsj+gamma_flip double-gate (~300/wk dead; single-gated trend converts 9.7%). (6) retire `option_momentum` directional (100% dead, month post-fix). (7) remove gamma_flip-as-MR-directional (~100/wk dead). Net: reallocates ~1,000 dead configs/wk into the confirmed resid_vix region; §7.3 pacing untouched.
- **Also from the late batch:** earnings-manifest publish announcement (activation = today's restart — folded into the D273 deploy block below; cadence caveat: first publish MANUAL, their timer not yet in tree, safe under max_age_days=None); **Q49 closed Crucible-side** (both kernel docstrings relabeled range-position, docstring-only, no cache re-key; our v32 ride-along covered the Forge side) — Q49 updated; their all-NaN-directional gate guard still on their list (§25-class, they relay when it lands).
- **No code, no grammar byte, no daemon touch this fold** — docs/relay/proposal staging only. v33 build awaits operator go (and the §A.3 answer for item 3).

## 2026-07-15 — Worst-quartile regime label CORRECTED → BEAR-ONLY (D273): Crucible re-derived the 06-13 T3a label per-block (the old one rode a hull-CPCV artifact) — bear 2.08× SOLE crater, ranging 0.90 ≈ base rate; `regime_supply` headline narrowed to `complement(bear)` (inert-until-restart); the un-shipped T2 ranging floor CLOSED

**Inbound `../Crucible/docs/handoffs/FORGE_worst_quartile_regime_label_correction_2026-07-15.md`:** the 06-13 label (bear 2.39× / ranging 1.33×) was computed from per-path Sharpes over convex-hull spans (36/45 paths ≈ full-period through intervening TRAIN groups) — the pattern their 06-28 audit rated an integrity bug and fixed in the production portfolio campaign (`093b893`, live 07-02); the label's source artifact predated the fix and was never recomputed. Per-block re-derivation (same pinned era-C 342-component book, run_id/seed/weights/overlay identical): **bear 2.08× — the ONLY regime > 1; ranging 0.90 — at/below base rate**. Hull bias materially reordered path ranks (Spearman 0.637; ~half the worst-quartile membership changed). Their ask: narrow the complement target to bear-only; do NOT cut the overall complement reservation or supply.

- **Shipped (versionless; TDD red→green, test_ranking 265 green, ruff + mypy --strict clean):** `ranking/regime_supply.py` — `complement_selected`/`complement_pool` bear-only; journal headline `complement(ranging+bear)` → **`complement(bear)`** (bear IS the headline now, the separate callout dropped; ranging stays a visible CELL, class names frozen at D144 spellings for journal grep-continuity); `main.py` call-site comment synced. Submitted set byte-identical (shadow metric, threaded nowhere near submit). **Daemon-inert until the next restart** (the running v32 daemon imported the module at startup — the D144 activation posture; rides the next deploy, not worth a bounce).
- **The T2 enforcement floor (never shipped) is CLOSED both ways:** its 06-16 [[D148]] re-scope was RANGING-only (bear = Crucible's `tail_leg` overlay, Forge-unsuppliable); the correction refutes ranging as a crater → nothing valid left for it to reserve. `t2-ranging-floor-and-supply.md` → CLOSED; correction blocks added to `tail-aware-ranker.md`, `worst-quartile-complement-supply.md` (Forge-suppliable complement now NIL — fully historical), `regime-orthogonal-arms.md` (the ranging/Path-C arm must stand on its own edge, no longer a crater fill; the bear half unchanged), `orthogonal-family-supply-for-pbo.md` (the ~85%-mr stream over-concentrates a no-crater regime bet — reinforces its un-crowding case).
- **NOT changed (their explicit no-whiplash list + hard rules):** the shipped mr supply-growth levers (D150/D151/D167 + successors — also stream-quality/R1-thesis-justified; no supply cut asked); grammar.yaml historical rationale comments (ANY byte change = a version bump, not warranted for prose); D103 `min_per_hypothesis`; **capitulation-bounce v31 REINFORCED** (bear craters remain the confirmed, now sole, over-represented regime); the 07-03 bear-crater directionality read (never depended on hull math).
- **Calibration sweep (their caveat):** corrected era-C basket CPCV p25 = **0.88, not 1.14** — the pool-level wall is BIGGER than the stale figure suggested. Repo + memory scan: NO Forge calibration anchored on 1.14 (every `1.14` hit is a contracts-version literal). The 1.343 standard-basis honest wall is a different basis (refit re-gates) — unaffected. Ranker training targets unaffected (per-config gate numbers come from the production gate path; Crucible: no promoted book affected).
- **Watch (Crucible-side, no Forge action):** their L2 selective-de-gross re-check (triggers had inherited "bear,ranging") is re-running on the fixed engine — they relay separately if its verdict moves; their §20 correction entry awaits their operator sign-off.

**DEPLOYED + VERIFIED 2026-07-15T07:55:03Z (operator "deploy"; commits `59e4e17` D273 + `188ad7a`/follow-up D274 + Q50/Q51).** Ritual: preflight → **NO-GO twice, both environment-caused, both fixed pre-stop**: (1) 9 cold-start goldens red — **Crucible started the earnings-coverage publisher 2026-07-13T23:32:11Z** (~1h after the v32 deploy) and the v32 loader read the LIVE manifest inside the test run (import-time `_UNIVERSE_EXPORT_DIR` bypasses `_isolated_home`) → D274 autouse dormant-`()` pin + loader/determinism tests re-bind the real loader; `_load_underlyings`' matching live-coupling logged Q50. (2) One flaky Platt-ECE test (DuckDB scan-order even/odd split) → logged Q51, durable fix queued. Then preflight **GO (1943 green)** → stop 07:54:56Z (exit 143 normal) → reset-failed → start 07:55:03Z. New daemon **PID 239496, NRestarts=0**; journal `grammar_version=v32 registry_hash=84362cdc911b4563`, `registry_loaded_from_export`, `reconciled batches=1 newly_gated_total=103`, **0** traceback/SchemaVersionMismatch/GrammarVersionError; §7.3-blocked at start (oldest in-flight 51.5% < 80%, normal). `forge healthcheck` **OVERALL=OK (14 ok, 0 warn)** — first all-green in weeks: **`earnings_coverage: present (1.4d old)`** (was "no export yet"; this restart is the D272 ACTIVATION boundary — manifest sanity-checked pre-restart: 140 covered, universe∩covered = 87/124, excluded = ETFs + ABNB/ARM/BRK.B/V/FCX/WBD/WDC — non-empty, non-disjoint), tmp_headroom + wf_p25-drift WARNs both cleared. **FEATURE-VERIFIED on the first unblocked iteration (batch `61fa14e6`, 2026-07-15T08:27:22Z, 200/200 submitted, 0 failed):** (1) **D273 live** — journal `regime_supply: complement(bear) selected 0/200 (0.0%) pool 0/964 (0.0%); cells [trending=115/499 ranging=69/392 bear=0/0 other=16/73]` (bear 0/0 as structurally expected; note the stream is no longer 85%-mr — trend 115/200 this batch). (2) **D272 coverage ACTIVATION verified in the recorded identity** — `batch_summaries.enumeration_inputs_hash` = `4f53cda18c2baa0c|188eb73111627fa9|026dc6351bc9c05b` (THREE-part; every pre-restart batch incl. `98e9dc1c` @05:58Z is two-part — the third part IS the coverage fingerprint, hard rule #6 satisfied). event_momentum submitted 0 this batch (all 633 died at prefilter — batch variation; the ⊆-covered spot-check lands with the first batch that carries the family; no leak is possible with zero emitted). Zero traceback/mismatch lines post-restart (journal grep count 0).

---

## 2026-07-13 — GRAMMAR v31 → v32 DEPLOYED+VERIFIED 2026-07-13T22:30:46Z (D272): earnings-coverage MANIFEST wiring — the durable D268 fix, LIVE but DORMANT-until-publish. Proposal `682e1abd` APPROVED (operator, in-session "let's enable v32 grammar").

**Deploy evidence:** commit `901479d`; ritual stop 22:25:40Z (exit 143 normal) → v32 bump in the DOWN-window → uncontended full suite **1943 green** (v31 pre-stop AND v32 post-bump) + ruff + mypy --strict + both grammar hooks clean → commit → reset-failed → daemon-reload → start. New daemon **PID 2285486/2285491, NRestarts=0**; journal `grammar_version=v32 registry_hash=a85eb966fc81d47a`, `grammar_versions: recorded manual_bump row for v32`, `registry_loaded_from_export`, `reconciled batches=1`, **0** traceback/SchemaVersionMismatch/GrammarVersionError. **NO hot-read hazard** (lesson applied): the old v31 daemon stamped v31 to its last iteration (15:25:12 PDT), only the new daemon stamps v32 — the grammar.yaml byte-edit was deferred to the DOWN-window. `forge healthcheck` OVERALL=WARN (**12 ok, 2 warn**): the new **`earnings_coverage: no export yet (dormant until Crucible starts the publisher)` = OK** (the OK-when-absent design), `contracts pin==installed 1.31.0` OK; both WARNs pre-existing/unrelated (`tmp_headroom` 4.4x<5.0x D259 disk-advisory — clear stale /tmp snapshots; `wf_p25 drift` the known-weak lane).

**What.** `_pick_underlying`'s earnings-gated pool becomes `(universe & covered) − _NO_EARNINGS_UNDERLYINGS`, where `covered` = Crucible's authored `earnings_covered_symbols.json` via the contracts 1.31.0 loader (`load_earnings_covered_symbols_from_export`, D271), read `max_age_days=None` + process-cached. Retires the v30 `_NO_EARNINGS_UNDERLYINGS` stopgap AS AUTHORITY (kept as free defense-in-depth) and closes its blind spot self-maintainingly — a future no-earnings universe add can no longer re-open the SOXL degenerate-leg pathology until a human edits the list. Corrupt/disjoint manifest → loud warn + v31-pool fallback (never halts generation); staleness → new `check_earnings_coverage_export` healthcheck line (OK-when-absent, >45d WARN). H-3: covered set folds into `enumeration_inputs_hash` only when non-empty. Rides the Q49 rv_rank/iv_rank range-position relabel (docs/comments only). `rules:` text UNCHANGED.
- **DORMANT-until-publish (verified 2026-07-13):** Crucible has NOT yet published `earnings_covered_symbols*.json` → loader cold-returns `()` → **byte-identical to v31**. Emission proof (3000 cold, live registry): covered set `()`; `enumeration_inputs_hash` 2-part (v31 shape); 640 event_momentum over 94 real companies; **0 no-earnings leaks**. Activation = Crucible's first publish + our next restart. Deploy relay nudges them to START the publisher.
- **Build evidence:** TDD red→green; new `test_earnings_coverage_manifest.py` (dormancy==v31, blind-spot closure, RTX-drawable, corrupt/disjoint fallbacks, frozen-list composition, process-cache) + fingerprint folds + healthcheck test; ALL pre-v32 cold-start goldens UNCHANGED (dormant path byte-identical); full uncontended suite **1943 green** (pre-stop v31 AND post-bump v32); ruff + mypy --strict + both grammar hooks clean. NO contracts change (1.31.0 suffices).
- **NEXT:** operator carries `PROMPT_CRUCIBLE_EARNINGS_MANIFEST_PUBLISH.md` — nudge Crucible to START the `earnings_covered_symbols` publisher (loader shipped 1.31.0/D271, wiring now LIVE+dormant; activates on their first publish + our next restart) + the Q49 kernel-docstring flag (their rv_rank/iv_rank docstrings say "percentile" but compute range-position). WATCH: first unblocked v32 batch (§7.3-blocked now on the pre-restart batch) will be v32-stamped, event_momentum dormant on real companies. [[promotion-gate-tiers-and-constraint]] [[earnings-coverage-manifest-v31-pending]]

## 2026-07-13 (latest, course-correction) — Crucible UPDATED `FORGE_capitulation_v31_followup_2026-07-13`: both stress-tests DONE — the capitulation edge is materially THINNER (clean index arm NEGATIVE under honest pricing; survivor = single-name high_vol convexity). Assessment: NO grammar change now — v31's shape already matches the surviving cell; their in-book split read decides. Q49 logged (rv_rank is a RANGE-POSITION, not a percentile — verified in-kernel).

**Their two probes (hard-rule-6 walkback of their own MEDIUM framing):** (1) **IV-crush + entry-premium reval** — realized crush mild (58%/42% mixed) and survivable alone, but the r=q=0 BS entry premium was ~4.4% cheap vs reconstructed marks; charging BOTH honestly flips the survivorship-clean index arm NEGATIVE (index:ALL −0.046, high_vol −0.067); the lone durable positive = **single-name high_vol +0.036 m2m** (convexity on dispersion — the caveat our review flagged). (2) **RV-gate sweep on clean data** — the gate DESTROYS the index edge at every level 50–70 but HELPS single names (+0.116 → +0.208 @70); bare-drop = the de-facto gate-off cohort (their original gate was inert) → **the gate-on-vs-off question is ANSWERED in-grammar; the injection escalation trigger is CLOSED — no gate-off cohort ever needed.**
- **Assessment (no build):** v31's emission is ~98% single-name and 100% gated in [50,80] — i.e., the family as built CONCENTRATES on the one surviving cell, and their sweep says the gate HELPS that cell (best point 70 ∈ our band). Their "reconsider the pinned gate" is flagged-not-prescribed and cuts against the index arm, which is dead bare OR gated — so a gate-lighter arm would spend emission measuring a refuted cell. Priority-downgrade needs no manual throttle (capitulation ≈ 2.6% of stream; the learned (hyp, directional, bucket) weights absorb funnel evidence automatically). **Candidate LATER tightening if their in-book split confirms: exclude index/ETF underlyings for the capitulation directional** (concentrate fully on the survivor) — wait for the fold-column read.
- **Q49 (MEDIUM, logged + piggyback queued):** their §3 finding verified in `crucible_engine_core` — `rv_rank` (and `iv_rank`, same kernel semantics) compute min-max RANGE-POSITION `(cur−lo)/(hi−lo)×100`, not a percentile, despite docstrings. Calibrated gates unaffected (kernel-unit tuning through the funnel); the hazard is INTENT-mapping (v31's [50,80] came from a "≥60th pct" framing — moot for v31 since their sweep also ran kernel-units, but the next cross-system threshold translation must use kernel semantics). Relabel queued on the next bump (`grammar-change.md` pending list; candidate ride = v32 `682e1abd`); kernel-docstring fix is Crucible's, flagged for the next relay.
- **NEXT:** await their tempered in-book fold-column read (index-vs-single × by-gate splits confirm/refute on campaign machinery — "can still surprise"); operator rules on `682e1abd` (v32 manifest wiring, unrelated to this thread); next relay carries the Q49 kernel-docstring flag.

## 2026-07-13 — Crucible `FORGE_capitulation_v31_followup_2026-07-13` received (SUPERSEDED by the update above — the injection-lane deferral + funnel-timing content below stands except the gate-off escalation trigger, now CLOSED): v31 acknowledged; injection lane DEFERRED (all three arms); funnel compare PENDING accumulation (~a day). NO Forge action owed — bookkeeping only, daemon untouched (v31).

- **Injection lane resolved without spend:** Crucible declines all three off-grammar arms for now — prove the durable in-grammar family first. (1) **Gate-OFF covered in-grammar**: their probe's gate was INERT so the measured edge was the bare drop; our [50,80] intended-strength build + the funnel ask-#2 threshold split (50–65 vs 65–80) + their in-flight re-run measure the gate's marginal value monotonically without bypassing the validator — a true gate-off cohort ONLY if that split reads non-monotonic/inconclusive (the recorded escalation trigger). (2) **swing_long deferred** (DTE = 2nd-order in their `directional_strike_dte_selection` evidence; probe horizon maps to swing_mid anyway). (3) **ATM delta deferred** (concurs with D125 + their OTM-optimal finding). If escalated later: ONE cohort covering the corners together.
- **Funnel read pending accumulation:** lineage at 8 decided configs at their writing (400 v31 runs total, 7.1% of MR — "the family is landing"); they run all four asked reads once the lineage reaches a few hundred decided (~a day at current emission), in-book/fold-column not solo. They carry both disclosed caveats (n_bars first-ever parameterization — no naive cross-version hold-time compare; rank-pin = single-name/confluence lineage, correct for the thesis).
- **Process correction accepted on their side:** future dark-supply asks will verify C2/family admissibility, not just registry presence (our carve-out finding).
- **Earnings-manifest wiring proposal DRAFTED (operator ask): `682e1abd` (v32) PENDING.** The D268 durable fix: `_pick_underlying` earnings-gated pool = `(universe ∩ covered) − frozen list` via the 1.31.0 loader; **dormant-until-publish** (verified 2026-07-13T17:29Z: NO `earnings_covered_symbols*.json` in exports yet — loader cold-returns `()` → byte-identical to v31). Design mirrors `_load_underlyings`/D078 (blessed loader, loud corrupt-warn + fallback, process-lifetime cache → activation at a restart boundary) + the H-3 fingerprint discipline (covered set folded into batch fingerprints); staleness moves to a new OK-when-absent healthcheck line (`max_age_days=None` at the read site — a dead publisher surfaces in ops, doesn't halt generation). Frozen list retired from MAINTENANCE, retained as defense-in-depth; tightening direction, operator-gated as a grammar bump (v32). Deploy relay will nudge Crucible to START the publisher.
- **NEXT:** operator approves/rejects `682e1abd` (build waits — can ship dormant now or after their publisher starts, either is safe); await their funnel results + two probe follow-ups (IV-crush revaluation, RV-gate re-run → sweep-bound tuning). [[promotion-gate-tiers-and-constraint]]

## 2026-07-13 — GRAMMAR v30 → v31 DEPLOYED+VERIFIED 2026-07-13T15:48:05Z (D270): capitulation-bounce LIVE — `momentum` drop-trigger MR directional via the FIRST §3.5 C2 per-id carve-out. Proposal `e9d74318` APPROVED (operator, in-session "approve"). Contracts 1.31.0 adopted pin-only alongside (D271 — the earnings-manifest LOADER landed; wiring = follow-on proposal).

**Deploy evidence:** commits `7687e21` (D271 pin) + `c50be71` (v31 feat); ritual stop 15:36:04Z (exit 143) → uncontended suite **1930 green** → commit → restart. New daemon **PID 1413824/1413829, NRestarts=0**; journal `grammar_version=v31 registry_hash=a2739d10a0596991`, `registry_loaded_from_export`, clean reconcile, 0 errors; healthcheck **12 ok / 1 warn** (pre-existing wf_p25-drift lane warn). **Disclosed incident, benign:** the deploy.md hot-read hazard fired — grammar.yaml edited in the live tree pre-stop; the v30-code daemon stamped iterations v31 from 15:31:50Z (manual_bump row changed_at = stamp-flip time, documented artifact). **Zero pollution: every iteration in the 15:31:50→15:36:04Z window was §7.3-blocked (73.5–75.5% gated < 80%) — no pre-restart v31 submission exists.** Lesson: defer the grammar.yaml byte-edit to the stop window even on live-tree builds (v26 pattern).

**What.** The approved family end-to-end: `momentum` (lookback randint[3,10], skip 0 riding SignalSpec params — D264 pattern) absolute drop trigger op `<` uniform(−0.083, −0.041) (log ≈ −8%..−4%; probe point −0.051) × PINNED `rv_rank` op `>` uniform[50, 80] elevated-vol gate (the intended-strength condition their probe's coding bug left inert; R1 op-agnostic per D107 — first family ever on the elevated side) × swing_mid always (horizon 15 → D102 k∈{2,3,4} → 30/45/60 all snap mid) × CALL-only free (engine `direction` defaults `long_call`) × time_stop `n_bars` randint[5,15] scoped to this directional (engine default 5 everywhere else — D169 concern respected) × NO veto (calm-side would strangle co-fire) × NO vol_target (C1 chain guard composes with the gate pin) × confluence-only (rank sorts DESCENDING = inverse mechanism → policy-excluded) × pin-excluded from trend's pool (label honesty + trend byte-identity).
- **Build evidence:** TDD red→green; suite **1930 green**; ruff + mypy --strict clean. ALL pre-v31 goldens byte-identical (fixtures never serve momentum; plumbing defaults pin the refactor — dedicated regression test); new `_REGIME_GOLDEN_V31_ACTIVE` (diverges from V29 at position 3, first carrier at 24). **Emission proof (live registry, 3000 cold): capitulation = 42/591 MR (7.1%)**; gate 42/42 `rv_rank >` [50.4, 79.3]; thresholds spanning the full sweep; lookbacks 3–10 all sampled; 42/42 swing_mid; vol_target 0, vetoes 0, trend/rank leakage 0; n_bars spanning [5,15]; underlyings single names + SPY. **`check-activations momentum` [ OK ] 151 max (SPY 17/AAPL 51/MSFT 29/NVDA 151)** — D254 gate cleared, per-name spread confirms the post-fix cache.
- **D271 ride-along (pin-only):** contracts **1.31.0** (`cbb8671`) = `load_earnings_covered_symbols_from_export` — the D268 coverage-manifest loader, PURELY ADDITIVE (verified diff), Forge doesn't call it yet; adopted because the sibling bump flipped the pin test red mid-build (D262/D267 discipline). **Wiring the manifest (retire `_NO_EARNINGS_UNDERLYINGS`) = its own operator-gated bump, proposal to follow.** Note: D268's "v31 = the manifest" is renumbered — capitulation took v31; the manifest is next.
- **Injection-lane arms (offered in the relay, operator decision pending):** gate-OFF (fails R1), swing_long (D102), delta 0.45–0.55 (P3). Relays ready: `PROMPT_CRUCIBLE_CAPITULATION_BOUNCE_RESPONSE.md` (updated to reflect the build) + `PROMPT_CRUCIBLE_GRAMMAR_V31_FUNNEL.md` (`--compare v30 v31`).
- **FIRST UNBLOCKED BATCH (verified live): `15d70291` submitted=200 failed=0 at 16:04:29Z (~16 min post-restart).** All 200 v31-stamped; 74 MR / 104 trend / 22 ve; **5 capitulation configs (6.8% of MR — matches the 7.1% cold-mix proof)**, every one on spec: e.g. MS swing_mid trig `<-0.0604` lookback 10/skip 0 × `rv_rank > 72.68` (rv_window 10/window 126); BSX `<-0.0554` lb 6 × `> 73.57` + time_stop n_bars=7; NEE `<-0.0507` lb 6 × `> 68.10` + n_bars=9 — real single names, confluence, fixed_risk, no veto (the MS config drew target_exit — the S5 coin flip, expected).
- **NEXT:** operator carries `PROMPT_CRUCIBLE_GRAMMAR_V31_FUNNEL.md` + `PROMPT_CRUCIBLE_CAPITULATION_BOUNCE_RESPONSE.md` (incl. the injection-lane decision for gate-off/swing_long/ATM arms); follow-on proposal for the D271 earnings-manifest wiring. [[promotion-gate-tiers-and-constraint]]

## 2026-07-13 — Crucible handoff `FORGE_capitulation_bounce_generation_request_2026-07-12` (MEDIUM) reviewed + verified → v31 proposal `e9d74318` PENDING operator. Nothing built, daemon untouched (v30).

**The ask: a capitulation-bounce long-call family — `momentum` (parameterized, lookback 5/skip 0) `< -0.051` drop trigger × `rv_rank > 60` elevated-vol gate, CALL only, swing_mid, ~10 td time stop. Probe (trade-level, 2% per-leg cost): pooled +0.107 / high_vol +0.127 (n=898); attacks the bear/high-vol cpcv-p25 crater as a worst-quartile COMPLEMENT (judge in-book, their framing — solo §8.7 will likely reject). Deliberately the corner the champion MR vetoes (rv_rank<62), so structural corr should be low. It is the CONVERSE of the bear-timing refutations (every bear signal was contrarian; this buys the bounce instead of fighting it).**
- **Verified:** registry triple (`momentum` v1 family=trend rank-coherent / `rv_rank` v1 / `days_since_jump` v3) in code AND snapshot 2026-07-13T010003Z (72 inds). `momentum` dark structurally: no threshold entry + no horizon entry → `is_threshold_skippable` every role → never emittable (corroborates their 0/462,990). Engine `Momentum` reads `params[lookback]/[skip]` (min_bars=max+1) — no engine change. **CALL-only is free** (`ThresholdSignal` `direction` defaults `long_call`; Forge never emits it). **`time_stop` reads `n_bars` default 5 and Forge never samples it** → the probe's 10-td hold needs a scoped n_bars [5,15] emission (D169 cross-hypothesis concern respected: this directional only).
- **KEY FINDING (their "sampler wiring only" understated):** `momentum` family=trend and §3.5 C2 admits only (mean_reversion, dealer_positioning) MR directionals → needs a **C2 per-id carve-out = operator-owned §3.5 surface + loosening** (hard rules #1/#4). MR is the ONLY viable host: time-stop-primary chassis (trend REQUIRES trailing exits) + contrarian semantics (trend hosting would pollute trend's learned cells + their fold-column lineage). R1 is op-agnostic by documented convention (D107) → `rv_rank >` satisfies it as written; flagged for operator confirm (every prior MR gate used the calm side — this family inverts the protection thesis deliberately).
- **Not grammar-expressible → injection lane (separate decision, 07-07 pattern):** gate-OFF arms (fail R1), swing_long (D102 one-id-one-bucket; horizon 15 → swing_mid always = probe bucket), delta 0.45–0.55 (MR swing_mid P3 caps 0.45; no P3 widening proposed — D125 has MR concentrating low; 0.40–0.45 edge covers the asked low end).
- **Guards in the proposal:** momentum pinned OUT of trend_continuation's pool (threshold entry would auto-admit it there with the wrong chassis) and OUT of the rank path (`cross_sectional_rank` sorts DESCENDING → top-N = STRONGEST momentum = the inverse mechanism); vol_target chain excluded (C1 collision with the pinned volatility-family gate); calm-vol veto slot skipped. `days_since_jump` fallback trigger is NOT free either (family volatility, also outside MR C2; regime-only today) — deferred per their framing.
- **Staged:** proposal `e9d74318-c7f2-4d49-a7bd-defdd0b65be9` (OPEN_PROPOSALS, PENDING) + relay `PROMPT_CRUCIBLE_CAPITULATION_BOUNCE_RESPONSE.md` (held, operator carries — C2 finding, injection-lane split for the off-grammar arms, rank-inversion heads-up, n_bars-default-5 disclosure). Their IV-crush revaluation + RV-gate re-run (in flight) slot into build-time sweep-bound tuning; neither blocks.
- **NEXT (operator-gated):** approve/reject `e9d74318`; on approval build per `docs/tasks/grammar-change.md` (TDD, emission proof incl. gate co-fire rates, `check-activations momentum` [ OK ] pre-deploy — D254, deploy ritual, funnel relay `--compare v30 v31`); decide the injection cohort for {gate-off, swing_long, ATM} arms. [[promotion-gate-tiers-and-constraint]]

## 2026-07-12 — SECTOR-SIGNAL RESEARCH VERDICT: DON'T-BUILD (D269). Staged, not a decision — NO build, NO bar move, daemon untouched (still v30, PID 3461559). Grouping ≠ mechanism; the axis is barren for a long-vega book.

**Trigger:** operator ("after seeing SOXL, is a sector-ETF play worth testing? use agents to research the indicators/signals"). Distinct from D268 (SOXL as a degenerate *underlying*); this asks whether a sector-aware *signal* adds orthogonal alpha.
- **Grounding (3 read-only sweeps):** (1) sector ETFs ALREADY enumerable — SOXL + 12/14 named sector ETFs are live Crucible-universe underlyings (Crucible-owned); no work needed to trade them. (2) options-only is NOT a blocker — §13.6/hard-rule-#7 ban only literal `family=="equity"`; basket signals already exist (`cs_dispersion`, `market_realized_vol`, `residual_momentum`). (3) the sector-*signal* axis is the MOST-tested idea here — GICS relval refuted ([[D215]]), dispersion on the do-NOT-add list.
- **Method:** decorrelation-first deep-research workflow (6 angles, 24 primary sources, 25 claims verified 3-vote). **Verdict NEGATIVE** — every candidate hits a disqualifying bucket: **(1) already owned** (sector-ETF vol is VIX-dominated → not orthogonal to `vix_*`/`market_realized_vol`); **(2) off-mandate short-vol** (sector IV overreaction/reversal is real but the harvestable side is *selling* vol; long leg 0.11%/day @10% sig, no-cost midpoints, 1-day rebalance → dead net of spreads); **(3) already refuted** (Goyal-Saretto IV−RV = the `iv_minus_rv`/`rv_rank` family, D214, direction was backwards); **(4) momentum-adjacent + wrong universe** (lead-lag is directional, intra-industry, concentrated in illiquid neglected names). Barren, not under-sampled (GRAMMAR_REVIEW §5 "stop").
- **Honest incompleteness:** session limit cut off credit-implied-vol (CIV) + VRP-term-structure verification (8 claims unverified, synthesis skipped); extracted evidence already negative (CIV level 81% VIX-corr; both premia accrue to the seller). Resume-to-close available (`resumeFromRunId`, cached replay free) — prior confirms negative.
- **Staged artifacts (held, operator rules on relay):** `SECTOR_VOL_MECHANISM_RESEARCH.md` (the DON'T-BUILD verdict) + `PROMPT_CRUCIBLE_SECTOR_ETF_XSECT_PRECHECK.md` (ONE optional cheap ask — sector-ETF cross-sectional *trend* residual-IC, low-prior/trend-collinear but closes the last never-isolated door on data). Un-refuted door still = a different *mechanism* (held fundamental-value pre-check), not a sector grouping. [[exhaust-long-options-before-v2-spreads]] [[promotion-gate-tiers-and-constraint]]
- **NEXT (this session):** replying to Crucible handoff `FORGE_put_call_flow_call_wall_stale_metrics_2026-07-12` (stale volume-family gated cpcv). NOT the sector work.

## 2026-07-12 — GRAMMAR v29 → v30 DEPLOYED+VERIFIED 2026-07-12T15:52:12Z (D268): exclude no-earnings underlyings from earnings-dependent generation — the event_momentum degeneracy fix LIVE. Emission 22.5% → 0; healthcheck 13/13 OK. Commit `4a950ab`, PID 3461559, NRestarts=0, journal `grammar_version=v30`, clean.

**Crucible relay `FORGE_event_momentum_no_earnings_underlying_degenerates_2026-07-12`:** event_momentum on a no-earnings underlying (SOXL — a live 17.5% leg of the FIRST promoted book) degenerates — `sue`/`days_since_earnings` NaN-fill → the engine's no-data fallback `allow=True` never gates + `sue`→FLAT, so a `realized_vol` confluence passthrough backfills a naked long-call that TRADES (expected_trades can't catch it) with zero PEAD contribution. Root cause Forge-side: the T1.4 earnings-ETF guard was frozen at the 4 tier-1 ETFs while the universe grew ~26 more no-earnings names; **22.5% of event_momentum emission was landing on them** (measured live, SOXL confirmed).
- **Operator decision (AskUserQuestion): stopgap now + accept the manifest.** Forge can't determine coverage itself (universe export is ticker-lists only; RTX-type single-names are ETF-lookalikes with real EPS). v30 = conservative Forge-only exclusion; v31 = the durable coverage manifest.
- **v30 change:** `_pick_underlying` excludes `_NO_EARNINGS_UNDERLYINGS` (superset: ETF/leveraged/inverse/commodity/vol/bond/index only — covered single names never excluded) for any earnings-gated config. No `rules:` text change (sampler policy, D268). Six cold-start goldens re-pinned (earnings-gated pool shrank → rng-stream reshuffle, licensed by the bump; flag-inertness + prefix relations re-verified). Hermetic test guards that RTX/AAPL stay drawable.
- **Verified:** TDD red→green (3 red first); ruff + mypy --strict clean; suite **1914 green**; emission proof — event_momentum degenerate rate **22.5% → 0**, all now real companies.
- **NEXT:** deploy ritual (stop → commit → restart → verify), then carry `PROMPT_CRUCIBLE_EARNINGS_COVERAGE_MANIFEST.md` (accept the manifest offer; D261 confirm-then-wire; v31 intersects + retires the hardcoded list). Their Crucible-side all-NaN-directional admissibility guard = a good complementary backstop. [[promotion-gate-tiers-and-constraint]]

## 2026-07-12 — CONTRACTS 1.29.0 → 1.30.0 ADOPTED+DEPLOYED 2026-07-12T07:14:57Z (D267): pin-only, no-op for Forge. Grammar UNCHANGED (v29); enumeration byte-identical. `contracts pin==installed 1.30.0`, healthcheck 13/13 OK.

**1.30.0 (Crucible `253da67`) adds ONE Literal — `PromotedPortfolio.weighting_scheme += "explicit"`** (operator-fixed maximin weights; the crossing-unblock for the first full-gate portfolio promote, pure_sue175, which was fail-looping reconstruction on `literal_error` → promoted_portfolios n=0). **NO-OP for Forge:** the literal is on a QuantIQ-facing model Forge never constructs or parses (its Crucible reads are `get_recent_gated_runs`/`get_promoted_strategies` → GatedRun only; verified zero parse sites). No read-face or inbox-face exposure either direction → the running 1.29.0 daemon was never at risk, and no D261-Ask-1 handshake owed. §13.5 major-check already passed (both major 1). Adopted because the editable-sibling bump left `uv.lock` dirty at 1.30.0 and flipped the exact-match pin test RED (would NO-GO deploy-preflight) — the standing D262 discipline. Deploy: stop → commit (`44b0441`, pin + uv.lock) → restart (PID 2604715, NRestarts=0); journal `grammar_version=v29 registry_hash=56bd15d4e858fa19` (v29 unchanged), clean; `contracts pin==installed 1.30.0`. Suite 1912 green. No relay owed. [[promotion-gate-tiers-and-constraint]]

## 2026-07-12 — GRAMMAR v28 → v29 DEPLOYED+VERIFIED 2026-07-12T06:12:13Z (D266): market_realized_vol LIVE — Crucible's PREFERRED (market-level) absolute-RV gate, wired same-day against their D258-pattern confirm (`CRUCIBLE_market_realized_vol_registered_2026-07-12`). Proposal `9f1c615b` APPROVED (the recorded 2121cafe fast-follow; operator attached the wire-against confirm).

**Deploy evidence:** commit `db4bc59`; PID 2370570/2370575, NRestarts=0; journal `grammar_version=v29 registry_hash=098e99ed2f2138a6` (hash rolled = their market_realized_vol snapshot), `recorded manual_bump row for v29`, clean reconcile, 0 errors; **healthcheck OVERALL=OK 13/13**. First unblocked batch `bf93550f` **submitted=200 failed=0** (~21 min post-restart). **LIVE emission (submissions table, all 200 rows): 200/200 v29-stamped; 52 MR, primary gates {market_realized_vol 14 (27%), hurst 9, gamma 9, vol_regime 8, iv_rank 5, realized_vol 4, rv_rank 3}; vetoes {ivol 12, market_rv 9}; pairings LIVE incl. market_rv+ivol 7, hurst+market_rv 4, gamma+market_rv 3, vol_regime+market_rv 1, realized_vol+market_rv 1 — every asked shape in real supply; thresholds absolute op `<` in-sweep (0.1617–0.2467 sampled), on both the rank arm (underlying=None) and single names (XLV).**

**What.** Their confirm supplied every string (all independently verified: registry grep of `2026-07-12T053611Z`; direct writer probe — 78.7% of SPY bars pass `<0.20`, identical across underlyings = market-wide BY DESIGN, 2022-12 knife window 7/21 open): id `market_realized_vol`, family **macro** (deliberate — C1 stacks with vol/idio families), v1, lookback 0, reference=SPY/window=21, semantics byte-matching their rv21 ledger tag → the 0.15–0.30 sweep translates 1:1 (the per-name v28 family's calibration weakness solved). Wired as (1) **R1 seventh gate** + MR primary pool + ranging boost (3.0), absolute-only/gate-only/horizon-1; (2) **MR veto pool widened to TWO members** (ivol, market_realized_vol) with the C1 guard generalized **per-ID** (`regime_veto_family_by_id`) — their explicit "pair it with EITHER existing gate"; one veto slot per config, three-gate stacks stay Q46.

**Build evidence:** TDD red→green (10 failing tests first); **all pre-v29 goldens UNCHANGED** (single-id pools consume rng identically under the per-id refactor — dedicated regression test pins it; unlike v28, no re-pins) + new `_REGIME_GOLDEN_V29_ACTIVE`; suite **1912 green** contended + uncontended; ruff + mypy --strict + both grammar hooks clean. Emission proof (live registry, 3000): MR primary {**market_realized_vol 163 (27.6%)** — ranging boost + the structural bonus that macro stays eligible in vol_target-chained configs where volatility gates are excluded; gamma 99, rv_rank 76, vol_regime 72, realized_vol 70, hurst 58, iv_rank 52}; veto draws ivol 176 / market_rv 117; **pairings: market_rv+ivol 86, rv_rank+market_rv 25, vol_regime+market_rv 19, realized_vol+market_rv 17 — every asked shape**; thresholds 0.1503–0.2995 all-absolute.

**Relay ready:** `PROMPT_CRUCIBLE_GRAMMAR_V29_FUNNEL.md` (`crucible funnel --compare v28 v29`; reads: market-level vs per-name lineages, and the pairing arms as the Q46 evidence trigger). [[promotion-gate-tiers-and-constraint]]

## 2026-07-12 — GRAMMAR v27 → v28 DEPLOYED+VERIFIED 2026-07-12T03:41:18Z (D265): realized_vol ABSOLUTE MR regime gate LIVE — the champion's percentile-normalization defect gets its systematic complement. Operator green-lit in-session with Crucible's convention reply (`CRUCIBLE_activation_cache_fix_and_rv_convention_2026-07-12`) attached; proposal `2121cafe` APPROVED.

**Deploy evidence:** commit `276d229`; new daemon PID 1611177/1611182, NRestarts=0; journal `grammar_version=v28 registry_hash=4dd514f4312ad816`, `recorded manual_bump row for v28`, clean reconcile, 0 traceback/SchemaVersionMismatch/GrammarVersionError. **healthcheck OVERALL=OK (13 ok, 0 warn, 0 crit — the long-standing wf_p25-drift WARN has cleared, latest +0.016).** First unblocked batch after ~12 min of normal §7.3 backpressure: `28123ff0` **submitted=200 failed=0**. **LIVE emission (inbox payloads, 133 un-consumed sampled): ALL v28-stamped; 41 MR, primary gates {rv_rank 10, hurst 7, gamma 7, realized_vol 7, iv_rank 6, vol_regime 4} → realized_vol = 7/41 ≈ 17% of MR; all params ABSOLUTE op `<` in-sweep (e.g. RTX swing_mid <0.2419, CVX swing_short <0.2939, rank-arm underlying=None <0.2103 — the rank genome emits too, rank-coherent ✓); 1/7 ivol-stacked (small n).**

**Build evidence:** TDD red→green (9 failing tests first, expected reasons); all five cold-start goldens deliberately re-pinned (base fixture serves realized_vol for X1 — unlike v26/v27 there was no untouched-golden option; flag-inertness none==empty ×2 + all inter-golden structural relations re-verified pre-pin); full suite **1900 green** contended AND uncontended; ruff + mypy --strict + both grammar hooks clean; emission proof (live registry, 3000): MR gates {gamma 132, vol_regime 98, iv_rank 94, rv_rank 90, hurst 89, **realized_vol 78** = 13.4%}, ivol stacked 42.3%, vol_target co-occurrence 0, thresholds 0.1503–0.2939 all-absolute.

**Crucible thread (same-day round-trip):** their reply FIXED the activation-cache poisoning (`5266250` — underlying folded into the token; **verified live: identical spec SPY n=1701 vs HAL n=111**; pre-fix rows orphaned, no purge needed; Q48 impact-window assessment still open) and RESOLVED the rv convention: their ledger rv21 = **SPY market-level** (regime tag), not per-name — our 2025-03 "tension" was two true readings of different variables. Design implication they drew: the protective gate is better expressed MARKET-level (reference RV; their 0.15–0.30 bounds translate 1:1 there); per-name is a "valid second family" (needs name-relative bounds eventually or tight arms strangle hot names — our expected_trades wall culls those pre-submission meanwhile). **No market-wide realized-vol id exists in the registry** (vix_level = implied) → v28 ships the per-name family; the market-level id REGISTRATION ASK is relayed (`PROMPT_CRUCIBLE_MARKET_RV_REGISTRATION_ASK.md` — id/family/version/lookback confirms, the D258 discipline; family choice carries a C1 consequence spelled out in the relay: macro stacks with rv_rank+ivol, volatility replaces). On their confirm: fast-follow bump, dormant until their publish.

**Relays ready to carry:** `PROMPT_CRUCIBLE_GRAMMAR_V28_FUNNEL.md` (`crucible funnel --compare v27 v28`; the fold-column split of interest = MR × regime-gate lineage on gate pass-rate + cpcv-p25 tails) + `PROMPT_CRUCIBLE_MARKET_RV_REGISTRATION_ASK.md` (cache-fix ack + market-RV ask + the C1 replacement note flagged for their fold-column read). [[promotion-gate-tiers-and-constraint]]

## 2026-07-12 — v28 PROBED before build (operator: "probe to make sure this actually works first"): BOTH probes PASS → proposal `2121cafe` still PENDING, now probe-backed. Probing exposed a LIVE Crucible writer-cache bug (activation_dates rows collide across underlyings since 66a616d/06-24) → relay `PROMPT_CRUCIBLE_FEATURE_CACHE_ACTIVATION_POISONING.md` + Q48. Daemon untouched (v27).

**Probe 1 — emission (in-memory v28 constants, no src edits; 2000 forced-MR cold samples on the live registry): PASS.** realized_vol primary gate on 314/2000 (15.7%; below the naive 3/14=21% because vol_target-chain draws exclude ALL volatility gates — C1-correct); ivol veto stacks on 46.2% (both arms measurable); thresholds 0.1501–0.2998, all absolute op `<`; vol_target never co-occurs (chain guard verified). Current grammar rejects all 314 on **R1 and ONLY R1**; with the widened R1, **314/314 fully valid** — the change surface is exactly as proposed.

**Probe 2 — live writer data (per-name): PASS, and the handoff's mechanism is CONFIRMED on our own data.** 2022-12: `rv_rank<62` open **21/21 days on every one of HAL/CVX/SLB/TGT/BAC** while absolute rv held ≥0.25 — the percentile gate normalizes exactly in the regime-wide spike (their defect, reproduced). 2025-04: both gates bind. Full-history selectivity is name-heterogeneous: pass rate at `<0.20` = HAL 4.0% / SLB 6.2% / TGT 21.2% / BAC 26.3% / CVX 32.5% / JPM 39.0% / SPY 77.0% → tight arms will zero-trade high-vol names (expected_trades will cull; the gate sorts the rest) — the full 0.15–0.30 sweep is right. Also: `rv_rank<62` passes ~80% of ALL bars on every name — the champion's current vol protection is nearly-always-open. Convention question relayed: their ledger rv21 0.135 (BAC 2025-03) vs our `<0.15` open=0 that month — confirm ledger-rv21 ≡ registry realized_vol v2 before reading sweep bounds 1:1.

**INCIDENT FOUND (Crucible-side, live): writer feature-cache `activation_dates` rows keyed WITHOUT underlying** (`66a616d` 2026-06-24: sid=signal_content_key + token=sha256(first_dt) — every name shares first_dt=2018-01-02 → first-writer-wins poisoning; verified: SPY/HAL/TGT/NVDA identical sets; `<0.299`→2005 / `<0.30`→563 / `<0.301`→2008 non-monotonic from a poisoned row). value_series/returns/regime_label layers verified CLEAN (their keys include underlying). Blast radius: Forge's activation-count prefilters ran on cross-name firing dates since ~06-24 (stream-quality noise floor, NOT promotion evidence — gate/engine/gated_runs independent + clean); `check-activations` per-name breakdown decorative since then (INERT detection unaffected, GO verdicts stand). Probe worked around it via per-name threshold-epsilon cache-busting (probe-only pattern). **Relay ready to carry** (repro + fix suggestion + purge ask + our-probe-pollution disclosure): `PROMPT_CRUCIBLE_FEATURE_CACHE_ACTIVATION_POISONING.md`. Forge-side impact assessment = **Q48** (funnel compare after their fix+purge). Probe scripts: session scratchpad (`probe_v28_absolute_rv.py`, `probe_v28_part2_cachebust.py`) — ephemeral, technique documented in the relay.

## 2026-07-12 — Crucible handoff `FORGE_mr_absolute_vol_gate_request_2026-07-12` (MEDIUM-HIGH) reviewed + verified → v28 proposal `2121cafe` PENDING operator. Nothing built, daemon untouched (v27).

**The ask: MR variants whose vol protection is an ABSOLUTE annualized-RV threshold (`realized_vol` op `<`, sweep 0.15–0.30), keeping the ivol percentile gate — targets the champion MR leg's two worst CPCV blocks (5+8), where the `rv_rank < 62` PERCENTILE gate normalizes in regime-wide vol spikes (knife-catches at abs rv21 0.135–0.27). Their counterfactual is honest: rv21>0.18 removal = P&L-NEGATIVE overall (+46,883 strong blocks) but GATE-POSITIVE (weak blocks decide cpcv_p25); no gated config probed — generating the family IS the probe (the ivol_lo pattern).**
- **Verified:** `realized_vol` v2, family `volatility`, lookback 20 (= their "21d RV"), rank-coherent (`registry_snapshot_2026-07-12T010004Z.json`); writer-computed TODAY (live vol_target X1 chains + trend confluence) → no D254 gate-3 concern (`check-activations` is directional-only anyway). All machinery exists: absolute `regime_range` path (vix_term_slope precedent), R1-widening pattern (D167 rv_rank / D254 vol_regime), ivol veto stacks C1-legally on any volatility primary (D263).
- **C1 expressibility finding (relay back when responding):** realized_vol = family volatility = same family as rv_rank/vol_regime → absolute-RV and rv_rank are MUTUALLY EXCLUSIVE in one config; the absolute gate REPLACES the percentile in the vol slot. "Percentile AND absolute" jointly would need a §3.5 C1 carve-out — NOT proposed. The asked both-gates shape (absolute RV + ivol) IS expressible; ~50% of variants carry ivol (`_REGIME_VETO_SHARE`) → both arms measurable.
- **Proposal `2121cafe-5be1-4325-ab46-104a95559dc1` (v28, PENDING, hard rule #4):** (1) R1 accepted OR += realized_vol (custom_predicates); (2) MR regime pool += realized_vol (search_space); (3) realized_vol regime_range (0.12,0.25)→(0.15,0.30) per the asked sweep — shared range also serves relative_value's broad pool, shifts under the same bump, noted; (4) `_MR_RANGING_GATES` += realized_vol (weight 3.0, calm-vol thesis class) → ~21% of MR primary draws; (5) grammar v27→v28 + archive + D-entry. No contracts change (matches their claim), no signal_horizon change, MR veto slot stays ivol-only.
- **Secondary observation logged Q47** (rolling_sharpe 63d trend warmup vs momentum_252 — explicitly no-action-yet, their in-book d36b1cb1 evaluation in flight).
- **NEXT (operator-gated):** approve/reject `2121cafe`; on approval build v28 per `docs/tasks/grammar-change.md` (TDD, emission proof: 2000 cold samples → MR×realized_vol share + ivol-stack split + threshold spread, deploy ritual, funnel relay `--compare v27 v28`). [[promotion-gate-tiers-and-constraint]]

## 2026-07-10/11 — GRAMMAR v26 → v27 DEPLOYED+VERIFIED 2026-07-11T07:45:31Z (D264): resid_vix LIVE — residual_momentum x vix_term_slope, the FIRST walk-forward-gate passer in program history, is now real Forge supply. Operator approved in-session ("just put it in the next grammar"; injection lane declined).

**Deploy evidence:** commit `ba1f2b2`; new daemon PID 4156062, NRestarts=0; journal `grammar_version=v27 registry_hash=c223fcfd62e5fade`, `manual_bump row for v27`, 0 errors. First iteration: 5000 enumerated → `submitted=200 failed=0` (batch `174e6d28`). **LIVE emission (inbox payloads): 15/200 residual_momentum, 18/200 vix_term_slope, incl. the exact pair `b2f9824a2afdf109` (v27, swing_mid, pct >0.8194, window 73/skip 18, gate >0.6614 — the tighter band their failure analysis asked for).** healthcheck OVERALL=WARN (12 ok; lone warn = pre-existing wf_p25 drift). Relay `PROMPT_CRUCIBLE_GRAMMAR_V27_FUNNEL.md` ready to carry (`crucible funnel --compare v26 v27`).

**Build evidence (pre-deploy):** TDD red→green (11 failing tests first); all 4 pre-existing cold-start goldens UNCHANGED (fixtures serve neither id — byte-identity held), new `_REGIME_GOLDEN_V27_ACTIVE` pinned; `check-activations residual_momentum` = **[ OK ] 746 activations 4/4 names** (D254 gate cleared; was [UNCHK] pre-wiring); live-registry emission proof (2000 cold): trend=380, resid=58 (~15%), vix gate=59, exact pair=11, example = the probe shape (swing_mid, pct 0.6882, window 65/skip 0, gate >0.3968); ruff+mypy --strict clean; full suite **1891 green** pre-bump AND post-bump (uncontended). Proposal `0a4d8da8` APPROVED in-session; D204 ride-along dropped (already shipped in v24 — stale grammar-change.md note fixed).

**Handoff `FORGE_resid_vix_generation_request_2026-07-11.md` (HIGH): their probe `probe_resid_vix_swing_mid` (hand-built on a Forge swing_mid trend chassis) blended 20% into their closest book passed WF median 2.0611 (gate 2.0, seed-robust spread 0.014) AND lifted cpcv_p25 1.166→1.2987. Both ids long-registered, both DARK (0 of 430,563 submissions). Verified + prepared, not shipped:**
- **Verified:** both ids in `registry_snapshot_2026-07-11T010003Z.json` (71 inds; residual_momentum family=trend/rank-coherent, vix_term_slope family=macro/market-wide). Dark-supply claim corroborated structurally: neither has an `indicator_thresholds` entry → `is_threshold_skippable` in all threshold roles → never emittable. `check-activations residual_momentum` = **[UNCHK]** (not enumerable yet, cannot probe pre-wiring; their campaign-grade probe (1,341 trades) proves the writer computes both — MANDATORY re-probe post-wiring pre-deploy, D254).
- **Proposal (v27, PENDING operator):** (1) residual_momentum trend directional, percentile-only (0.60, 0.90) op ">", custom window/skip params sampler [63,252]/[0,21], horizon 63 → **swing_mid always** (the validated chassis); (2) R2 pool += vix_term_slope, regime_range (0.0, 2.0) op ">" — **reverses D131's deliberate exclusion** (its "not validated for trend conditioning" rationale is directly superseded by their measurement); horizon 1 gate-only. Delta 0.30–0.55 already covered (v16). Piggyback: D204-deferred R2 evidence_to_relax metadata fix.
- **Structural finding:** the asked swing_mid-VS-swing_long DTE axis is NOT expressible for one indicator id under D102 horizon-matched derivation (k∈{2,3,4}×horizon snaps medium horizons to swing_mid always, long_lookback to swing_long always). swing_long arms → the injection lane or a future D102-class change.
- **Recommended fast lane (separate decision):** one-off inbox cohort injection (07-07 winning-burst pattern) sweeping the FULL asked grid incl. swing_long — real supply for their RUNNING overnight assembly campaign in ~a day; grammar v27 remains the durable/learnable path ("validated == traded requires the config to arrive through the pipeline").
- **Secondary asks logged:** Q45 (nine dark registry indicators; no standing triage loop — proposed a periodic dark-supply report, visibility not auto-activation), Q46 (multi-gate regime emission; veto machinery = the generalization seam; non-blocking per Crucible). Gate debounce/confirmation ask has NO grammar/engine axis — relay back when responding.
- **NEXT (operator-gated):** approve/reject proposal `0a4d8da8`; decide the injection lane; then build v27 per `docs/tasks/grammar-change.md` (TDD, worktree-or-stop-window, emission proof, check-activations [ OK ], deploy ritual, funnel relay). [[promotion-gate-tiers-and-constraint]]

## 2026-07-09 — GRAMMAR v25 → v26 DEPLOYED+VERIFIED 2026-07-10T01:57:19Z: `ivol` name-selection VETO on mean_reversion (D263) — the validated `ivol_lo` lever LIVE. Operator: "let's go to v26 so we can lock in the gains." Emission proof: 273/562 MR configs carry ivol, stacked on rv_rank (the validated form).

**Extends the D258 veto machinery from trend/dsj to MR/ivol. Built in the live tree (Python not hot-read — daemon kept producing v25 undisturbed); grammar bump in the deploy stop-window.**
- **What.** `ivol` (per-name CAPM-residual idio-vol, family `idiosyncratic_vol`) as an OPTIONAL 2nd `regime_filter` on `mean_reversion` — a percentile veto (op `<`, plateau [0.2,0.3,0.4], window 63) excluding the high-idio-vol "falling knives" (Bhootra-Hur). Crucible-validated: +0.163 cpcv, 6/6 champions; book-level cpcv-p25 +0.087 "wall held" — a TAIL/construction lift, NOT a promotion unlock.
- **STACKS on the vol gate (opposite of dsj).** `ivol` (idiosyncratic_vol) ≠ the MR level gates (`rv_rank`/`vol_regime` = volatility), so §3.5 C1 permits it to AND on top — the validated form (verified: 80/191 sampled ivol configs stack on `rv_rank`). Required GENERALIZING the D258 C1 guard: `_DSJ_VETO_FAMILY` (hardcoded volatility) → per-hypothesis veto family from the registry. `rules:` text UNCHANGED; R1/C1 need no edit (R1 plural-aware, C1 generic).
- **Byte-identity.** dsj path + both existing goldens (`_REGIME_GOLDEN_PRE`, `_REGIME_GOLDEN_DSJ_ACTIVE`) UNCHANGED (their fixtures don't serve ivol → MR veto pool empty). New `_REGIME_GOLDEN_V26_ACTIVE` (dsj+ivol served; 3/15 carry ivol). `ivol` already registry-live (1.28.0/D262) → v26 emits immediately on deploy, no dormant window.
- **Verified pre-deploy:** enumeration suite 340 green (byte-identity held); full suite green; ruff + mypy --strict clean; `check-activations ivol` = GO ([UNCHK] — not yet emitted under v25; re-probe post-deploy). New `regime_percentile_window` spec field (None→252 default, byte-identical for all existing indicators; ivol=63).
- **DEPLOYED+VERIFIED 2026-07-10T01:57:19Z (commit `180087c`).** Ritual: stop → bump v25→v26 + archive `v26.yaml` + `test_v1_grammar` v26 → uncontended suite **1875 green** + both grammar hooks (committed via `uv run git commit` — hooks need the venv `python`) → restart. New PID 2750088, NRestarts=0; journal `grammar_version=v26`, `recorded manual_bump row for v26`, clean. **Emission proof: 273/562 MR configs carry `ivol` (~49%), sample stacked on `rv_rank`** (the validated form). healthcheck OVERALL=WARN (12 ok; lone warn = pre-existing wf_p25 drift). Relay `PROMPT_CRUCIBLE_GRAMMAR_V26_FUNNEL.md` (carry: `crucible funnel --compare v25 v26`). [[promotion-gate-tiers-and-constraint]]

## 2026-07-09 — D261 FOLLOW-UPS CONVERGED: relay ANSWERED — Crucible adopted the sequencing rule + shipped contracts 1.29.0 (`parse_skipping_unknown_literals`). Forge adopted 1.29.0 + WIRED their tolerant reader (D262). DEPLOYED+VERIFIED 2026-07-10T01:19:14Z — daemon on 1.29.0, `registry_family` healthcheck OK (reader armed). Grammar unchanged (v25); enumeration byte-identical.

**The two D261 follow-ups + Crucible's same-day reply (`PROMPT_CRUCIBLE_IVOL_RECLASS_OUTAGE.md` → `FORGE_ivol_reclass_outage_response_2026-07-09.md`):**
- **Ask #1 (sequence vocabulary additions) — ADOPTED by Crucible.** Land contracts → consumer confirms adoption → THEN land the value-bearing export. Sharp nuance they added: their `crucible-registry-publisher` timer republishes from the tree ~6h, so **the export-side commit IS the publish** — the rule binds at commit, not the manual trigger. Recorded both sides.
- **Ask #2 (own the tolerance in contracts) — SHIPPED as contracts 1.29.0** (`parse_skipping_unknown_literals(RegistrySnapshot, payload, skip_in=("indicators",)) → (snap, skipped)`): drops `skip_in` elements with an unknown enum member + prunes additive fields in one pass, strict otherwise; `skipped` designed to wire into healthcheck. Purely additive (a new fn, no vocab change) → cannot reproduce the outage.
- **D262 — Forge adopts 1.29.0 + wires the contracts reader** (hard rule #2 / D250 seam — inter-system tolerance lives in contracts, not a Forge hand-roll). Pin `FORGE_EXPECTED_CONTRACT_VERSION` 1.28.0→**1.29.0**; `registry_loader` now calls `parse_skipping_unknown_literals` and re-emits any `skipped` as a structured `registry_unknown_family_skipped` WARN → `check_registry_unknown_family` healthcheck (`registry_family`, journal-fed; mirrors D246). An interim Forge-side pruner was built+tested, then REPLACED by the 1.29.0 helper before commit. **Enumeration byte-identical** (ivol not enumerated; no vocab change).
- **Honest limit unchanged:** graceful-degrade ≠ adoption — a skipped family silently shrinks the enumerable set (benign for non-enumerated `ivol`; a temporary loss if a USED indicator is reclassified). Sequencing (now agreed) is the clean fix; the WARN/healthcheck is the adopt signal.
- **Verification:** 26 changed-scope green; ruff + mypy --strict clean; full suite green on 1.29.0.
- **DEPLOYED+VERIFIED 2026-07-10T01:19:14Z (commit `6b1f5b8`).** Preflight GO (1871 green) → stop (old PID 1768029, exit 143) → daemon-reload → start. New daemon **PID 2362816, NRestarts=0**; journal `grammar_version=v25 registry_hash=b568bc7ad66ae3e5`, `registry_loaded_from_export` clean, reconciling, no literal_error/traceback. `forge healthcheck`: **`contracts pin==installed 1.29.0`** + **`registry_family: no unknown-family indicators skipped`** (reader ARMED); OVERALL=WARN (12 ok, 1 warn, 0 crit) — lone WARN = pre-existing `wf_p25 drift` (D260, unrelated).
- **v26 `ivol_lo` grammar wiring: NOT needed / parked** (operator 2026-07-09) — optional, doesn't unlock promotion; no forcing function, independent of the 1.29.0 adoption. Bank the marginal MR construction lift whenever wanted. [[promotion-gate-tiers-and-constraint]]

## 2026-07-09 — INCIDENT FIXED: adopt contracts 1.28.0 (D261) — the daemon was failing EVERY poll on the reclassified `idiosyncratic_vol` registry. Bundled in the same restart: dsj VERIFIED LIVE (D258 activated) + dsj-active golden pinned. Grammar unchanged (v25); enumeration byte-identical.

**Crucible's `ivol_lo` handoff reply (`FORGE_ivol_lo_family_answer_2026-07-09`, answering relay `PROMPT_CRUCIBLE_IVOL_LO_MR_GATE.md`) reclassified `ivol` → family `idiosyncratic_vol` (contracts 1.28.0) — which triggered a live incident + three landed changes, one deploy:**
- **INCIDENT + FIX (D261) — the "deploy".** Crucible published contracts 1.28.0 (new family literal `idiosyncratic_vol`) AND a registry snapshot using it (`registry_snapshot_2026-07-09T234324Z.json`, live 23:43Z) before Forge adopted 1.28.0 → the running 1.27.0 daemon failed EVERY poll from 23:43Z: `ValidationError: RegistrySnapshot … literal_error, input_value='idiosyncratic_vol'; continuing next poll` (SOFT outage — alive, producing nothing; the D245-class asymmetric-upgrade trap on the registry-READ face, which `parse_forward_compatible` doesn't cover — a literal_error, not a prunable extra field; D260's 23:22Z "no halt" predated the 23:43Z snapshot). Fix: bumped `FORGE_EXPECTED_CONTRACT_VERSION` 1.27.0→1.28.0 + restart. Purely additive, **enumeration byte-identical** (ivol not enumerated by Forge; rv_rank/vol_regime/realized_skew stay volatility) → NO grammar bump. `uv.lock` already had 1.28.0.
- **dsj is ACTIVE, not DORMANT (D258 corrected).** Registry served `days_since_jump` since `…T00:06:43Z` (family volatility, v3, lookback 252 — matches `_v25_registry`); the daemon's computed `registry_hash` rolled `1456268f3db3995e` → `2e2499a2b07947f9`; veto emits on `trend_continuation` (50% share) ~1 batch post-deploy (no dormant window — dsj published by 00:06Z). D258 STATUS line updated.
- **dsj-active golden PINNED (registry-hash-free).** Crucible publishes **no** `registry_hash` (Forge hashes the snapshot) → D258's "re-pin against their hash" plan VOID. Pinned `_REGIME_GOLDEN_DSJ_ACTIVE` + `test_d258_dsj_active_cold_start_golden` vs the deterministic `_v25_registry` fixture — legitimately diverges from dormant `_REGIME_GOLDEN_PRE` (veto eligibility rng draw consumed on eligible trend configs); dormant path byte-identical.
- **`ivol_lo` grammar wiring UNBLOCKED (v26, deferred).** With `ivol`=`idiosyncratic_vol`, §3.5 C1 will permit `ivol_lo` stacked on the vol_regime/rv_rank MR champion — the exact validated form. Params: percentile plateau **[0.2,0.3,0.4]** (op `<`), window 63 FIXED. Honest scope (Crucible book preview): cpcv-p25 +0.087 (1.137→1.224, "wall held"), full-period Sharpe flat-to-down → a TAIL effect, NOT a promotion unlock; adopt OPTIONAL, no forced share.
- **NEXT (operator-gated, NOT started): the v26 `ivol_lo` MR grammar wiring** (pin already adopted here). Extend the D258 veto machinery to `mean_reversion` (regime_veto map key + MR veto constant + **generalize `_DSJ_VETO_FAMILY`** to the veto's OWN family so `idiosyncratic_vol` guards C1, not `volatility`) + an `ivol` `IndicatorThresholdSpec` (regime_percentile_range [0.2,0.4], window 63); tests + grammar **v25→v26** + archive + D-entry + deploy. ivol already registry-live → activates immediately on wiring. [[promotion-gate-tiers-and-constraint]]

**Deploy (D261): DEPLOYED + VERIFIED 2026-07-10T00:24:57Z (commit `61fc821`).** Suite **1868 green** (pin adopted) → stop (old PID 10235, exit 143) → commit → daemon-reload → start. New daemon **PID 1768029, NRestarts=0**; journal `grammar_version=v25 registry_hash=000d5a44d9c2ac9e` (new hash = the idiosyncratic_vol snapshot content; **grammar UNCHANGED**), `registry_loaded_from_export` CLEAN — the `literal_error: idiosyncratic_vol` is **GONE**, `reconciled batches=1 newly_gated_total=198`, full learned-weight maps + first UNBLOCKED iteration healthy, no traceback/SchemaVersionMismatch. `forge healthcheck`: **`contracts pin==installed 1.28.0`** (skew WARN cleared), OVERALL=WARN (11 ok, 1 warn, 0 crit) — the lone WARN = pre-existing `wf_p25 drift` (D260-documented, unrelated). **Incident RESOLVED.**

## 2026-07-09 — ranker-eval slowness ROOT-FIXED: vectorized the learned-model trainer with numpy (D260). Verdict IRLS on 250k×106 was ~15 min / 15 G pure-Python single-core; now ~18 s, fp-identical models. [[D260]]

**Operator: "ranker-eval is slow" → profiled it → the cost was the pure-Python fits, not IO/features.**

- **Root cause:** the honest era grew to **N=250,533 rows × 106 features**; the pure-Python Newton-IRLS Hessian is O(N·d²) ≈ 14 B multiply-adds/train. D132 chose pure Python but sized it for a **10k-row window** that `build_dataset` never enforced (25× past premise). `build_dataset` itself is only ~20 s — never the bottleneck.
- **Fix (D260):** numpy-vectorized `_fit_irls` / `_solve_ridge` / `_standardize_design` (linalg only, no RNG — rule-#8-clean). **Scoring untouched → daemon byte-identical.** Same math: old-vs-new on a real 251k snapshot = `max|Δcoef|` 5.6e-13 (verdict) / 9.7e-10 (robustness), metrics identical to 11-13 sig figs.
- **Timing:** full-frame fits **17.6 s (verdict) + 2.6 s (robustness)** vs ~15-18 min pure-Python → the eval's 21-min CPU should drop to ~1 min. Also 15 G → ~0.3 G RAM (ndarray vs list-of-lists).
- **Deploy:** training-only; **no daemon restart** (only the eval trains; daemon scores). numpy added to venv (`pyproject.toml`); committed `a66af5a`. Suite **1866 green** (lone red = concurrent contracts **1.28.0** ivol bump vs 1.27.0 pin — minor, no runtime halt, operator's work — NOT touched).
- **CONFIRMED in production:** manual `forge-ranker-eval` at 23:22Z ran in **1 min 30 s wall** (was ~21 min), CPU 1286 s → 243 s, memory 15.4 G → 11.4 G. Trained+published verdict (auc 0.833) + robustness (r2 0.207) matching validation; `model: fresh 0.0h` clears the D259 CRITICAL. The `wf_p25 drift` WARN persists (latest -0.005) — same known-weak-IC lane, provably NOT from this change (models fp-identical); still a watch item, don't act.

## 2026-07-09 — gate-tail prereg CONFIRMED (finalized) + fixed a CRITICAL: ranker-eval starved by a full /tmp (58h stale models) → cleaned/retrained + new healthcheck tmp_headroom guard (D259). [[D259]]

**Operator: "it's 07-09, flip the gate-tail on" (= finalize it) → then "clean up so it doesn't recur" → chose the proactive /tmp-headroom healthcheck.**

- **gate-tail finalized:** prereg `9063b405` → **confirmed** (flip-gate MET at flip time, 7/3; post-deploy rewire Δ +0.390). Gate-tail was already live on v25 (survived v24→v25) — nothing to deploy; this cements it.
- **CRITICAL found + root-caused:** healthcheck was CRIT (models **58h stale**). The daily `forge-ranker-eval` had failed every run since 07-07 on `cp forge.db /tmp: Disk quota exceeded` — **/tmp was full of four 5.5G forge.db snapshots I left during the D255 ve probing** (my discipline gap; `PrivateTmp=no` = shared /tmp). That also explains the missing 07-08/09 rewire checkpoints.
- **Fixed:** cleaned /tmp (73%→37%, +22G) → triggered the ranker-eval retrain (in progress; models refresh, rewire clock resumes). The eval script itself self-cleans (`trap … EXIT`); daemon was unaffected.
- **Prevention (built):** healthcheck `tmp_headroom` — /tmp free as a ratio of forge.db size (WARN <5×, CRIT <3.5×, scales as the DB grows), alerts on the CAUSE before the eval breaks vs the `model` check that CRITs 2 days late on the symptom. 12 unit tests green, ruff/mypy clean, no daemon restart (CLI/timer picks it up). MANPAGE → twelve checks.
- **Still open:** holdout prereg `61837dd2` (deferred; ~3 decided holdout on 07-07, needs days) — on the 07-09+ nudge with the ve-supply monitor.

## 2026-07-08 — v25 grammar DEPLOYED + VERIFIED 2026-07-09T00:14:11Z (D257 exit fix + D258 dsj veto + contracts 1.27.0 adopted). forge.service on v25, NRestarts=0, no errors.

**Two Crucible-driven changes + the folded contracts pin, one v25 enumeration-policy bump (ff-merged `v25-dsj-exit-fix` → main `0559239`; `rules:` text untouched):**
- **D257 — exit fix (ACTIVE):** `zscore_reversion_exit` dropped from `mean_reversion`'s S5 set (inert pair exit; Crucible `FORGE_inert_pair_exits_2026-07-08`). Scope narrower than the handoff — `convergence_exit` was already relative_value-only; only the MR mis-scope needed fixing. Caveat logged: shifts share to `target_exit` (Crucible-flagged harmful) → "what replaces it" left OPEN pending their `exit_timestop_sweep`. Feedback-invalidation ask = no-op (Forge keys feedback on hypothesis, not exit-id). `_REGIME_GOLDEN_PRE` re-pinned (MR seq 11/14) after re-verifying none==empty inertness.
- **D258 — dsj veto (DORMANT until registry):** `days_since_jump` event-frequency veto wired as an OPTIONAL SECOND regime gate on `trend_continuation` (needed because R2 forces the trend-strength gate + S3 permits >1 → dsj must be additive; Forge emitted one gate). All 3 Crucible confirms verified against their snapshot build (family `volatility`, version 3, plateau 30–65). Empty veto pool until Crucible publishes → byte-identical cold path (verified: journal shows dsj absent on `registry_hash=1456268f3db3995e`). C1-safe by construction (skipped when any volatility signal present — `rv_rank`/`vol_regime` OR the `realized_vol` vol_target chain). **Emission gated on Crucible's registry-live ping (~1 day; states registry_hash for re-pinning dsj-active goldens).** Path B (dsj-as-primary swap) deferred to v26 pending their `dsj_swap` arm.
- **Contracts 1.27.0 folded in + adopted:** `FORGE_EXPECTED_CONTRACT_VERSION` 1.26.0→1.27.0 (RunResult.measurement_basis + fullhist_refit_of, Crucible DSR Q4 — purely additive, forward-compat tolerant). Completes the operator's in-flight upgrade (`uv.lock` now committed).
- **Deploy evidence:** preflight GO (1866 passed) → stop (exit 143, normal `--loop` SIGTERM) → reset-failed → daemon-reload → start. Journal: `grammar_version=v25 registry_hash=1456268f3db3995e`, `reconciled batches=2 newly_gated_total=223`, all feedback maps loaded, **no traceback / SchemaVersionMismatch / GrammarVersionError**. `NRestarts=0`.
- **Crucible-side services deliberately NOT restarted:** 1.27.0 is additive/non-breaking, and Crucible's own dsj reply says it is deferring its db-writer/watcher restarts behind the in-flight book-campaign queue (a mid-campaign writer restart risks the finalize path). Forge-on-1.27.0 ↔ Crucible-on-1.26.0 interoperate safely.
- **TODO:** relay v25 + deploy timestamp to Crucible (`crucible funnel --compare v24 v25`, `crucible-handoff.md`). Relay OUT already: `PROMPT_CRUCIBLE_DSJ_V25_CONFIRMS.md` (answered). D257/D258 entries + `docs/GRAMMAR.md` synced. [[promotion-gate-tiers-and-constraint]]

## 2026-07-08 — Path C dossier RE-PRICED (`docs/proposals/path-c-scope-expansion.md` §0) — resume signals #1 + #2 now MET; operator decision brief ready. Nothing sent, nothing built; daemon untouched.

**The step-2 follow-through on the alpha-budget results: re-price the parked Path C decision against the dossier's own §3 resume signals.**
- **Signal #1 (drought in practice) MET:** the whole in-scope lever program ran to completion post-parking (F3 → quality lane → prefilter flips → ve floor → gate-tail → holdout D256 "lever list exhausted" → grammar v20→v24); standing promotions still ZERO (the only promote verdicts were the 07-01/02 refit flukes killed by the charged DSR).
- **Signal #2 (monitor firms the verdict) MET-and-closed:** alpha-budget replaces the M1/M2 monitor with a closed answer — standard-basis honest max 1.343 at n=10,004 (no creep at ~10× the D152 population), running max ~0.7σ under the noise envelope, accumulation mechanically cannot reopen long-options. Final OOS check = prereg `098ea730d5f2` (resolves ≤07-21; a breach pauses the re-pricing).
- **New bar folded in:** any Path C sleeve inherits the campaign multiplicity → must clear the triple {cpcv-p25 ≥ 1.5 · WF ≥ 2.0 · sharpe_baseline ≥ 1.254-and-rising}; the sizing ask must measure that triple net-of-cost on the STANDARD basis. Grammar-review sharpening folded: net-long-vol invariant REPLACES the rung ladder (rungs 2–3 = out-of-identity, do not size as entry).
- **First step if operator un-parks (unchanged, cheap, commits nothing):** refresh + send `PROMPT_CRUCIBLE_PATHC_DEBIT_VERTICAL_SIZING.md` (drafted 06-15 — predates the charged-DSR triple/basis discipline; needs the refresh). Only real timing question: send now vs after prereg resolution. **Governance gate first regardless:** operator-ratified §20 reconciliation of Crucible's hard-rule-9 before any byte ships. Off-ramps (§3) stand; the promotion-criterion revisit is listed as an exit, not advocated.
- Doc-only change (dossier §0 + banner, this STATUS block). No D-entry (no design change; a D-entry lands with the operator's un-park/hold call).

## 2026-07-07 — Alpha-budget analysis BUILT + RUN (`scripts/alpha_budget.py`; spec + full results `ALPHA_BUDGET_SCOPE.md` §7). Standard-basis honest wall = **1.343** — the handoff's "flat ~1.40 wall" CONFIRMED (my interim "noise-creep to 2.99" read was a basis conflation, corrected below); every ≥1.5 sighting incl. both transient promotes is a post-selection fullhist-REFIT re-measurement; de-facto charged-DSR standalone bar = **sharpe_baseline ≥ 1.25-and-rising**. Read-only; daemon untouched.

**Context: operator asked whether Forge (not Crucible) is the viable half of the pipeline → run the Dim-C alpha-budget retrospective FIRST. Scoping + build, all verified against a forge.db snapshot (max decided_at 2026-07-08T01:01:59Z):**
- **Transient promotes (scoping find, stands):** verdicts holds 3 transient `promote` rows — 2 configs, both mr×swing_mid v22 (`7d07c259297e99de` WF 3.09/cpcv-p25 1.97; `3d6ed2afbfe4837e` WF 2.50/cpcv-p25 2.99). Both re-gated 07-03T06:15Z with **DSR deflated by n_trials=46,131** → DSR 0.9955/0.9995 → **0.012/0.073 → reject**. Charged DSR touched exactly those 2 rows ever; everything since is `n_trials=1` (**Step 4 exists in Crucible's code, NOT standing**). `promoted_patterns`=0; `component_contributions_*.json` publishes EMPTY on ~2-min cadence. "Zero standing promotions" holds.
- **⚠️ Correction of this block's earlier "noise-creep" claim (build-run finding):** the honest ≥1.5 values (2.99/2.17/1.97/1.87) are **fullhist-refit re-measurements** — post-selection re-gates on longer windows (3d6ed: standard-window cpcv **0.125** → refit **2.99**; refit panel = 8,947 honest rows). On the single standard-window basis (earliest row per hash): honest n=10,004 (**98.6% trend** — the verified-coverage lane ≈ the xsect-trend path; ve 100 / mr 29), **max cpcv-p25 = 1.343**, running max tracks **−0.7σ under** the noise envelope (no break-out) → handoff §3.4/§6.3 are NOT stale after all.
- **Results (report + prereg JSONs at `~/forge_data/alpha_budget/`, stamp 20260708T010159Z):** Q1 noise-null verdict flips on μ0×N_eff (null survives for N_eff ≥ 607 @ μ0=0 / ≥ 61 @ μ0=median; D215's measured ρ=0.158 → N_eff≈113) — not decisive alone; it cannot split "modest-edge bulk" from "selection-shifted null". Q3 **calibration CONSISTENT (spread 0.011): Crucible's charged DSR deflates `sharpe_baseline` with T = trade count** (cpcv-based combos wildly inconsistent → relay question answered empirically). **Required sharpe_baseline for DSR ≥ 0.95: 1.254 @ 46k / 1.303 @ 100k / 1.359 @ 250k trials**; the promotes had 1.06/1.08 → correctly killed. Campaign max honest sharpe = 1.648, so charged DSR is clearable — **the binders remain cpcv-p25 ≥ 1.5 (max ever observed on a consistent basis: 1.343) + WF ≥ 2.0**. Q4 prereg REGISTERED — **`forge prereg` id `098ea730d5f2`** (cut 2026-07-07T15:05:50Z, in `config/preregistrations.jsonl`): v24+burst cohort honest max cpcv-p25 ≤ **1.479**, resolve at honest n≥3,000 or 2026-07-21 via `forge prereg resolve 098ea730d5f2` (burst hashes resolve from Crucible's gated exports — they are NOT in forge.db verdicts).
- **Net for the Path C / promotion-criterion conversation (scope §7):** standalone promotion needs the triple {honest cpcv-p25 ≥ 1.5 — never observed on a consistent basis; WF ≥ 2.0; sharpe ≥ ~1.25-and-growing-with-N}; accumulation cannot help (the noise bar rises with N). Relay WRITTEN → `PROMPT_CRUCIBLE_ALPHA_BUDGET_DSR.md` (operator carries): Q1 46,131 derivation · Q2 Step-4 rollout plan (a default-on flip = feedback-era boundary for P(component) training) + should Forge populate `search_n_trials` · Q3 confirm deflation basis · Q4 optional `measurement_basis` export field to kill the refit basis-trap class.
- New standalone script only (ruff clean; no clock/RNG — stamps derive from the snapshot; stdlib+duckdb). No D-entry yet (research; a D-entry lands when results back an operator ask).

## 2026-07-07 — RESEARCH BURST: 1,000 sweep-winning trend/MR configs submitted to Crucible for research data (NOT a promotion play). Direct-to-inbox, paced; ~300 gated so far (all reject, expected). Daemon untouched.

**Operator ask: "ship trend and MR components using indicators that won from our sweeps" — seed the gated DB with tested exemplars of the D236/D254 sweep winners for research.** One-time; trend/MR = correlated core, so all-reject is the expected, correct outcome (the value is the tested rows + failure_buckets).
- **Cohort (1,000):** 500 trend (`trend_continuation` + `sma_slope`/`ad_slope` directional AND `chandelier_exit`) + 500 MR (`mean_reversion` + `vol_regime`/`zscore_returns`). Representative SEEDED draw (seed=20260707, NOT ranker-picked → no censoring bias), all passed the real prefilter battery, 0 overlap with prior submissions.
- **Why these:** the winning DIRECTIONALS were starved in the natural v24 stream — ever-gated sma_slope 80 / ad_slope 21 / vol_regime-in-MR 156, drowned by the established indicator pool; chandelier already abundant (bundled as the winning exit per operator).
- **Mechanism:** direct-to-inbox via `submit_candidate` — the live daemon holds the forge.db write lock, so the submissions table is unusable; same pattern as the `requeue` script. Paced by inbox backpressure (≤250 pending, D179 flood lesson). Consequence: NOT in Forge submissions / §7.3 / feedback (intentional — keeps research data out of the learning loop); identified for research by the config_hash manifest. 0 rejections at ingest (errors flat 3106).
- **Verified:** all 1,000 fed; ~297 gated in the rolling export (trend 116 / mr 181), decision=reject (expected). Rest draining. Full results Crucible-side via the manifest (rolling export undercounts — early gated roll off the 10k window).
- **Artifacts (durable NVMe `~/forge_data/winning_cohort/`):** cohort.json, cohort_hashes.txt (manifest), submit_cohort.py, draw_v2.py, RESUME.md. Crucible relay: `PROMPT_CRUCIBLE_WINNING_INDICATOR_RESEARCH_BURST.md`. No D-entry (no design change; existing mechanisms). [[promotion-gate-tiers-and-constraint]]

## 2026-07-07 — FLIP DEPLOYED+VERIFIED: exploration holdout MVP (FORGE_EXPLORATION_HOLDOUT_FRAC=0.05, D256, prereg 61837dd2). Last teed-up lever — program COMPLETE. Also fixed 2 pre-existing v24 stale goldens. [[D256]]

**Operator: "keep it at MVP, go ahead." The plain random holdout (not decorrelation-targeted — that would re-introduce bias; targeting decorrelation is an enumeration-floor job).**

- **What:** reserve ~5% of each batch (10/200) as a seeded RANDOM draw from the survivors the ranker didn't pick — REPLACES rank slots (total unchanged), tags them `selection_mode='holdout'`. Composes with gate-tail (gate-tail picks top n−holdout_n, holdout adds the random rest). Cap 0.10; unset=byte-identical. REVERT = drop the line + daemon-reload + restart.
- **Why now:** breaks the censored feedback loop — the F3/wf_p25/estimand models only ever get labels for configs they scored high, so they can't learn where they're wrong. Under gate-tail's hard P-gate that's sharper (ve exclusion could self-reinforce). The holdout keeps unbiased labels (and some ve) flowing regardless of the ranker.
- **Bonus:** stands up a free ranker-vs-random A/B (ranked vs held-out component-rate).
- **Prereg 61837dd2** (cut 22:49Z): ranked comp-rate > held-out comp-rate (ranker beats random) AND overall comp-rate ≥ baseline−0.006. Resolve by selection_mode on the post-flip cohort. Held-out ≥ ranked = ranker red flag.
- **Fraction:** 0.05 (half the cap) — conservative MVP; raise toward 0.10 later if the A/B wants power. Deploy = unit-env ⇒ daemon-reload + restart; verify `exploration_holdout: N of M submitted` in-journal.
- **Levers status:** flip #2 confirmed, flip #1 insufficient/kept, gate-tail live+kept (D255), holdout deploying. **The teed-up lever list is now exhausted.**

## 2026-07-07 — Probe verdict: gate-tail does NOT starve useful ve → KEEP, no carve-out (D255). ve-supply monitor set. gate-tail live on v24; healthcheck OK. [[D255]]

**Investigated the gate-tail ve-share drop (18%→8%) with a probe before deciding — it overturned the revert case.**

- **The correction:** I'd read ve's *realized* component-rate (0.003) as its *predicted* P. Wrong. Probe on `shadow_scores.model_score` (logged per-config P): ve's predicted P has real spread (median 0.024, max 0.32), **54.7% clears the 0.02 gate** — gate-tail selects *within* ve, doesn't exclude the family.
- **Decisive probe:** of **485 realized ve COMPONENTS, 90.3% clear the 0.02 gate** (median P 0.139); ve REJECTS have P median 0.0099 (39% clear). So P(component) predicts ve clearance — gate-tail keeps ~90% of the ve that matter and cuts the reject-bound ve. **Carve-out NOT necessary; no revert.**
- **Decision: KEEP gate-tail** (running on v24, `GATE-TAIL ACTIVE` p_floor=0.02 verified in-journal, coexists with the operator's v24 grammar — orthogonal ranking vs enumeration).
- **Monitor (light):** `scratchpad/monitor_ve_supply.py` tracks ve **components/day** (not rate — gate-tail keeps higher-P ve so the rate may rise; the book wants COUNT). **Baseline: blend era = 5.4 ve comps/day.** Trigger: revisit the carve-out only if gate-tail-era ve comps/day sits materially below ~5.4 once it has ~1-2 days of decided ve. A nudge to re-run it is scheduled.
- **Timeline note:** the operator concurrently deployed grammar v24 (D254, 15:05Z) which restarted the daemon over my 03:22Z gate-tail restart — gate-tail survived (unit env in the merged HEAD). D-number collision on 253/254 → this entry is D255.

## 2026-07-07 — GRAMMAR v22 → v24 DEPLOYED 15:05:50Z (trend D236 + MR D254 slices from the Crucible signal-quality handoff). Daemon healthy on v24; OVERALL=OK. [[D254]]

**Operator: "fold it in and deploy." The whole trend/MR grammar upgrade shipped in one `v22 → v24` deploy** (v23/D236 trend slice had never shipped, so it lands here too).
- **What deployed.** v23 (D236): trend `sma_slope`/`ad_slope` directionals + prune momentum-dup/weak dirs (§2.1–2.3), `chandelier_exit` default trend exit + `atr_multiplier` sweep (§2.7), `days_to_fomc` event window (60→14d, §2d). v24 (D254): admit **`vol_regime<2` to R1** as the MR regime gate (§2b.1 — Crucible's +0.244 CPCV-p25 xsect-MR champion), reweight MR ranging gates (drop hurst-boost/bias-away, add vol_regime), keep `zscore_returns`; D204 R2 `evidence_to_relax` fold. §2c.1 folded byte-identically (VE-direction retraction — no grammar change; days_to_fomc comment corrected to the magnitude-timer rationale).
- **Deploy (deploy.md).** Preflight GO (deploy-surface clean + full suite **1829 green** on contracts 1.26.0) → stop → ff-merge `5237d75..07c7aaf` in the live tree → restart. New daemon **PID 1280850, NRestarts=0**; journal: `grammar_version=v24`, `recorded manual_bump row for v24`, `registry_loaded_from_export`, no traceback/SchemaVersionMismatch/GrammarVersionError. `forge healthcheck` **OVERALL=OK (11 ok, 0 warn, 0 crit)** (`contracts pin==installed 1.26.0`). Operator's in-flight `fable-audit/README.md` + untracked docs left untouched by the FF.
- **HONEST SCOPE.** Stream-quality lift (better trend selection + MR component quality), NOT a promotion unlock — the bear-tail hedge (trend §0) and correlated-core (MR) caveats stand; `stuck_state` zero-promotion is Forge's steady producer state. Deploy landed mid gate-tail cohort (D252, operator's go) → that cohort's attribution now also carries the grammar bump.
- **⚠️ Strategy flag (§2c.1, 2026-07-07):** Crucible triple-confirmed single-name **vol-event DIRECTION is refuted** (call_wall/put_wall don't replicate, backtest WF≈0, efficiently priced — a signed-flow DATA gap). This is in tension with the "single-name vol_event = the in-v1 promotion path" thesis ([[promotion-gate-tiers-and-constraint]]) — flagged for the roadmap; independent of this trend+MR deploy.
- **NEXT:** relay `v22→v24` + the deploy timestamp to Crucible for `crucible funnel --compare v22 v24` (`PROMPT_CRUCIBLE_GRAMMAR_V23_FUNNEL.md`, updated to v24); watch the first UNBLOCKED batch mix for the vol_regime MR gate + chandelier trend exits.

## 2026-07-06 — Dependency/structure audit written: `fable-audit/dependency-structure/REPORT.md` (sixth track). Read-only; nothing executed; daemon untouched.

**Operator-requested structural audit at HEAD `5ac7941`: dependency graph (cycles/god objects/layering) + dead code, duplication, >50-line functions — prioritized P0–P3 in the report's §8.**
- **Graph healthy at the strict level:** zero top-level import cycles; the 2 soft cycles = the known prefilters↔feedback type-only cycle (SRC-M3) + a new lazy `cli.main ↔ shadow_null_cmd` cycle via `_build_feature_cache`. 4 layer-leak edges total, all vocabulary-misplacement (era-cut/coverage/stranded constants trapped in heavyweight feedback modules).
- **Headline item (P0-1):** the nine ~1.00-similarity learned-weight loaders + nine formatter twins in `cli/main.py` still re-parse the gated-runs export up to ~10×/iteration — one fix ticks codebase-quality item 9 AND pipeline-performance P1-1 (~430→~80 lines + the biggest recurring parse waste).
- **Dead code (P0-2):** near-clean repo — exactly 1 dead top-level name (`_REWIRE_DELTA_CRITERION`) + the test-only `compute_hypothesis_weights`/`compute_hypothesis_reward_weights`/`_iter_hypothesis_outcomes` stratum in rejection_weights (~150 lines; deleting closes pipeline-performance P4-8).
- Nothing changed outside `fable-audit/` + this block; all items are versionless code changes with byte-identity cautions marked (hard rule #6, `model_id` content-hash).

## 2026-07-06 — Services audit (D253): `forge-eod-check` RETIRED (timer disabled + units/script removed + docs); other 7 units confirmed earning. Also: D248's 3 doc bugs fixed + D245 follow-up landed (`crucible-handoff.md` both-directions restart rule, `2c7e6c0`). Daemon untouched by this work. [[D253]]

**Operator: "do 3, then audit all services/timers/watchers — forge-eod-check can probably go." Verified, then retired it. (Numbering: self-assigned D249 → renumbered D253; the concurrent session took D249–D252.)**
- **eod-check retire evidence:** nightly headless-Claude report (created 06-10) with a v17-fossilized prompt; alerting superseded 24× faster by the hourly healthcheck (D197/D240/D246); **failed its one real test** — the 07-04 21:06 report ran 3h into the D240 stall, saw the signature (841 depth-cap blocks, mix 100% depth-cap), and dismissed it as benign §7.3; nothing consumes the reports. Executed: timer disabled BEFORE its 21:00 fire; installed copies + `~/.local/bin` script removed; repo units/script `git rm`'d; 9 doc/comment sites updated. Reports at `~/forge_data/eod_checks/` kept as history. Revert recipe in D253.
- **Rest of audit:** backup/healthcheck/ranker-eval all firing. ranker-eval KEEP — its streak clocks fed the D252 flip decision and remain the monitor while the 9063b405 post-flip cohort reads out; cadence revisit after that resolves. Cross-system flag: `crucible-meta-king-publisher.timer` still publishes daily for the D190-retired king arm — relay-worthy, Crucible's side.
- **Addendum:** operator-requested CONDITIONAL plan written to `fable-audit/code-complete-retirement/REPORT.md` — the prioritized retirement list IF Forge+Crucible are ever declared code-complete (P0 grammar-change machinery → P1 flip apparatus → P2 dormant levers → P3 dev surface; explicit KEEP line; preconditions in its §0). Trigger NOT met; nothing executed.

## 2026-07-06 — FLIP DEPLOYED: gate-tail ranker re-wire (D252, prereg 9063b405) — P(component) gates eligibility, wf_p25 tail orders. Verified live (GATE-TAIL ACTIVE), healthcheck OK. [[D252]]

**Operator: "we can flip the gate-tail." Flip #1 being settled (D251, insufficient/kept) was the sequencing gate — now clear.**

- **The flip:** `FORGE_QUALITY_RANK_MODE=gate-tail` + `FORGE_REWIRE_P_FLOOR=0.02` on the systemd unit. P1.1 hard-gate form of the `--quality-rank` lane: P(component) gates eligibility at 0.02, wf_p25 tail orders survivors, §6.2 composite bypassed. Default `blend` (D193) unchanged in code; env selects the re-wire. REVERT = delete the two Environment lines + daemon-reload + restart (byte-identical).
- **Flip gate MET (pre-commitment):** SPRT promote logLR +15.45/2.77, mean Δ +0.344, fresh-PASS streak **6/3** — verified at flip time.
- **DEPLOYED 2026-07-06 (PID 3539733):** unit-env change ⇒ `daemon-reload` + restart. Uncontended suite 1829 green; journal `quality_rank: wf_p25 GATE-TAIL ACTIVE (p_floor=0.0200) hard-gate (composite bypassed)` — flip reached the iteration (NOT the blend line); healthcheck OVERALL=OK (11 ok). Changes the ranking → resolve 9063b405 LATER on the POST-FLIP cohort (rewire Δ vs blend baseline). No longer confounds the flip preregs (resolved D251).
- **On flip #1 (answered):** its `insufficient` is STRUCTURAL, not thin-cohort — the controlled shadow-count can't re-run post-deploy, and the pre/post cohort is confounded (more data won't fix either). Kept as a correctness fix. Definitive closure would need a one-off single_day-vs-cumulative script (optional, not run).
- **Next teed-up lever:** `FORGE_EXPLORATION_HOLDOUT_FRAC` (after gate-tail's post-flip cohort reads out).

## 2026-07-06 — Flip preregs RESOLVED on the clean cohort (D251): #2 exclude_regime_filter CONFIRMED, #1 cumulative_trading INSUFFICIENT (kept on correctness). gate-tail held. [[D251]]

**Ran the resolution on the clean post-runner-fix cohort (≥00:38Z; 90% real-gate, 1,432 decided in ~2h — the earlier 88%-`other`-failure window discarded as biased).**

- **Flip #2 (`5082d332`) → CONFIRMED.** ve signal_correlation reject 58%→4.5%, ve survival 5.5%→15.2% (~2.8×), content redundancy intact. Headline component-rate dip (0.127→0.097) is the INTENDED ve mix-shift (ve was ~0 component pre-flip too, 0.0033 — no regression). book-PBO Crucible-side/pending.
- **Flip #1 (`848a1f67`) → INSUFFICIENT; flip KEPT.** (1) The controlled shadow-count CAN'T re-measure a flip post-deploy — `shadow_null_cmd` reads the deployed prefilter.yaml as prod (now already cumulative_trading) → degenerate net-0. (2) Post-flip cohort shows no lift: trend 41.9→40.0 (flat), em 91→95 (up, tiny), **ve 44.5→38.6 (down, opposite prediction)** — confounded, not clean-refute but not confirming. **Kept** because it's *also* a correctness fix (trading-day vs the buggy calendar-day mode); dropped the ve-supply-lift expectation.
- **Levers:** flip #2 confirmed; **gate-tail (`9063b405`) HELD** until #1 settled (now settled = keep). Sequence next: gate-tail (unit-env + `daemon-reload`) → `FORGE_EXPLORATION_HOLDOUT_FRAC`, one at a time. The 07-07 reminder is now redundant (resolution done early — the gate rate recovered faster than expected).
- **Lesson:** a permutation-null shadow-count must run BEFORE the flip; post-deploy it's degenerate. Definitive post-hoc #1 read would need a one-off single_day-vs-cumulative script on the same configs (offered, not run).

## 2026-07-06 — Built `parse_forward_compatible` into the export read path (contracts 1.26.0, D250) + DEPLOYED onto 1.26.0: Forge reconcile self-heals on future additive fields. Daemon healthy. [[D250]]

**Operator: "let's build the parse_forward_compatible." Wired it into the two export loaders Forge re-reads (gated + failed) — the read-side (Forge-consumer) fix completing the trap-class arc alongside D245 (Crucible inbox) + D249 (Crucible runner).**

- **What/why:** 1.25.0 added the helper; 1.26.0 (`crucible_contracts 86c8515`) makes `load_recent_{gated,failed}_runs_from_export` parse rows via `parse_forward_compatible` — so a FUTURE additive Crucible export field (e.g. an eventual `failure_buckets` republish) prunes+warns instead of fail-looping the daemon on `extra_forbidden` (the D244 read-side trap). Correct seam per the helper's own docstring (tolerant RE-READ, not first ingest) + hard rule #2 (Forge reads exports only via these loaders). Byte-identical today; genuinely-invalid rows still raise.
- **Forge:** pin 1.25.0 → 1.26.0; `forge check` OK; contracts suite 328 green. **DEPLOYED 2026-07-07 01:08:20Z** (PID 3101697): uncontended suite 1829 green; clean startup, no traceback; `reconciled batches=1 newly_gated_total=198` (tolerant loaders parsed the live gated export); healthcheck **OVERALL=OK (11 ok)**, `contracts pin==installed 1.26.0` (WARN cleared). The restart also moved the daemon off its stale 1.24.0-in-memory onto current code — byte-identical (no model change 1.24.0→1.26.0).
- **Trap-class arc now closed on all three faces:** D245 inbox ingest (Crucible watcher restart), D249 runner re-read (Crucible runner restart), D250 Forge reconcile re-read (this).

## 2026-07-06 — Contracts 1.25.0 pin ADOPTED (D249, resolves D248's side-finding) + the runner-side `other`-failure spike (88% of the cohort) RESOLVED by runner restart. Pipeline healthy. [[D249]]

**Operator: "let's restart all the runners needed for 1.25.0" — the Crucible backtest runners were the ones that needed it; both restarted onto 1.25.0 @ 00:38Z and the `other` spike stopped. Forge's piece = adopt the pin (fixes D248's failing `test_expected_contract_version_matches_installed`).**

- **1.25.0 = `parse_forward_compatible`** (validators.py; no model change) — the durable fix for the D244/D245 additive-field `extra_forbidden` trap: a process behind the contract prunes the unknown minor field + retries instead of failing.
- **The `other` spike, explained + fixed:** the RUNNER-side face of the same trap — after Forge→1.24.0 emitted `mechanism`/`regime` and the inbox accepted them (D245), Crucible's runners (stale model) failed re-reading them as `other`. Ran 15:58Z→23:49Z (8,463 configs ≈ 88% of the clean cohort), **stopped dead when `crucible-runner@{1,2}` restarted onto 1.25.0** (00:38Z). Forge's D240 failed-flush absorbed them → no depth stall (but masked the spike — see D246).
- **Forge adoption:** pin 1.24.0 → 1.25.0 (`contracts_check.py`); `forge check` OK, 93 version/contract tests green. **Daemon restart DEFERRED** — 1.25.0 changes no parsed model, so no `extra_forbidden` trap on the running 1.24.0-in-memory daemon (unlike D244). Pin-adopt clears the WARN + is reboot-safe; restart at leisure.
- **Open / not-Forge:** contracts 1.25.0 still uncommitted WIP in that repo (operator to commit — D104 reboot hazard); Crucible inbox-watcher still 1.24.0-era (accepting fine). Worth building next: wire `parse_forward_compatible` into Forge's `consumer` reconcile so a future skew self-heals on our read side too.
- **Cohort note (flip preregs):** the 88% runner-failure meant only ~12% (1,106) of the 15:54Z cohort got a real gate, and that decided set is likely BIASED by the `other` failures — so the clean, unbiased cohort effectively **restarts at the 00:38Z runner fix**. The 07-07 reminder still applies; expect the component-rate read to need more post-fix decideds.

## 2026-07-06 — Dead-weight sweep (D248): safe tier landed — 5 never-referenced symbols removed, 4 answered 1.24.0 relays archived. Runtime-inert, NO restart. ⚠ Side-finding: `../crucible_contracts` carries UNCOMMITTED 1.25.0. [[D248]]

**Operator: "sweep for dead weight → deletion manifest," then "clean up the Safe items." Five parallel audit lanes (symbols / one-way flags / services+scripts+deps / doc link-graph / tests+CLI), every candidate cited to the last commit referencing it. Verdict confirms D247: essentially dead-code-free — the safe tier was 5 symbols and 4 closed relays.**
- **Landed:** dead symbols removed — `ThresholdProposal.baseline_width`/`.proposed_width` (dead since D073 `b75426e`), `_RULE_CATEGORIES`/`_COST_ESTIMATES` (`grammar/models.py` phase-1 scaffolding), `fixtures_dir`+`FIXTURES_DIR` (`tests/conftest.py`, bootstrap, zero consumers); D190 king `__pycache__` ghost dirs deleted (untracked); 4 ANSWERED 1.24.0 relays root→`_archive/` with banners (ADOPTED / DAEMON_LIVE — its publisher-restart clearance still stands / CLOSED_ACK per D244; INBOX_1_24_REJECTING per D245). ruff/format/mypy-strict clean; suite **1828 green + 1 UNRELATED pre-existing failure** (next line).
- **⚠ SIDE-FINDING (not this change):** `test_expected_contract_version_matches_installed` fails because `../crucible_contracts` has **uncommitted 1.25.0** in its working tree (validators change in flight; their HEAD is still 1.24.0 `0a0cd1c`) — the editable install already reports 1.25.0 vs our 1.24.0 pin. Daemon unaffected (1.24.0-in-memory since 15:54:09Z; journal healthy 17:35Z). Expect a Crucible handoff; adoption = the D244/D245 ritual (pin bump + restart BOTH directions). D104 hazard: a reboot silently picks up the dirty contracts tree.
- **Deferred (needs-owner manifest, in D248):** ve `|move|` flag path (D235 pre-authorized removal), `compute_hypothesis_reward_weights` (superseded D105), `is_percentile_emitting` (never wired), `probe_option_momentum_min_months.py` + its `probe_results/` JSON (one-shot, archive class), `pytest-cov` + coverage config (never invoked), 2 bannered-historical doc stragglers (`docs/handoffs/PROMPT_FORGE_NEXT_ACTIONS.md`, `docs/reviews/2026-06-14-sunday.md`), `PROMPT_CRUCIBLE_GEN_LEVERS_VALIDATION.md` stale HELD banner, `TABLE_NAMES` relocate. Doc bugs to fix cheap: MANPAGE:135 wrong kill-switch spelling (`--no-cross-sectional` vs `--no-cross-sectional-rank`); `FORGE_F3_RANKER` absent from MANPAGE env list; straddle proposal missing resolution banner.

## 2026-07-06 — Tech-debt sweep (D247): never-read forge.yaml keys + dead `with_overrides` retired; architecture.md module-map completed. Runtime-inert, daemon untouched, NO restart needed. [[D247]]

**Operator-approved cleanup (items 1–3 of a four-lane read-only inventory: entry points / pipeline / config surface / external interfaces). Headline finding: the codebase is essentially dead-code-free — zero orphan modules, CLI↔MANPAGE 1:1 — so the haul was deliberately small.**
- **Landed:** `afeedb4` retired §10.1 keys `data_root`/`log_root`/`feedback.*` (never read anywhere; `_resolve_run_defaults` consumes only db_path/crucible/enumeration/submission; feedback cadence is really `--consume-feedback` per iteration) + dead `ForgeConfig.with_overrides` (zero production callers) — recorded §10.1 deviation in D247, DESIGN.md untouched; `c1da76f` completed the architecture.md ranking/+feedback/ module-map rows; 2 orphaned `scripts/__pycache__` .pyc (D241-retired sources) deleted, untracked. Full suite **1829 green**, ruff + mypy-strict clean, production forge.yaml parse-verified post-change (batch 200 / max_inflight 600 / seed 42 intact). forge.yaml loads once at boot → running daemon unaffected; next restart is consistent by construction.
- **Numbering note:** this work initially self-assigned D246, but the healthcheck inbox-rejection check (`d2937a0`) took that slot concurrently → renumbered to D247, commit amended pre-push.
- **Deferred to operator (Q44 + D247 entry):** (1) write-only tables `pre_filter_logs`/`promoted_patterns` — keep-as-forensics vs retire-writes ruling; (2) `_archive/` the 3 completed one-time scripts (backfill_verdicts D111, migrate_verdicts_decided_at D117, requeue_high_value_configs); (3) unused contracts symbols (validate_config_against_registry, refit/portfolio set) = shared-package territory, handoff note at most. All dormant-by-design flags explicitly untouched (staged levers, not debt).

## 2026-07-06 — INCIDENT RESOLVED: 1.24.0 asymmetric-upgrade inbox stall (100% submissions rejected ~13h) — Crucible inbox-watcher restart + Forge rejected-flush (D245). Production flowing. [[D245]]

**Found while answering "where are we" — the daemon had produced ZERO accepted submissions for ~13h since the D244 restart. Different mechanism than D240/D244: the SUBMIT-direction mirror of the D244 read-path trap.**

- **Root cause (asymmetric 1.24.0):** 1.24.0 adds `StrategyConfig.mechanism`/`regime` (default None); Forge on 1.24.0 emits them as `null` on every submitted config; Crucible's `crucible-inbox-watcher.service` was still pre-1.24.0 in-memory (`extra="forbid"`) → **100% of submissions rejected** (`extra_forbidden`) into `inbox/errors/`. D244 fixed Forge READING the gated export (`failure_buckets`); this is Forge WRITING to the inbox, where Crucible's consumer held the stale model. Wedged the §7.3 per-batch limiter on `b381a83d` (0/200, never gates), surfacing only as a `no submission in 13.3h` WARN that looked like normal backpressure.
- **Third failure category:** inbox-REJECTED in-flight (never ran) are in NEITHER `gated_runs` NOR `failed_runs` → neither the D240 failed-flush nor the reconcile join retires them (only the 5-day aged-out flush would).
- **Resolved (cross-system, same-day):** (1) relay `PROMPT_CRUCIBLE_INBOX_1_24_REJECTING_SUBMISSIONS.md` → **Crucible restarted the inbox watcher onto 1.24.0 @ 2026-07-06T15:49:54Z** (`FORGE_..._inbox_watcher_restarted_2026-07-06.md`); fallback (Forge `exclude_none`) declined — 1.24.0 contract stands both sides. (2) Forge cleared the dead batch — `scratchpad/flush_rejected_inflight.py` (keyed on `inbox/errors/`) retired the 200 `b381a83d` rows → `gated`+sentinel → in-flight 0 → restart (PID 720197 @ 15:54:09Z).
- **Verified end-to-end:** fresh batch `b2f228f4` submitted=200, `inbox/errors/` **flat at 3106** (accepted, draining into runs), `forge healthcheck` **OVERALL=OK**, `submission: submitting (last 0.0h ago)`.
- **Follow-ups:** (a) contract-bump deploy discipline = restart BOTH directions' processes (Forge submitter + Crucible inbox watcher) — TODO fold into `crucible-handoff.md`; (b) **BUILT (D246):** `forge healthcheck` now has an `inbox_rejections` check (counts recent `inbox/errors/`; WARN@25/CRIT@100 in a 6h window) so a skew CRITs within hours instead of sitting silent — no daemon restart (CLI/timer picks it up); (c) a durable Forge retire-inbox-rejected reconcile — declined (would MASK skew; prefer the alert).
- **NEXT / cohort correction:** the flip-prereg "clean cohort" start moves to **2026-07-06T15:54:09Z** (the post-D244 02:10Z window was 100%-rejected → no valid cohort existed). The 2-day reminder (`trig_01WJn4mKctTG3Mnn7pXtx9J7`, fires 07-07 17:00Z) reads these docs at fire time and will pick up the corrected start; ~25h of clean cohort by then (may read "insufficient, wait more"). Then the teed-up levers `9063b405` (gate-tail) + `FORGE_EXPLORATION_HOLDOUT_FRAC`, one at a time.

## 2026-07-05 — Contracts 1.24.0 DEPLOYED + coordination CLOSED (D244): daemon LIVE on 1.24.0 (01:57:31Z), `extra_forbidden` trap closed, healthcheck OK. Crucible keeps their publisher deferred (buckets auto-compute on read). [[D244]]

**Crucible landed the D243 coordinated F1/F3/F4 bump (contracts 1.24.0) + notified. Adopted the version pin; verified a live-safety gap the handoff understated.**
- **Adopted (version-pin ONLY):** `FORGE_EXPECTED_CONTRACT_VERSION` → 1.24.0 (`contracts_check.py:79`), `uv.lock` → 1.24.0; `forge check` green, 12 version + 49 consumer/reconcile tests + ruff green. Committed (reboot-safe). Forge consumes NONE of the new surface — feature builds stay DEFERRED behind ve-supply per D243.
- **LATENT STALL (the thing to watch):** the running daemon (17:05Z boot, 1.23.0-in-memory, `GatedRun` extra="forbid") will fail-loop on `extra_forbidden` → §7.3 stall the MOMENT Crucible's exporter republishes carrying `failure_buckets`. Healthy NOW only because the newest export (`gated_runs_2026-07-06T010136Z.json`) still lacks the key (Crucible's exporter pre-1.24.0). Crucible's "daemon unaffected" is true for the runtime version check (major-only) but MISSES this parse-path trap (D124; same class as this morning's 15h stall, D240).
- **FIX = restart the daemon onto 1.24.0 (operator-gated, TIME-SENSITIVE vs Crucible's republish; NOT deferrable like the feature builds).** Confirmed SAFE any time — 1.24.0 parses both current no-field AND future with-field exports (`failure_buckets` = default_factory=list + auto-computed from gate_results). Ritual: deploy.md stop → uncontended suite → (pin already committed) → restart → verify. Relay `PROMPT_CRUCIBLE_CONTRACTS_1_24_ADOPTED.md` flagged the gap back to Crucible.
- **DEPLOYED + VERIFIED 2026-07-06T01:57:31Z (D244)** via deploy.md — preflight full suite **1829 green** on 1.24.0; new daemon **PID 47590**; clean startup (grammar_version=v22, registry_loaded, no traceback/SchemaVersionMismatch); **reconciled batches=1 newly_gated_total=9 on 1.24.0 (no `extra_forbidden`)**; `forge healthcheck` **OVERALL=OK** (`contracts: pin == installed 1.24.0`). **Latent stall CLOSED.** Crucible had proactively HELD their gated-runs publisher (still 1.23.0-in-memory, verified no `failure_buckets` key) pending this confirm (`FORGE_contracts_1.24.0_parse_safety_2026-07-05.md`); relay `PROMPT_CRUCIBLE_CONTRACTS_1_24_DAEMON_LIVE.md` cleared them to restart it any time.
- **COORDINATION CLOSED (both sides, `FORGE_contracts_1.24.0_closed_2026-07-05.md`).** Crucible **deliberately keeps their gated-runs publisher on old code** (no consumer until the F3 migration; restarting the DBProxy client risks wedging the db-writer accept loop — not worth it for zero benefit), so `failure_buckets` won't appear IN the export file until a future coordinated restart. **KEY LEARNING for the F3 build:** the 1.24.0 `mode="before"` validator **auto-computes `failure_buckets` from `gate_results` on read**, so the bucket-training migration consumes buckets with NO publisher restart — no plumbing dependency to pre-stage. Forge ack: `PROMPT_CRUCIBLE_CONTRACTS_1_24_CLOSED_ACK.md`. **Thread done; nothing owed. Priority back to the two flip preregs on a clean post-stall cohort, then the teed-up levers.**

## 2026-07-05 — Crucible F1/F3/F4 generation-discipline design loop CLOSED both sides (D243). Agreed contracts bump + 3 Forge builds DEFERRED behind ve-supply; nothing built/deployed. [[D243]]

**Operator relayed the closing ACK; Crucible confirmed all three Forge items + fixed LM-P1-6. Six-message round-trip (Crucible handoff → Forge answers+asks → Crucible answers → Forge confirms → Crucible accepts → Forge ACK). Three Forge relays at repo root, all now tracked. Design agreed end-to-end; hygiene not levers (only F2 moves the CPCV-p25 wall).**
- **AGREED, all deferred + operator-gated + behind ve-supply:** ONE coordinated `crucible_contracts` minor bump = 3 additive hash-excluded changes — `failure_buckets: list[str]` on the gated-runs export, `FAILURE_BUCKET_SEVERITY_ORDER` versioned constant, `mechanism`/`regime` free-str on `StrategyConfig` (+ Forge-published closed vocab). Crucible owns the producer half; Forge adoption = pin bump + fixtures + `forge check` + **daemon restart** (= why the bump can't be done now, even though operator OK'd "if needed").
- **Forge forward-obligations (build when ve-supply clears; each flag-OFF + gated):** (1) migrate the two raw-scalar training channels (D114 quality term + wf_p25 lane — both read `gate_results[*].value/.threshold`) to `pass/fail + failure_buckets`, shadow ≥2wk preserving the D231 per-family skill; (2) the `(family × data-era)` freeze ledger keyed on the published metric-era boundary (NOT grammar_version; do NOT reuse `COLD_START_HYPOTHESES`' reset); family-id = C3 realized cluster once it lands; (3) publish the version-stamped mechanism/regime vocab artifact.
- **F3.1 finding STANDS recorded:** two prod learned paths DO ingest raw gate scalars today (cpcv_p25/wf_median in the D114 sampling reward; wf_p25 in the `--quality-rank` lane) — a real but non-urgent honesty deviation, fixed by obligation (1). **Caveats settled:** LM-P1-6 FIXED (`2db9a10`) → §1.4 floor-not-count caveat LIFTED (campaign-scope DSR N exact); Caveat A moot (search_n_trials null, never 0).
- **Relays (repo root, TRACKED, stay live until the bump ships):** `PROMPT_CRUCIBLE_GENERATION_DISCIPLINE_F1_F3{,_CONFIRM,_ACK}.md`. **NEXT: nothing due from Forge** — waits on the operator-ratified contract bump; priority unchanged = the two flip preregs on a clean post-stall cohort, then the teed-up levers (`9063b405` gate-tail, `FORGE_EXPLORATION_HOLDOUT_FRAC`).

## 2026-07-05 — Full codebase-health review (D241): code fully CLEAN; ledgers reconciled; 56-record archive sweep #2; stale "P0 perf pending" pointer corrected (P0 deployed 07-02). Docs-only; daemon untouched. [[D241]]

**Operator: "full codebase review — health, stale docs, previous-audit completion, new gaps." Four read lanes (health / docs / audits / hygiene) + three edit lanes; everything landed is docs-only.**
- **Health: CLEAN.** ruff 0 · mypy-strict 0 (101 files) · full suite **1846/0** · zero TODO/FIXME · hard rules #2/#8 grep-clean · contracts pin 1.23.0 == installed. Every finding was docs/ledger drift, not code.
- **The correction that matters: pipeline-perf P0-1/2/3 were ALREADY DEPLOYED 2026-07-02** (D219; `18a30eb`/`2d601a0`/`bf822c3`; journal proves it: submit 195s→**4.7s**, reconcile 30s→**2.0s**). The "P0 fsync rewrite unblocked/pending" NEXT lines in the D234–D240-era blocks were stale — ignore them; nothing of P0 remains. Remaining perf levers: **P1-1** parse-export-once (weights=17s/iter) and **P2-1** prefetch (141s/iter, now dominant).
- **Landed:** `64f794a` archive sweep #2 (root *.md 72→16; also carried the repo-sync of the already-live `FORGE_BACKUP_KEEP=5` — installed units are symlinks into this tree) · `17b69bb` F1_F3 relay tracked · `5ccda63` docs sweep (D240 propagated into MANPAGE/HOW-TO/investigate-live/architecture/glossary; healthcheck 8→10; SPRT flip-gate; glossary watermark 8d→5d; NEW_BOX pin→pointer; suite count ~1,850; INDICATOR_THRESHOLDS fossils struck; battery.py "seven"→"nine") · `69da7fe` backlog (Q10/18/25/35/36/37/38/42 RESOLVED, Q13/Q20 superseded, Q19 partial; fossil loosen proposal `4b155abd` REJECTED in-file per D206 format) · `fc7f67d` fable-audit 4 tracks reconciled to D195–D240 reality.
- **Operator queue (flagged, not executed):** (1) **72+ commits unpushed to origin** — the repo has no off-box copy; (2) IMPLEMENTATION_DECISIONS.md is past its own 1MB rotation trigger (codebase-quality item 16); (3) skim 2 root relays (`PROMPT_CRUCIBLE_V22_FUNNEL_COMPARE.md`, `PROMPT_CRUCIBLE_OPTIONS_PRIMARY_STANDALONE.md`) — archive or act; (4) `forge grammar reject-proposal --id 4b155abd-67f2-47a7-9d49-9996a70365cd` at the next unlocked-DB window (in-file half done); (5) stray 4.5GB `~/forge_data/forge.db.bak-pre-flush-20260624`. Genuinely-open audit items are now accurate in `fable-audit/*/WORKPLAN.md`.
- **NEXT (unchanged from D240):** re-resolve the two flip preregs on a clean post-stall cohort (~1–2 days), then flip `9063b405` (gate-tail; unit env + daemon-reload) and `FORGE_EXPLORATION_HOLDOUT_FRAC`, one at a time.
- **Addendum 2 (operator: "1. lets rotate, 2. these are outdated") [[D242]]:** ledger ROTATED — D001–D200 (859KB) → `_archive/IMPLEMENTATION_DECISIONS_D001-D200.md`, live ledger now D201+ (~153KB; D236 gap = v23 branch, legit); `check-added-large-files` excludes `_archive/`; the two stale relays (V22_FUNNEL_COMPARE, OPTIONS_PRIMARY_STANDALONE) archived with ARCHIVED-unsent banners (root *.md → 14). Queue now: stray 4.5GB bak only (of the housekeeping items). **Addendum 3: bak DELETED (operator-directed; ~4.3G freed, dailies intact) — housekeeping queue EMPTY.**
- **Addendum (same day, operator: "push to origin. reject the proposal and retire."):** (1) `4b155abd` DB-row half REJECTED via `forge grammar reject-proposal` (live DB, first-attempt lock window; verified `rejected`/aj/23:28:13Z — queue item 4 done). (2) Three probe scripts retired + their path-loaded tests (`signal_correlation_regime_pair_audit.py`, `decorrelation_proxy_alignment.py`, `wf_quality_probe.py`); unit suite 1643 green post-removal, MANPAGE updated (item 5's script half done). (3) Pushed to origin (item 1 done). Remaining from the queue: ledger 1MB rotation, 2-relay skim, stray 4.5GB bak.

## 2026-07-05 — Crucible F1/F3/F4 generation-discipline handoff ANSWERED + CONFIRMED (design agreed; NOTHING built/deployed). Two relay drafts staged, ready to pass.

**Operator relayed `../Crucible/docs/handoffs/FORGE_generation_discipline_F1_F3_2026-07-05.md` (recs F1+F3, hygiene NOT levers — only F2 new-mechanisms+new-data moves the CPCV-p25 wall). Answered its 3 open questions from code/DB, exchanged confirmations with Crucible (`..._ANSWERS_...md`).**
- **F3.1 finding (the real one):** two production-loop learned paths ingest RAW gate scalars from `gate_results[*].value/.threshold` — the D114 joint-quality term in the enumerator sampling reward (`rejection_weights.py:332,443-452`; cpcv_p25 + wf_median, weight 0.25, on by default) and the `--quality-rank` wf_p25 lane label (`ranking/dataset.py:139`). The F3 `P(component)` ranker is already coarse-label-only (COMPLIANT). PBO/DSR ingested nowhere. Not five-alarm (family-level aggregation + D124 era-cut labels) but a genuine deviation from F3's "coarse labels only."
- **F4 settled:** Forge submits `search_n_trials` unset on 100% of 334,989 configs; deterministic enumeration = no within-config search. Crucible confirmed unset→`n_trials=1` (`_runner_gates.py:437`); cross-config breadth charged from runs-table counts, not the per-config field. Crucible caveat B: they under-count N by dropping failed trials (LM-P1-6) → our `alpha_budget` N is a FLOOR.
- **Design agreed (all flag-OFF, operator-gated, sequenced BEHIND ve-supply):** (1) migrate D114 + wf_p25 lane to `pass/fail + failure_buckets` (9-bucket SET is source-of-truth; shadow ≥2wk preserving the D231 per-family steering skill) then Crucible ships a by-construction coarsened training export (must coarsen `gate_results.value/threshold` FIRST — the primary read, `metrics` only a fallback at `rejection_weights.py:192`); (2) Forge owns a `(family × data-era)` freeze ledger keyed on the **metric-era boundary (= our D124 pin `2026-06-09T22:52:57Z`), NOT grammar_version** — self-correction: do NOT reuse `COLD_START_HYPOTHESES`' version-reset; family-id = C3 realized cluster once it lands (interim `hypothesis`, over-freezes conservatively); (3) additive `mechanism`+`regime` on `StrategyConfig` (free-string on wire + Forge-published closed vocab so F2 growth doesn't force contract bumps; hash-excluded, grammar_version template; sign+kill stay Forge-internal).
- **Drafts (untracked, repo root, ready to pass):** `PROMPT_CRUCIBLE_GENERATION_DISCIPLINE_F1_F3.md` (answers+asks) + `PROMPT_CRUCIBLE_GENERATION_DISCIPLINE_F1_F3_CONFIRM.md` (confirmations). **NEXT:** operator passes CONFIRM to Crucible; on their reply the first buildable piece (likely the `failure_buckets` contract bump + bucket-only migration shadow) gets its own D-entry WHEN IT SHIPS. Daemon untouched; nothing deployed.

## 2026-07-05 — INCIDENT + FIX: §7.3 stall cleared (601 phantom failed-runs flushed) + durable failed-run reconciliation DEPLOYED (contracts 1.23.0, D240). Daemon healthy, submitting. [[D240]]

**Operator: "are we ready to enable the next levers?" → ran the two flip-prereg reads; discovered the daemon had been STALLED for ~15h. Operator: "lets do both" (flush now + durable fix).**

**Incident.** Daemon `active` but `blocked: in-flight depth 601 exceeds cap 600` every iteration since ~01:10 UTC; reconciliation frozen. In-flight = 601 phantom `submitted` rows spread ~150/day across 07-01→07-04 (oldest 4.7d). Crucible healthy (inbox drained, exports fresh). ALL 601 in Crucible's `failed_runs` export (634 runner_failure + 52 pool_break) — accepted-then-runner-failed runs that never enter `gated_runs`, so Forge's reconcile join can't match them; they pinned §7.3 depth. **Root cause: Crucible shipped the D205 durable feedback (`failed_runs_*.json`) but neither `crucible_contracts` nor Forge consumed it.**

**(A) Flush — DONE, LIVE.** `scratchpad/flush_failed_inflight.py` retired the 601 (all confirmed in the failed export; 0 genuine-pending) to `gated`+aged-out-sentinel — same marker the D052/D110 flush uses (excluded from depth/H-1/M-7). Ritual: stop → dry-run vs live DB → `--apply` (in-flight 601→0) → restart. **Verified: `submitted=200` batch, then healthy per-batch §7.3 wait. Production flowing again (new daemon PID 2466274).**

**(B) Durable fix — DEPLOYED 2026-07-05 17:05:09 UTC (PID 3010796).** contracts 1.23.0 (`01da53b`; additive; 303 tests/100% cov): `FailedRun` + `load_recent_failed_runs_from_export`. Forge (`651ca6e`): pin→1.23.0; `consumer._flush_failed_runs` wired into `reconcile_all_pending` (retires failed runs every poll, before the aged-out flush). Uncontended gate suite **1846 green**, mypy-strict clean. Verify: active, NRestarts=0, no traceback, `reconciled: batches=1 newly_gated_total=195` (no depth block), `contracts: pin == installed (1.23.0)`, `submission: submitting` (15h-stall WARN cleared), healthcheck **OVERALL=OK**. Resolves `PROMPT_CRUCIBLE_FAILED_RUN_FEEDBACK.md`.

**Prereg reads (the original ask).** **Flip #2 (`5082d332`, exclude_regime_filter): CONFIRMED (supply side)** — ve signal_correlation reject-fraction 58.2%→6.4% (reached denom; 18.8%→1.2% on enumerated), ve survival 5.5%→15.3% (~3×), content redundancy still fires. Book-PBO sub-condition still Crucible-side/pending as always. **Flip #1 (`848a1f67`, cumulative_trading): INSUFFICIENT — read compromised.** Live per-family permutation survival went DOWN (opposite the prereg + D226 shadow-count), but the post-flip cohort is only 11 batches (frozen by the stall) AND confounded by enumeration-mix; the controlled D226 shadow-count said UP. **Not refuted — re-resolve on a clean cohort once prod has run unstalled.** Neither prereg formally resolved yet.

**NEXT:** (1) After a clean post-fix cohort accrues (~1-2 days unstalled), re-resolve both flip preregs — flip #1 needs the controlled read since its live cohort was stall-compromised. (2) THEN flip the next levers one at a time: `9063b405` (gate-tail, gate MET — needs `FORGE_QUALITY_RANK_MODE=gate-tail` on the UNIT file → `daemon-reload`) then `FORGE_EXPLORATION_HOLDOUT_FRAC`. (3) Also unblocked: pipeline-perf P0 fsync rewrite (separate ritual).

## 2026-07-04 — FLIP #2 DEPLOYED: signal_correlation `exclude_regime_filter` is LIVE (D239). Two post-D220 flips live; daemon healthy. [[D239]]

**Operator: "sounds good lets deploy" → deployed `5082d332` (signal_correlation regime-gate exclusion), ~30 min after flip #1.**
- **Deployed 2026-07-04 18:45:42 UTC** (11:45:42 PDT; daemon PID 1800092). Verify: active, NRestarts=0, clean startup (registry_loaded, grammar_version=v22, no traceback), `forge healthcheck` **OVERALL=OK (10 ok, 0 warn, 0 crit)**.
- **The flip:** `prefilter.yaml signal_correlation.exclude_regime_filter: true` (D227: 94% of ve signal_correlation kills were the regime gate co-firing, median Jaccard 0.949 — structural, not content redundancy). Ritual **stopped the daemon FIRST** (it hot-reads prefilter.yaml + now has the D227 code, so a live edit would activate ungated), suite uncontended (1843 green), commit `ca44dcd`, restart.
- **Test prep:** `c543120` decoupled the base-mechanism tests (pinned `_ctx` to `exclude_regime_filter=False`); the flip added `test_shipped_calibration_excludes_regime_filter`.

**RESUME POINTER (post-compaction):** Two flips are LIVE and accruing cohorts. **NEXT:**
1. **Resolve the two flip preregs on their POST-FLIP cohorts** (needs ~1-2 days of decided verdicts): `848a1f671392` (cumulative_trading, deployed 2026-07-04T18:19:53Z — per-family permutation survival ↑, component-rate not down) and `5082d332b26e` (exclude_regime_filter, deployed 2026-07-04T18:45:42Z — per-family signal_correlation reject-fraction ve ~0.20→~0.01, ve survivors ↑, book-PBO ≤0.178 hold). Resolve on data AFTER each deploy timestamp.
2. **Remaining teed-up levers to flip (one at a time):** `9063b405` (gate-tail; flip gate MET — but needs the FORGE_QUALITY_RANK_MODE=gate-tail env on the UNIT file → `daemon-reload` required, D-flip pending) and `FORGE_EXPLORATION_HOLDOUT_FRAC` (set the frac). `e1a43ba8` DROPPED (D235).
3. Also unblocked-for-deploy: pipeline-perf **P0 fsync rewrite** (separate ritual).
Daemon: healthy, on HEAD (all 59 session commits now live). D220 confirmed (D237); hold lifted.

## 2026-07-04 — FLIP #1 DEPLOYED: permutation_test `cumulative_trading` is LIVE (D238). First post-D220 flip; restart also deployed 57 committed flag-OFF session commits. Daemon healthy. [[D238]]

**Operator: "yes lets flip" → deployed `848a1f67` (cumulative_trading) via the full feedback-change/deploy.md ritual.**
- **Deployed 2026-07-04 18:19:53 UTC** (11:19:53 PDT; new daemon PID 1598585). The restart was mandatory (running daemon was on 07-02 01:11 code, lacked the D224 loader) and deployed **all 57 committed session commits** (D221–D237, all flag-OFF/byte-identical) + the flip. Preflight GO (full suite 1842 green, deploy-surface clean, contracts pin 1.22.0).
- **The flip itself:** `prefilter.yaml permutation_test.forward_return_mode: cumulative_trading` (was the buggy single_day default). 4 deploy-state tests updated (assert cumulative_trading / back-compat keyless default / decoupled shadow-null base / true-determinism idempotency rewrite).
- **Verify (deploy.md):** ActiveState=active, NRestarts=0, clean startup (registry_loaded, grammar_version=v22, no traceback/SchemaVersionMismatch); loop iterating + reconciling; `blocked: in-flight depth 726 > cap 600` = normal §7.3 backpressure; `forge healthcheck` **OVERALL=OK (10 ok, 0 warn, 0 crit)** — the prior transient wf_p25-drift WARN cleared.
- **NEXT:** resolve prereg `848a1f671392` on the POST-FLIP cohort (data after 2026-07-04 18:19:53 UTC — NOT the 07-02 registration cut); watch the per-family submission mix + component-rate. Then flip the next levers ONE at a time: `5082d332` (signal_correlation regime-gate), `9063b405` (gate-tail, gate MET), `FORGE_EXPLORATION_HOLDOUT_FRAC`.

## 2026-07-04 — D220 prior-weight prereg CONFIRMED (post-cut component-rate 0.1229 vs ≥0.06, durable) → the HOLD IS LIFTED. Teed-up flip levers ready; gate-tail flip gate now MET. [[D237]]

**Operator: "are we ready for the d220 resolution" → checked the clock: it's actually 2026-07-04T17:09Z (the session had carried a stale 07-02 date from the compaction summary). D220's window (≥07-04T08:02Z) has PASSED — so we resolved it.**
- **D220 CONFIRMED (`b7ecc2d2e96f`):** post-cut component-rate **0.1229** (6732/54756 decided, submitted ≥ cut) vs prereg **≥0.06** / ~0.048 baseline. DURABLE by day (07-02 0.117 / 07-03 0.122 / 07-04 0.128, rising); +62% over the immediate pre-cut 0.0757. Nothing else deployed in the window (all flag-OFF/byte-identical) → attributable to the 0.10→0.50 prior-weight raise. Resolved confirmed.
- **THE D220 HOLD IS LIFTED.** The teed-up flag-OFF levers (all at OFF defaults, prereg'd) are now deployable:
  - **`848a1f67`** cumulative_trading (validated win, D224/D226) — **flip first**.
  - **`5082d332`** signal_correlation regime-gate exclusion (D227).
  - **`9063b405`** gate-tail (D222/D228) — **flip gate now MET** (SPRT promote, logLR +41.66, fresh-PASS 4/3).
  - **`FORGE_EXPLORATION_HOLDOUT_FRAC`** exploration holdout (D232) — operator sets frac.
  - **`5c4ba16f`** D216 ve-supply floor (separate prereg, open).
  - `e1a43ba8` DROPPED (D235).
- **SEQUENCING (operator-owned):** flip ONE at a time, each with its own cohort cut, so each prereg resolves cleanly (deploying several at once re-confounds attribution). Each flip = `prefilter.yaml`/env change → feedback-change ritual + `deploy.md` (stop→suite→commit→restart→verify) — **operator-gated** (I can't restart the live service). Also unblocked: the pipeline-perf **P0 fsync rewrite** (separate ritual, ~11 CPU-h/day).
- **NEXT: operator runs the `848a1f67` deploy first** — I can prepare the exact edit + ritual checklist on request. Daemon healthy (component-rate 0.123, submitting).

## 2026-07-02 — ve `|move|` null (e1a43ba8) DROPPED: Crucible quality read finds NO support + INVERTS the thesis (more-magnitude ve = MORE correlated). Prereg → refuted. Book-PBO baseline corrected 0.107 → 0.178. [[D235]]

**Operator forwarded `FORGE_ve_move_null_selection_quality_RESULT_2026-07-02.md` (Crucible's answer to my quality prompt).** The only thing keeping `e1a43ba8` shelved-not-dropped was: is the |move| cut a quality tightening or a supply throttle?
- **Crucible's read (622 honest single-name ve comps, all classifiable):** (1) the cheap realized-P&L proxy CAN'T isolate the signal-level |move| partition — 100% realize as directional (0/622 reach magnitude 0.5) because a |move|-signal through a directional OTM structure → delta-$ P&L (structure confound; **blocker is proxy dispersion, NOT sample size**). (2) the weak within-band gradient **INVERTS the thesis**: more-magnitude ve (Q5) is MORE correlated (|corr| 0.070→0.118) + loads HIGHER on PC1 (0.066→0.104) = LESS decorrelated; marginal_sharpe only mildly favors Q5. → **no support for "magnitude-selected ve is higher-quality."**
- **DECISION: DROP e1a43ba8.** Refuted on survival (D226 −62%) + quality-unsupported/thesis-inverted (this read) + redundant (flip-1 `848a1f67` +2.2× and the D216 floor already supply ve). Prereg `e1a43ba8ee14` resolved **refuted**. The airtight route (build the exact `config_hash → ve_null_profile` tag for a Crucible re-score) stays available on request but is low-EV. No code change (flag was already OFF/byte-identical).
- **BASELINE CORRECTION (propagate):** the promotable book's CSCV PBO is **0.178** (10-group/45-path confirm), NOT the 0.107 I'd been citing (8-group/28-path est). **Still clears 0.40 / below the 0.50 floor — still promotable.** Corrected in memory.
- **POSTURE: unchanged; daemon untouched.** Teed-up 07-04 levers: `848a1f67` (flip), `5082d332` (flip), `9063b405` (flip, SPRT-gated), `FORGE_EXPLORATION_HOLDOUT_FRAC` (activate). `e1a43ba8` retired.

## 2026-07-02 — Track A (estimand re-aim) contracts gap CLOSED + VERIFIED: `component_contributions` reader in contracts 1.22.0 loads cleanly; export still EMPTY (n=0) → build stays HELD until first promotion.

**Operator: "PROMPT_CRUCIBLE_MARGINAL_CONTRIBUTION_EXPORT.md — Verification completed."** Verified Crucible's side: `crucible_contracts.load_component_contributions_from_export(exports_dir) → {config_hash: ComponentContribution}` (1.22.0, installed) loads the live export cleanly and returns `{}` (cold-start contract, never raises). Its docstring gives the exact usage: **re-aim `compute_hypothesis_component_weights` off it — reward LOW `correlation_to_incumbent` × POSITIVE `marginal_sharpe`**. The prompt was already ✅ RESOLVED (export `component_contributions_*.json` = `a7228f9`, reader = `afbe737`, adopted at D216 cont. 2/3); this confirms it end-to-end.
- **But the export is EMPTY** (`contributions: {}`, n=0, 147 bytes) — no book has promoted yet, so there's no per-component marginal-contribution data to consume. **Track A (P2.2 / D216 Layer-1 estimand re-aim) is now DATA-blocked, NOT contracts-blocked.** Build stays HELD — re-aiming the learned family-weight against `{}` validates nothing (the plan's explicit warning). **Trigger to build = the first promotion populates the export** (then: build flag-OFF, shadow the re-aimed weights vs the component-rate weights ≥2 weeks before proposing a flip). No Forge action needed now.

## 2026-07-02 — Pipeline-perf audit STARTED (learned-systems audit complete): P3-1 (phase_timings `weights` bucket) + P3-3 (compact prefetch log) + P3-4 (single-txn shadow-scores write). All byte-identical, safe under the D220 hold. [[D234]]

**Operator: "continue." Learned audit done (D228–D233) → opened the pipeline-performance tier (the daemon burns ~11 avoidable CPU-h/day, taxing shared Crucible compute).**
- **Started with the two byte-identical observability items** (safe under D220 + D104 — no persistence/submission/cadence change; a reboot just adds/changes a journal field):
  - **P3-1 (`3acc66b`):** the ~28s learned-weight loader stanza between `reconcile` and the battery now has a `weights=` phase_timings bucket (was invisible). The audit's DO-FIRST — makes later fixes journal-verifiable.
  - **P3-3 (`0068f44`):** `feature_cache_prefetch_batch` no longer dumps two full ~124-ticker dicts + the ticker list every iteration — replaced with `max_coverage`/`n_full_coverage`/`below_full` aggregates (~70% journal-volume cut). `data_unavailable` + event name kept verbatim.
  - **P3-4 (`af4b1a7`):** `run_shadow_scoring` (telemetry-only shadow write, never touches submissions) now writes the whole batch in ONE transaction instead of per-row autocommit (~200 fsyncs/batch → 1). Never-raises + rollback posture preserved + tested; output byte-identical.
- **The BIG wins are deferred + deploy-gated:** P0-1/2/3 (the DuckDB `executemany` autocommit/fsync rewrite; ~190s→6s submit, ~11 CPU-h/day) change persistence timing → feedback cadence, so their DEPLOY waits for post-D220 (07-04) to keep the resolved prereg's attribution clean. Each needs the deploy ritual + equivalence tests.
- **POSTURE: byte-identical; daemon untouched behaviorally.** NEXT: more safe-now hygiene (P3-4 single-txn shadow scores, P3-5 rate-limiter conn reuse, P3-2 adaptive blocked sleep) OR hold for the 07-04 window to deploy the P0 perf wins + flip the teed-up levers. Live-stream changes still held until D220 `b7ecc2d2` (≥07-04).

## 2026-07-02 — Learned-audit P5.5: honest out-of-sample R² for the ridge (temporal holdout) — replaces the overfit-optimistic in-sample train_r2. Telemetry only; artifact byte-identical. [[D233]]

**Operator: "continue." Took P5.5 (the plan's cheap honesty win) after the buildable P3/P4 items.**
- **The defect:** `train_robustness_model` reports an IN-SAMPLE `r2` (fit + scored on the same rows) — overfit-optimistic; the daily ridge always looks decent, and the P4.1/D231 lane KEEP call leans on its apparent skill.
- **P5.5 (`a6e40fa`, 3 tests):** new `robustness_oos_r2` — deterministic TEMPORAL holdout (order by `decided_at`, fit on older train split, score newer test split; None below 20 rows). `train-robustness` now prints `oos_r2=` next to `train_r2=`. Kept OUT of the artifact (`train_metrics` is hashed into `model_id`) → artifact **byte-identical** (invariants green).
- **POSTURE: telemetry only; artifact + daemon untouched.** Part B (precision@N/NDCG@N at the submission cutoff) deferred. Remaining P5 (Thompson/UCB — best after the D216 floor settles; elite archive — blocked on the B8 effective-N handoff; dead-code sweep — chore) stays low-priority. The learned-systems audit is now effectively complete (D228–D233). Live-stream changes still held until D220 `b7ecc2d2` (≥07-04).

## 2026-07-02 — Learned-audit P3.3 SHIPPED flag-OFF: exploration holdout (seeded ranking-bypass for unbiased labels). Byte-identical default; completes the buildable learned-systems audit (D228–D232). [[D232]]

**Operator: "continue." Took the deferred P3.3 (B7) as its own careful build.**
- **The defect:** every learned component (F3, wf_p25 lane, estimand) trains on Forge-SELECTED submissions — a direct feedback loop; the diversity floors mitigate but don't correct.
- **P3.3 (`fc9e8b0`, 10 tests):** `FORGE_EXPLORATION_HOLDOUT_FRAC` (default 0.0 → OFF; clamp [0,0.10]). When >0, `rank_batch_with_holdout` rank-selects the top `n−holdout_n` then draws `holdout_n` at RANDOM from the survivors ranking did NOT pick (seeded via `SeedHierarchy`, rule #8); total submitted stays ≤ n (replaces slots, no oversubscription). `submissions.selection_mode` ('ranked'|'holdout') tags the draw so evals can split biased-vs-unbiased. `rank_batch` untouched; scoring shared.
- **Byte-identical when OFF:** frac 0 → `holdout_n==0` → plain `rank_batch(n=batch_size)`, all rows 'ranked'; submitted stream + config_hashes unchanged (only forge.db gains the column). **124 invariants green**; dry-run integration test confirms the live branch.
- **POSTURE: flag-OFF, NOT activated; daemon untouched.** Activation = a submission-mix change → operator-gated + D220 hold (≥07-04). **Completes the buildable learned-systems audit (P3.1/P3.2/P3.3/P4.1 = D228–D232).** Follow-ups: wire the eval selected-vs-holdout split (once holdout rows accrue); alpha-budget charging; P5 (Thompson/UCB, elite archive — mostly blocked). REMAINING teed-up flag-OFF levers for 07-04: `848a1f67` (cumulative_trading, validated win), `5082d332` (signal_correlation regime-gate), `9063b405` (gate-tail, now SPRT-gated), `FORGE_EXPLORATION_HOLDOUT_FRAC`; shelved: `e1a43ba8` (ve |move|, refuted).

## 2026-07-02 — Learned-audit P4.1: wf_p25 lane per-family skill probe → KEEP. The marginal POOLED signal is the mean_reversion drag; the lane has real skill on ve (+0.080) + trend (+0.143). Retire-or-keep rule set. [[D231]]

**Operator: "continue to the next thing." Pivoted P3.3 → P4.1 (P3.3 = a core-submit-path + schema change, bigger/byte-identical-sensitive → dedicated increment; P4.1 = higher-value, lower-risk telemetry using the D229 paired infra + targeting the ve goal).**
- **P4.1 probe (`e5ffc27`, telemetry):** `evaluate_tail_shadow_by_hypothesis` splits the paired Δ Spearman (tail − incumbent, same rows) by family; `eval-robustness` prints it ve-first. Live (honest era):
  - **volatility_event n=55 Δ=+0.080 PASS** · **trend_continuation n=4702 Δ=+0.143 PASS** · mean_reversion n=3276 Δ=−0.062 FAIL.
- **Finding:** the lane's weak POOLED skill is an aggregation artifact — mr (dominant row count) drags the pool to ~0, MASKING real skill on the families the lane serves (ve = promotable book; trend = decorrelating). The lane down-ranks the mr monoculture we WANT less of. Inverts the "only not-harmful" read → the lane is actively helpful where it matters.
- **Retire-or-keep RULE (set):** judge on PER-FAMILY Δ (ve OR trend clearing +0.05 on fresh windows via the D228/D229 SPRT), NOT pooled skill; RETIRE only if BOTH ve+trend lose skill. mr/pooled FAIL are NOT triggers. **Current verdict: KEEP.**
- **POSTURE: telemetry + a decision rule; daemon untouched.** Completes the buildable learned P3/P4 items (D228/D229/D230/D231). **REMAINING:** P3.3 (exploration holdout — deferred, dedicated increment) + P5 (mostly blocked). Hold live-stream changes until D220 `b7ecc2d2` (≥07-04).

## 2026-07-02 — Learned-audit P3.2 SHIPPED: feature/score-drift (PSI) + model-adoption gating + hypothesis-weights fallback WARN. Blind newest-wins rotation now has a drift signal + an ADOPT/BLOCK guard. Telemetry only. [[D230]]

**Operator: "continue with follow-up and P3.2." Did both — §8.6 follow-up (D229) + P3.2 (this).**
- **The gap:** the daemon adopts the newest daily-trained model by mtime with no guard — a bad retrain silently goes live, and no signal flags input-distribution drift.
- **P3.2 (`156e523`, 7+ tests):** new pure `forge/ranking/drift.py` — `population_stability_index` (quantile PSI) + `psi_severity` (0.1/0.25 bands) + `adoption_verdict` (BLOCK a non-positive fresh signal). Wired as telemetry: **healthcheck** gains `check_hypothesis_weights_fallback` (WARN when the §6.2 sampler degraded to UNIFORM — feedback loop muted) AND its `wf_p25 drift` check now reads the PAIRED `spearman_delta` (the real adoption signal); **`forge status`** gains an `adoption guard` line (F3=ADOPT +0.440 / wf_p25 per lane); **`eval`** gains a score-distribution PSI line (window vs honest-era baseline). Verified live.
- **POSTURE: telemetry only; daemon untouched.** Surfaces the signals; the ACTUAL adoption block (daemon refusing a BLOCK artifact) is an operator-gated production change, deferred. **RESUME:** P3.3 (randomized exploration holdout — operator-gated, flag-OFF) or **P4.1** (wf_p25 lane retire-or-keep — now that its skill gate (D229) + drift guard (D230) both read the paired signal, the retire-or-keep call is well-instrumented). Hold live-stream changes until D220 `b7ecc2d2` (≥07-04).

## 2026-07-02 — Learned-audit P3.1 COMPLETE: §8.6 tail streak now a PAIRED SPRT too (tail Spearman − incumbent Spearman, same rows). Both promotion gates are controlled-α significance tests. Telemetry only. [[D229]]

**Operator: "continue with follow-up and P3.2." Did the §8.6 follow-up (this); P3.2 next.**
- **The defect:** the §8.6 tail clock PASSed on an ABSOLUTE Spearman ≥0.30 — rewards a model for tracking a signal the incumbent P(component) already ranks, not the marginal skill P4.1 needs to judge the lane.
- **P3.1 follow-up (`463ed04`, 360 tests):** `TailEvaluation` gains `incumbent_spearman` + `spearman_delta` (tail − incumbent, same rows; the triples already had `composite_score`). New `shadow_tail_verdict` (paired). `_sprt_flip_gate` generalizes the D228 gate; `rewire_flip_gate` + new `tail_flip_gate` both delegate; `forge status` prints both gate lines. Daily timer + `eval-robustness` report the paired delta. Legacy rows (no `spearman_delta`) are skipped → gate stays NOT MET, no fabricated PASS (verified live).
- **POSTURE: telemetry only; daemon untouched.** Completes P3.1 — both streak gates (rewire + §8.6) are controlled-α paired SPRTs; the ad-hoc absolute/coin-flip criteria are retired. **NOW: P3.2** (feature-drift PSI/JS per checkpoint + adoption gating: refuse to rotate to a model with negative fresh-window IC; surface the hypothesis-weights uniform-fallback as a healthcheck WARN). Then P3.3 / P4.1. Hold live-stream changes until D220 `b7ecc2d2` (≥07-04).

## 2026-07-02 — Learned-audit P3.1 SHIPPED: SPRT sequential test replaces the 12.5%-FP "3 consecutive PASS" flip gate. The gate-tail flip is now judged by a controlled-α significance test. Telemetry only; daemon untouched. [[D228]]

**Operator: "continue to the next tier" → learned-systems P3 (promotion & feedback discipline; P0/P1 done = D220–D223). P3.1/B5: the flip gates were statistically ad-hoc.**
- **The defect:** the gate-tail flip gate (D223) needed "k≥3 fresh PASSes AND a fixed-sample 95% CI excluding 0." "3 consecutive" = **0.5³ = 12.5% false-promote** under a coin-flip null; and peeking a fixed-sample CI every day inflates Type-I (optional stopping). No explicit α, no effect size.
- **P3.1 (`e66a74b`, 10+ tests):** new pure `forge/ranking/sequential_test.py` — a **Wald SPRT** on the per-checkpoint PAIRED deltas (challenger − incumbent). H0 mean=0 vs H1 mean=min_effect; log-LR crosses the Wald boundaries (α=0.05, β=0.20) → promote/reject/continue, controlling false-promote under daily peeking. `rewire_flip_gate` now gates on it (`met` == "promote") over the data-sufficient (`qualifies`) fresh deltas; `forge status` prints the SPRT verdict; `_REWIRE_DELTA_CRITERION` demoted to display-only. Deterministic (rules #5/#6).
- **POSTURE: telemetry only; daemon submission path untouched; nothing flipped.** The gate-tail flip (`9063b405`) is now judged by a controlled-α test instead of the 12.5%-FP streak. **FOLLOW-UP (P3.1 rest):** convert the §8.6 tail streak (absolute Spearman ≥0.30) to the paired SPRT — needs incumbent-Spearman-on-same-rows in `eval-robustness`; `_TAIL_SPEARMAN_CRITERION` stays PROVISIONAL until then. Then P3.2 (feature-drift + adoption gating) / P3.3 (exploration holdout, operator-gated) / P4.1 (wf_p25 lane fate). Still hold live-stream changes until D220 `b7ecc2d2` resolves (≥07-04).

## 2026-07-02 — Strategy-audit P1-2b SHIPPED flag-OFF: signal_correlation excludes the regime_filter GATE — un-throttles event families. Measured: 94% of ve kills are the gate co-firing structurally, not redundancy. Byte-identical; flip pre-registered. [[D227]]

**Operator chose "un-throttle event families." P1-2(b): signal_correlation kills 20% of multi-signal ve configs (vs 2-6% elsewhere). Measured the mechanism on live data FIRST (PRE-H3's "measure before changing"), and it's a category error, not a threshold miss.**
- **Measurement (live, no code):** 293k live signal_correlation rejects → 55% Jaccard ≥0.95, **88% involve `sig_regime`**. Live-cache verify (N=1500, `scripts/signal_correlation_regime_pair_audit.py`): ve **65/320 rejected (20%), 61 (94%) are regime-pairs** (median Jaccard 0.949) on **event-calendar gates** (days_to_nfp/cpi/fomc/opex). Genuine content-pair redundancy is rare (9) + marginal (0.869).
- **Root cause:** `sig_regime` has role `regime_filter` — a CONTEXT GATE, not an alpha signal. The gate co-firing with the signals it gates is STRUCTURAL (a non-binding gate), not the "two edges = one" redundancy the filter exists to catch. Penalizing it is a category error.
- **P1-2b (`902e04a`, 4 tests):** new flag `signal_correlation.exclude_regime_filter` (default **False** → byte-identical). When True, only alpha-bearing signals (non-regime_filter) are compared → gate↔content co-firing no longer rejects, content↔content still does. **Role-aware, NOT family-aware** — the mechanism spans all families (relval 100%, trend 100%, mr 72% regime-pair), so excluding the gate is narrower + more principled than a ve threshold (refines the workplan's (b), logged as D227). Invariants + 396 tests green; daemon untouched.
- **Prereg `5082d332b26e`:** post-flip ve signal_correlation reject fraction falls ~0.20 → content-only ~0.01-0.02, ve survivors rise, no book-PBO harm; keep if confirmed else revert. **Flip = feedback-change, operator-gated, after D220.** Independent of the D224/D225 permutation flips (different filter).
- **POSTURE: flag-OFF, NOT flipped; daemon untouched.** **Completes strategy-audit P1-2:** (a)=D225 ve |move| [REFUTED by the D226 shadow-count], (b)=this [VALIDATED by measurement, ready to flip], (c)=predicted_activations [control, no change]. **RESUME:** P1-3/P1-4 are grammar bumps (operator-gated) — or the learned-systems tier (principled P blend). Still hold live-stream changes until D220 `b7ecc2d2` resolves (≥07-04).

## 2026-07-02 — SHADOW-COUNT RESULT (live N=2000): flip-1 (cumulative_trading) is a WIN (ve 2.2×, rebalances off mr); flip-2 (ve |move|) is REFUTED (ve survival −62%). Recommendation INVERTS: flip 848a1f67 after D220, SHELVE e1a43ba8. [[D226]]

**Operator: "move forward with the ve-null shadow-count so we can prepare for flip on 07-04." Built the tri-arm harness, ran it live, and it did exactly its job — it split the two bundled flips apart and produced a decisive, plan-inverting read.**
- **Harness (`392c029` + tri-arm `fed9575` + socket-resilience `0565d45`, 12 tests):** `forge shadow-null run` scores permutation_test under THREE nulls over the LIVE cache — A=production (single_day signed), B=cumulative-signed (flip-1), C=cumulative+ve|move| (flip-1+2) — and prints two survival-delta tables: FLIP-1 = B vs A (all families), FLIP-2 = C vs B (ve-only, |move| is family-scoped). Shared cache + rng → only the null moves a verdict; daemon byte-identical (invariants green). Hardened to skip-and-continue on a shared-socket broken pipe (the first N=3000 run died on one).
- **LIVE RESULT (N=2000, seed 0, 1444 reached, 0 socket-skips):**
  - **FLIP-1 `cumulative_trading` (848a1f67) — WIN:** event_momentum 9→40 (+31, 4.4×), trend 98→128 (+30), **ve 25→55 (+30, 2.2×)**, mr 118→85 (−33), relval 14→0 (−14); TOTAL +44. Confirms the prereg for trend/leading families AND lifts ve 2.2× on its own (delivers the D216 single-name-ve supply goal) while pruning the mr-monoculture → **rebalances toward decorrelated supply. → flip after D220.**
  - **FLIP-2 ve `|move|` (e1a43ba8) — REFUTED:** ve 55→21 (**−62%**, 16 gained/50 lost); all other families net 0. The prereg predicted survival RISES; it FALLS. The |move| null is a STRICTER bar (all-positive pool, high mean), not looser — the D225 "signed wrongly rejects ve" premise is empirically wrong at the 5-day horizon. (Quality-vs-quantity caveat: |move| may select genuinely magnitude-driven ve over directional-drift ve, a tightening the survival count can't judge.) **→ HOLD; do NOT flip on the current prereg.**
- **POSTURE: telemetry only; nothing flipped; daemon untouched.** **NEXT (≥07-04, after D220 `b7ecc2d2` resolves + hold lifts):** flip **848a1f67 alone** (feedback-change ritual: operator + deploy window + prereg confirm on a later cohort; watch component-rate — the family mix shifts). **SHELVE e1a43ba8** pending a Crucible-side quality read — **prompt drafted `PROMPT_CRUCIBLE_VE_MOVE_NULL_SELECTION_QUALITY.md`** (operator to review/send): do |move|-selected (magnitude-driven) ve configs contribute more to the PBO-0.107 long-vol book — higher `marginal_sharpe` / lower `correlation_to_incumbent` / PBO holds — than directional-selected ones? Win → un-shelve + re-prereg on book-contribution (not survival); no diff → drop e1a43ba8. Zero Forge deploy either way. Gate-tail (`9063b405`) still separate, waits for flip gate 3/3. Flip-condition cron `057aadf1` + `forge status` flip-gate line stand.

## 2026-07-02 — Strategy-audit P1-2 SHIPPED: null-correction SHADOW-COUNT harness (`forge shadow-null run`) — measures the D224/D225 flips on live data before flipping. Daemon byte-identical; nothing flipped. [[D226]]

**Operator: "move forward with the ve-null shadow-count so we can prepare for flip on 07-04." Built the sanctioned "shadow-count first" harness — the piece that lets us MEASURE what preregs `848a1f67` (cumulative_trading) / `e1a43ba8` (ve |move|) predict, before the D220 hold lifts and we flip.**
- **P1-2 harness (`392c029`, 11 tests):** `forge/prefilters/shadow_null.py` (pure aggregator) + `forge/cli/shadow_null_cmd.py` (`forge shadow-null run`). Runs the §5.2 battery under the **production** null over the LIVE cache, then re-scores ONLY permutation_test under the **corrected** null (`corrected_null_calibration` flips just the two knobs) on the SAME configs — sharing the feature cache (reuses the battery prefetch) and the `rng_factory` (identical shuffles, so the ONLY thing that moves a verdict is the null construction). Emits a per-family survival table (`reached`/`prodP`/`corrP`/`gained`/`lost`/`net`) + one JSONL record. **Submits nothing; never writes `prefilter.yaml`; daemon untouched** (main.py = +2 lines import/add_typer; invariants green).
- **Why one-shot not a daemon hook:** "tree IS production" (D104) — a separate process sharing Crucible's writer socket (read-only; never `forge.db`/inbox) gets the live-cache "daemon shadow-count" intent with the daemon byte-identical. Fixed-seed enum + empty priors (the `cmd_prefilter` pattern): the per-family **delta** is a filter property (robust to sampling); absolute rates are diagnostic (harness prints the caveat).
- **POSTURE: telemetry only; nothing flipped.** **NEXT (this session): run `forge shadow-null run -n 3000` against the live cache** to produce the actual ve/all per-family survival delta (the baseline numbers the flip decision needs). **THEN (≥07-04, after D220 `b7ecc2d2` resolves + the hold lifts):** flip `848a1f67` then `e1a43ba8` one at a time, each prereg-confirmed on a later cohort; gate-tail (`9063b405`) also waits for the flip gate 3/3. Flip-condition cron `057aadf1` + `forge status` flip-gate line stand.

## 2026-07-02 — Strategy-audit P1-2a SHIPPED flag-OFF: vol-appropriate |move| permutation null for volatility_event (un-throttles the validated family); byte-identical default, flip pre-registered. [[D225]]

**Operator: "keep going." Continued the strategy-methodology P1 "un-throttle the validated family" chain → P1-2a (the vol-appropriate null).** vol_event profits from move MAGNITUDE (long-straddle payoff), not signed drift — so even the D224-corrected SIGNED forward-return test wrongly rejects ve configs whose net drift is ~0. This throttles the exact PBO-orthogonal family (single-name ve) that D216/06-29 validated.
- **P1-2a (`db309b0`, 5 tests):** new `permutation_test.volatility_event_absolute_move` flag (default **False** → byte-identical). When True + `cumulative_trading` + `config.hypothesis == volatility_event`, ve is tested on **|cumulative forward move|** (real AND null); **family-scoped** — every other family stays signed. Crux test: a ve signal firing before big ALTERNATING-sign moves is rejected by the signed test (cancels to ~0) but passes on |move|. `absolute` kwarg on the D224 helpers; default False → byte-for-byte identical (invariants + 380 prefilter tests green).
- **Prereg `e1a43ba8ee14`** (post-flip ve survival rises + more decorrelated ve supply, without lowering component-rate / degrading book-PBO; keep if confirmed else revert). Pairs with the cumulative_trading flip (`848a1f671392`). **Flip = feedback-change ritual, operator-gated, after D220.** The shadow-count the plan wants (how many ve configs newly survive) needs live feature-cache data (daemon shadow pass / Crucible replay) — the mechanism is built so that measurement + flip are unblocked.
- **POSTURE: flag-OFF, NOT flipped; daemon untouched.** **RESUME PLAN (post-compaction, operator-directed "move on to next items"): build the ve-null SHADOW-COUNT harness (strategy P1-2 sanctioned "shadow-count first").** A dual-evaluation telemetry pass: run `permutation_test` under BOTH the current signed/single-day null AND the corrected nulls (`cumulative_trading` + ve `|move|`) over the SAME live feature-cache data, log per-family survival delta — NO submission change. Needs the live feature cache (Crucible writer socket) → runs in a daemon pass / one-shot wired to the cache, NOT a `/tmp` snapshot (the activation+return data isn't in forge.db). This MEASURES what the P1-1/P1-2a preregs (`848a1f67`/`e1a43ba8`) predict, de-risking the flips + giving baseline numbers. THEN (07-04, after D220 `b7ecc2d2` resolves + the hold lifts) flip the validated levers one at a time, each prereg'd; gate-tail (`9063b405`) also waits for the flip gate 3/3. Alternatives if shadow-count is deferred: remaining P1-2 (family-aware signal_correlation event-pair exemption; predicted_activations control), or analyze the per-config RNG seed for pipeline-perf P2-3 memoization (now unblocked by the P1-1 semantics fix; NOT trivially byte-identical). Flip-condition cron `057aadf1` + `forge status` flip-gate line stand.

## 2026-07-02 — Strategy-audit P1-1 SHIPPED flag-OFF: permutation_test `cumulative_trading` mode fixes the dominant filter's two forward-return bugs; byte-identical default, flip pre-registered + operator-gated. [[D224]]

**Operator: "continue to the next tier." Picked strategy-methodology P1 (the core producer goal: un-throttle the validated family) over pipeline-perf — but built the FIRST item flag-OFF (byte-identical) so it's safe under the D220 hold + tree-is-production (D104).** permutation_test rejects ~51.4% of all enumerated configs and had **two real bugs**: (a) reads the SINGLE calendar day at T+horizon, not the cumulative T+1..T+k holding-window return; (b) shifts by CALENDAR days, so Fri activations lose ~40% of their sample to weekends.
- **P1-1 (`639cd8b`, 7 tests):** new `permutation_test.forward_return_mode` calibration knob — `single_day` (**default = legacy**, byte-identical when absent from `prefilter.yaml`) | `cumulative_trading` (cumulative over the next `horizon` TRADING days via the returns index, **null built on the same statistic** so it's not scale-mismatched). Optional in the loader → tree byte-identical (invariants + 376 prefilter tests green). Reuses the window returns (one fewer feature_cache call); RNG only in the permutation loop → legacy branch byte-for-byte identical.
- **Prereg `848a1f671392`** (the flip prediction: post-flip trend/leading-family survival rises + the weekday bias vanishes, without lowering component-rate; keep if confirmed else revert). **Flip = feedback-change ritual** (operator + deploy window + later-cohort confirm), sequenced AFTER D220 resolves. Coordinates with pipeline-perf P2-3 (memoization) — land THIS semantics fix first.
- **POSTURE: flag-OFF, NOT flipped; daemon untouched.** Why this tier over pipeline-perf P1 now: strategy-methodology is the operator's core producer goal, and building it flag-OFF is clean under the D220 hold; pipeline-perf P1-1 (parse-once) shifts warm-path weights → NOT byte-identical → deferred. **RESUME:** strategy-methodology P1-2..P1-5 (vol_event battery A/B + grammar bumps — operator/grammar-gated, build-shadow-first) OR pipeline-perf byte-identical items; the 3 flip conditions have a daily cron reminder (`057aadf1`) + `forge status` flip-gate line.

## 2026-07-02 — Learned-systems P1.2 SHIPPED: numeric gate-tail flip gate + clean rewire streak; flip prediction pre-registered. Recommendation on "flip now?": **NO** (gate 1/3, D220 prereg open). [[D223]]

**Operator: "continue... should we flip it now?" → answered NO + built P1.2 (the flip machinery).** The rewire streak's first record was a full-pool "look" (not a fresh per-checkpoint window); "3 consecutive" alone is a weak bar (12.5% null false-flip).
- **P1.2 (`c0796e2`, 6 tests):** (1) the daily timer marks the no-prior full-pool window `qualifies:false`/`is_first_look:true` — never counts. (2) `rewire_flip_gate()` (pure/tested) excludes full-pool looks + encodes the numeric gate: **k≥3 fresh PASSes AND pooled fresh-window Δ 95% CI excluding 0** (both). Surfaced in `forge status` (`gate-tail flip gate MET/NOT MET`). (3) **Prereg `9063b405750c`** — the pre-flip commitment (Δ≥0.05 on the POST-flip cohort, resolved only if the gate was MET at flip; keep gate-tail if confirmed else revert byte-identical).
- **FLIP DECISION — NO, not now** (evidence-based, not caution): (a) flip gate **NOT MET** — fresh-PASS **1/3**, n=1, no CI yet (the contaminated 06-30 full-pool FAIL now excluded, leaving the honest 07-01 PASS); (b) the **D220 prior-weight prereg is still open** (≥07-04T08Z) — stacking a second big ranking change confounds both attributions; (c) the gate reads **raw un-recalibrated P** (P1.3 max_ce 0.356) where floor 0.02 barely bites (eligible_fraction 0.9869); (d) the edge is real but recency-dependent (Δ+0.180 recent, full-pop d −0.074). **Flip when: the gate is MET on clean fresh windows AND D220 resolves** (then fold in the P1.3 recalibrator + floor re-derivation at the same deploy).
- **POSTURE:** telemetry only; daemon untouched; nothing deployed. **RESUME:** let the flip gate accrue (daily timer); next audit work = remaining learned-P (blend two-part form, streak B5 already partly addressed) or the P2/strategy tiers. Hold submitted-stream ranking changes until D220 resolves.

## 2026-07-02 — Learned-systems P1.1 SHIPPED: gate-tail mode hard-gates at queue level, matching the validated shadow (flag-OFF byte-identical, daemon untouched). [[D222]]

**Operator: "continue to the next step" → P1.1 (the gate-then-tail shadow↔production fidelity gap).** The `rewire_streak` shadow validates the HARD-gate form (`gate_tail_rank_score`, Δ +0.180 CI [+0.060,+0.309]) but production wired `gate_tail_prior` into the 0.50 composite SLOT — a SOFT gate where strong hygiene floats a below-floor config above an eligible one. Flipping gate-tail mode would ship an unmeasured intervention.
- **Fix (`d159b2d`, option A):** `rank_batch` gains `gate_tail_ordering`; in gate-tail mode **the composite IS the `gate_tail_prior` value** (§6.2 hygiene blend BYPASSED). Ineligibles pin to **0.0 — a fixed point of the diversifier's `score·(1-penalty)` multiply**, so they can never outrank an eligible config (hard gate survives §6.3 diversification, without the shadow's out-of-[0,1] −1e9). Eligibles order by `tail_norm` (monotone in the tail pred the shadow ranks by) → **identical order**. Shadow & production share the same P model + tail model + floor (`FORGE_REWIRE_P_FLOOR` 0.02; the timer reads the same env) → parity by construction. **Parity test** proves production order == shadow `gate_tail_rank_score` order; **byte-identical** flag-OFF (invariants + 396 tests green).
- **POSTURE: gate-tail mode is NOT live** (`FORGE_QUALITY_RANK_MODE` unset → blend, byte-identical). No deploy; daemon untouched. This makes gate-tail *safe to flip* if/when the operator chooses (gated on P1.2's clean streak).
- **REMAINING (P1.1×P1.3 intersection, deferred to the flip):** persist the P1.3 held-out `(a,b)` recalibrator + re-derive `FORGE_REWIRE_P_FLOOR` on the calibrated scale + switch shadow+production to calibrated P together — a coupled change best done atomically at the operator flip (doing it now = artifact/determinism churn for a non-live consumption). **RESUME:** P1.2 (restart the rewire streak clean + define the flip gate numerically) then P2/remaining audit tiers. Still hold any submitted-stream ranking change until the D220 prereg resolves (≥2026-07-04T08Z).

## 2026-07-02 — Learned-systems P1.3 SHIPPED: calibrate P(component) — floor-relevant ECE criterion + eligible-fraction monitor (telemetry only, daemon untouched). [[D221]]

**Operator: "keep momentum with the next stage." P1.3/B3 = the calibration defect (audit §1: F3 P over-predicts ~3-5× above p≈0.3, drifts across artifacts). Shipped as MEASUREMENT + a co-primary criterion + a keep-rate monitor, WITHOUT touching the live score path (recalibrating the P in the §6.2 sum would confound the open D220 prereg; the base fit stays byte-identical).**
- **4 commits, 24 tests:** (1) `forge/ranking/calibration.py` (`20603f0`) — pure/deterministic reliability_table + ECE + Murphy brier-decomp + a dependency-free Platt `(a,b)` recalibrator (reuses the verdict-model IRLS). (2) `evaluate_shadow` (`cc844c2`) emits `model_ece` (overall — toothless here, 0.018), **`model_max_ce`** (the floor-relevant max gap over populated bins — live **0.356 → FAIL**), `model_ece_platt` (held-out Platt = the reachable floor, **0.006** → miscalibration is RECOVERABLE); centralized `shadow_auc_verdict` + new `shadow_calibration_verdict`. (3) `RewireEvaluation.eligible_fraction` (`d255e4e`) — the gate-tail floor KEEP-RATE (live **0.9869** at floor 0.02). (4) `forge status` (`2ec6d51`) — a `P calibration/floor` drift line. `eval` + `eval-rewire` + the daily timer print + JSONL-track all of it.
- **Why max_ce not overall ECE:** the P mass sits in well-calibrated low bins (median ~0.0004) so overall ECE is ~0 even while the high-P sliver the floor SELECTS is 3-5× off; max_ce over ≥20-row bins captures exactly the miscalibration that bites. **Why two verdicts:** AUC blesses RANKING (blend); calibration blesses the ABSOLUTE floor read — kept separate so a well-ranking-but-miscalibrated model isn't wrongly failed. **No live behavior change** (telemetry; daemon untouched; base fit byte-identical, hard rule #6).
- **Deferred to P1.1 (operator-gated at flip):** persist the `(a,b)` in the artifact + re-derive the `FORGE_REWIRE_P_FLOOR` on the calibrated scale + wire calibrated-P into the gate-tail lane. The recalibrator's application is inseparable from the floor re-derivation, so it lands there.
- **RESUME PLAN:** **P1.1** (Task #14) — close the gate-then-tail shadow-vs-production fidelity gap (make production hard-gate below-floor before the composite under gate-tail mode; flag-OFF byte-identical; parity test) AND fold in the P1.3 recalibrator (persist `(a,b)` + re-derive the floor). Ungated to build; still don't deploy a ranking change that moves the submitted component-rate until the D220 prereg resolves (≥2026-07-04T08Z). Then P1.2/P2 per the audit.

## 2026-07-02 — Prereg `b7ecc2d2e96f` EARLY READ (D220 prior-weight raise): +8.5h post-cut, realized component-rate **0.117 vs ~0.078 recent baseline** — prediction (≥0.06) holds decisively. Kitagawa split: **72% of the lift is within-family ranker skill** (the intended P effect, robust to the trend↔mr oscillation); vol_event floor ruled out as confounder. **Keep 0.50 live; hold prereg open for ≥48h formal resolution.** Next: P1.3 (calibrate P(component)).

**Prereg check (fresh `/tmp` snapshot, cut 08:02:09Z, now 16:38Z ≈ 8.5h post-cut; 7,314 of 7,400 post-cut submissions decided — near-complete, not decision-lag-censored).** Component-rate (`decision IN component,promote`) by `submitted_at` (attributes to the live ranker weight): **post-cut 0.1174 vs pre-cut(all) 0.0777**; recent trailing windows −24h..cut **0.078**, −48..−24h 0.056, −72..−48h 0.064, −7d..−72h 0.090 → the recent baseline is ~0.06–0.09 and post-cut clears every one.
- **Confounder controls (the decisive part):** (1) **vol_event floor ruled OUT** — its component-rate is ~zero (0.0046→0.0019) and share barely moved (0.061→0.072), so D216 is NOT driving the lift. (2) **Kitagawa decomposition of the +0.0391 aggregate lift: rate effect (within-family ranker skill) +0.0283 (72%), mix effect (composition/oscillation) +0.0109 (28%).** Within-family rates rose in BOTH cores (trend 0.121→0.164, mr 0.045→0.060) — not explainable by the trend↔mr mix oscillation → clean evidence the higher F3-prior weight is selecting better configs. **Even discounting the entire 28% mix effect as the oscillation confounder, the residual rate lift alone → ~0.106, still » the ≥0.06 prediction.**
- **VERDICT: strong early confirmation; prereg HELD OPEN (not resolved) for ≥48h** to confirm across a full trend↔mr cycle before formal resolution + the keep-vs-revert call. 0.50 stays live (current state, evidence supports it; revert only triggers on failure). Re-run this decomposition on a fresh snapshot ≥2026-07-04T08Z. Probe: `scratchpad/` (gitignored; method recorded here).

## 2026-07-02 — Learned-systems P1 started: the DEPLOYED ranker composite ranks components BELOW random (prior-weight A/B, D220). `eval-prior-weight` shipped; **operator approved → `ranker.yaml` prior raised 0.10→0.50 (prereg `b7ecc2d2e96f`), DEPLOYED 2026-07-02T08:11Z (verified).** [[D220]]

**Operator: "start on the next tier you feel is best" → learned-systems P1 (the live ranking path — the audit's highest leverage).** P1.4/B2 (`cbdb232`): built `evaluate_prior_weight_ab` + `forge ranker-model eval-prior-weight` (offline A/B re-scoring submitted shadow rows under alternate composite prior weights, holding the hygiene terms' relative proportions). **Finding (live snapshot, n=131,187 decided / 6,326 components):** the deployed §6.2 composite (learned F3 prior at weight **0.10**) ranks realized components at precision@K **0.032 / AUC 0.488 — BELOW the 4.8% base rate**; the four hygiene terms (signal_density/novelty/regime_diversity/permutation_test = 0.90 of the weight) are ~coin-flip-to-anti vs realized promotion and DROWN the good F3 prior (pure-P AUC **0.838**). Corroborates the June-review B2 at scale. **Recommend raising `prior_promotion_proximity` 0.10 → ~0.50** (AUC→0.809; the diversifier D103/D136 floors protect family variety independently, post-ranking). **GATED:** censored within-submitted-set → confirm on a live shadow lane + prereg + deploy (ranking-policy change). No ranker behavior change shipped — the eval is telemetry.
- **APPLIED (operator: "raise to 0.50 now"):** `ranker.yaml` `prior_promotion_proximity` 0.10→**0.50** (hygiene renormalized 0.17/0.14/0.11/0.08; sum 1.0, loads+validates). Prereg **`b7ecc2d2e96f`** (cohort_cut 08:02:09Z; predicted post-cut component-rate ≥0.06 vs the ~0.048 pre-cut baseline; keep if it rises, else revert). Deployed via `docs/tasks/deploy.md` (restart #3, 08:11Z): **verified NRestarts=0, grammar v22, registry loaded, no traceback, D216 floor preserved.** REVERT = restore the 0.30/0.25/0.20/0.15/0.10 set + restart. The later-cohort component-rate is the real test (the offline A/B is censored within the submitted set); resolve prereg `b7ecc2d2e96f` on the post-08:02Z cohort.
- **RESUME PLAN (post-compaction, operator-directed):** (1) **check/resolve prereg `b7ecc2d2e96f`** — the D220 prior-weight raise's post-08:02Z component-rate vs the ~0.048 baseline (needs days of post-cut submissions to accrue; re-run `forge ranker-model eval-prior-weight` on a fresh `/tmp` snapshot + `forge prereg` when data exists). (2) **keep momentum with P1.3** (calibrate P(component) — 3–5× over-predicted, load-bearing for the gate-tail floor + F3 eligibility; ranking-invariant → won't confound the P1.4 prereg), then **P1.1** (gate-then-tail shadow-vs-production fidelity gap). Both ungated to build. Tasks #13 (P1.3) / #14 (P1.1) hold the detail.

---

## 2026-07-02 — Fable-audit P0 execution + DEPLOY: ungated learned-systems/strategy hardening (env-guard, floor call-site tests, eval-robustness label, D217 record, per-family battery-survival telemetry) → restarted `forge.service` 2026-07-02T06:56Z (operator-requested). [[D217]]

**Operator: "start fixing everything, follow the plan, update all docs" + "restart the service after all this." Working the `fable-audit/` P0 items that are ungated (build/test/doc); the operator landed the tree + activated D216 in parallel (single-folder D104).**
- **learned-audit P0.3 (`6f44d86`):** `FORGE_REWIRE_P_FLOOR`'s bare `float()` could crash-loop the daemon on a typo'd unit env. Extracted `_rewire_p_floor()` + `_quality_rank_mode()` (degrade-never-crash, warn-once, mirrors `_orthogonal_family_floors`): malformed → default + one warn, never raises; unset → byte-identical. 6 tests.
- **learned-audit P0.4b (`a075147`):** integration tests proving the D216 floor call site is LIVE (env set → `orthogonal-family floor ACTIVE` prints; unset → nothing) — guards the D185 inert-call-site class. Gate-tail dispatch left at parse-level coverage (off the live path; needs an F3+model fixture).
- **learned-audit P0.5 (`73d6637`):** `eval-robustness` now labels the actual `--gate` (was hardcoded "cpcv_p25", mislabeled every non-cpcv gate); documented the D216 floor UNIT (max-normalized weight, NOT a delivered share — realized share floats with the oscillating max) in the docstring + MANPAGE.
- **learned-audit P0.2 ([[D217]]):** backfilled the missing D-entry for the 2026-06-26 gate-then-tail scorer (`edb03e6`/`fdeed29`/`92e9061`/`ceeefa4`) — the blend-no-op A/B motivation, two-part form, flags/defaults, floor calibration, shadow evidence, flip gate.
- **strategy-audit P0-2 (`c365d14`):** `battery_survival_by_hypothesis` journal line (survived/enumerated per family) completes the D064/D065 per-hypothesis funnel — the D216 activation is unreadable from the sampling mix alone (the battery differentially kills families: audit measured ~94% ve rejected vs ~52% mr). Pure telemetry (byte-identical enumeration). Gate-class tagging (earnings vs macro) deferred — it changes submission records. Housekeeping: committed the `fable-audit/` workstream (`63d7bfe`) + gitignored disposable `scratchpad/` (`0b2018c`).
- **DEPLOY (operator: "restart the service after all this"):** preflight GO (full suite **1754 passed**), `docs/tasks/deploy.md` ritual (stop 06:55:58Z exit 143 normal SIGTERM → daemon-reload → start). **Verified:** NRestarts=0, ActiveState=active, grammar_version=v22, `registry_loaded_from_export`, no traceback/SchemaVersionMismatch; **D216 floor env preserved** (`FORGE_ORTHOGONAL_FAMILY_FLOOR=volatility_event=0.20`). The new battery-survival line appears on the first UNBLOCKED batch (post-prefetch, hours out under §7.3). **No grammar/gate/determinism change; the D216 activation posture is unchanged.**
- **POST-DEPLOY increments (operator answered: disarm / normalize / pipeline-perf):** §5.5 auto-tune **DISARMED** (`6c083b4`, [[D218]] — `auto_tune.enabled: false`; hot-read, no restart; guard test) + one-time **ruff-format normalization** (`038cb46`, 25 files whitespace-only, suite 1755, `ruff format --check` clean tree-wide → the `--no-verify` excuse is gone). **pipeline-perf P0 (fsync) reconcile fixes:** P0-2 delta-first verdict insert (`2d601a0` — serialize gate-results JSON only for NEW runs, not the ~10k re-dropped each pass) + P0-3 skip the no-op UPDATE for already-gated rows (`bf822c3`). Both byte-equal (existing tests green) + a new optimization guard each.
- **P0-1 RESOLVED + fsync set DEPLOYED (2026-07-02T07:38Z; operator: "recommended is good, restart after p0-1 lands").** Chose **stop writing per-row rejected `pre_filter_logs`** ([[D219]], `18a30eb`) — zero readers (verified), aggregates already in `batch_summaries` + the new `battery_survival` line; **~190s off the submit phase + ~95% of the table's growth**, over the CSV-rewrite (byte-equality risk for data nothing reads). Deployed P0-1+P0-2+P0-3 together via `docs/tasks/deploy.md`: preflight **1750 passed**, restart 00:38:07 PDT / **07:38Z**, verified NRestarts=0 / grammar v22 / no traceback / **D216 floor preserved**. The `phase_timings` win (submit ~190s→single-digits, reconcile −17–20s) shows on the first UNBLOCKED iteration (post-prefetch). Also drafted the P0-3 Crucible gate-class relay (`f622f29`, operator carries it).
- **REMAINING = deeper op-gated tiers:** learned-systems P1 (live ranking path — calibration, prior-weight A/B, gate-then-tail fidelity), pipeline-perf P1/P2 (parse-once, persistent feature cache, permutation_test memo), deeper CQ/strategy items. All separate efforts.

---

## 2026-07-02 — D216 Layer-2 ACTIVATED (deploy): `FORGE_ORTHOGONAL_FAMILY_FLOOR=volatility_event=0.20` set on `forge.service` + daemon restarted. Prereg `5c4ba16ff6cf`. Lifts single-name vol_event sampling share ~2.9%→~10.7%; REVERT = delete the env line + restart (byte-identical). [[D216]]

**Operator: "activate Layer-2 (deploy call) to chase the first promotion" + "commit and push the tree" + floor 0.20. Deployed via `docs/tasks/deploy.md`, single-folder in-tree (D104).**
- **Gate:** full uncontended suite **1745 passed** (165s) on the committed build (`ce83584`); ruff+mypy clean — fixed 4 pre-existing `E501`s in `contracts_check.py`'s 1.22.0 comment block mid-deploy so the pre-commit hook passed. Activation itself adds no src (unit env + prereg + docs).
- **Activation:** `Environment=FORGE_ORTHOGONAL_FAMILY_FLOOR=volatility_event=0.20` on `deploy/systemd/forge.service` (committed → reboot-safe). Ritual: stop 22:28:44Z (exit 143, normal `--loop` SIGTERM) → commit build → commit activation → restart. Verify at restart: `NRestarts=0`, journal clean, `hypothesis_weights` floor line on the first UNBLOCKED iteration (may be hours out under §7.3 backpressure — the flag acts on the family draw).
- **Prereg (D208) `5c4ba16ff6cf`** (cohort_cut 2026-07-02T05:31:30Z): claim = the added decorrelated vol_event supply improves the assemblable book (CSCV PBO ≤0.40 + positive ve marginal contribution) on post-cut data; action = make the floor standing if confirmed, else drop the env (byte-identical OFF). **Alpha-budget (D207) hurdle at activation: v22 = 4.38 (submitted) / 5.04 (enumerated) search-luck Sharpe.**
- **Caveat (07-01 magnitude×PBO, `cont.3`):** PBO is NOT the binding wall now (best honest book 0.067); the wall is edge MAGNITUDE (selected cpcv-p25 ~1.0). Layer-2 supplies the DECORRELATED half (necessary), not magnitude (the harder, still-open half) — so this is a supply-side A/B, not a guaranteed promotion. Do NOT double-count with Crucible's default-OFF `cluster_preselect` (they COMPOUND on a future PBO shift).
- **Layer-1 (estimand re-aim) stays HELD** — consumes `component_contributions`, empty until the first promotion. **No grammar/gate/determinism change; the flag-OFF path stays byte-identical (hard rule 6).** External warrant for the lever: the vol-event cross-sectional research verdict ([[VOL_EVENT_CROSS_SECTIONAL_RESEARCH]]) — single-name vol_event is the idiosyncratic/orthogonal supply; "cross-sectional" belongs at assembly.

---

## 2026-07-01 — P3 "decorrelated supply, deeper" — BOTH TRACKS: built the Layer-2 orthogonal-family floor-lift (A/B flag, OFF, byte-identical) + relayed the contracts-export gap that blocks the Layer-1 principled estimand. Daemon UNTOUCHED (flag not set); activation operator-gated. [[D216]]

**Operator P3: "the grammar keeps producing near-identical MR — the real long-term fix is generating genuinely decorrelated edges. Forge/generation work, not a quick probe." → chose BOTH tracks.**
- **Diagnosis (verified live 07-01):** the learned `hypothesis_weights` estimand is per-hypothesis **component-rate** — it rewards "more of what already clears," the across-book homogeneity PBO penalizes. **The monoculture OSCILLATES trend↔mr** (06-25 ~85% mr → 06-29 81% mr → **07-01 trend=1.000 weight, ~50% submitted**) — the two halves of the 0.78-corr core — while the Crucible-validated in-v1 orthogonal family (single-name `volatility_event`, PC1 load 0.10, book PBO 0.107) sits **pinned at exactly the D067 5% floor (weight 0.050)**: the estimand gives it ~zero organic weight; only the exploration floor keeps ~7% flowing. Reconciles [[D215]]'s "redundancy is selection-side" (MR supply is diverse at 0.158): no selection decorrelates below 0.78 inside the core → the only generation lever that adds a dimension is the **family mix** — supply more vol_event (the 06-29 winning book was 0→67% ve).
- **Track A (Layer-1 principled, Crucible-gated):** the correct fix re-aims the estimand from component-rate → **marginal portfolio contribution** (`correlation_to_incumbent` × `marginal_sharpe`). Crucible SHIPPED that signal internally (D213 Ask-2, `1926cbb`) but it is **NOT in `crucible_contracts`** (verified) → a **contracts-export gap** (hard rule #2; Forge has no return data at generation, [[D186]]). Drafted (HELD) `PROMPT_CRUCIBLE_MARGINAL_CONTRIBUTION_EXPORT.md` — one ask: expose it per-component (or per-family) in a consumable form.
- **Track B (Layer-2 interim, BUILT flag-OFF):** `FORGE_ORTHOGONAL_FAMILY_FLOOR` env knob — a bounded per-family floor-lift over the learned weights (`apply_orthogonal_family_floor` in `rejection_weights.py`; `_orthogonal_family_floors` parser + call-site wiring in `main.py`). **OFF by default → byte-identical (hard rule 6):** empty map → block skipped, enumeration unchanged (invariant test; determinism goldens intact). `volatility_event=0.20` lifts vol_event's sampling share **2.9%→10.7%** while trend eases 58%→54% — starves nothing (`max` semantics; every family keeps its D067 budget). 10 new tests; affected scope **764 green**; ruff+mypy clean; tree importable (reboot-safe).
- **POSTURE: daemon UNTOUCHED — no restart, flag not set.** Activation (set the env on `forge.service`) is the operator-gated deploy: prereg the prediction (D208), charge the alpha budget (D207), later-cohort-confirm (§8.4), revert (drop the env) if PBO doesn't improve. Full uncontended suite + `docs/tasks/deploy.md` at deploy-prep. **No grammar/gate/determinism change.** Honest ceiling unchanged: in-v1 NARROWS via the validated vol_event family; the third *risk driver* is still v2/Path C ([[exhaust-long-options-before-v2-spreads]]).

**↳ same-day — Crucible DELIVERED the Layer-1 signal (`FORGE_marginal_contribution_export_status.md`, commit `a7228f9`); operator chose loader→contracts + hold-the-re-aim.** Correction accepted (owned): `marginal_contribution` was **probe-only** (never in the live path) → exposing it was a BUILD, not a read-side unblock. Shipped as a **separate export** `~/optbt_data/exports/component_contributions_<iso>.json` (schema `component_contributions/v1`, `{config_hash:{portfolio_id, correlation_to_incumbent, marginal_sharpe}}`, leave-one-out per promoted book) — NO contracts model bump. **Verified live: publisher running, export present but `n_contributions: 0` (empty)** — Crucible's caveat holds, the signal is **promotion-gated** (populates only as books promote). **(1) Loader → contracts** (rule #2 + every export reader lives in `crucible_contracts`; a read-helper ≠ the model bump they avoided): drafted (HELD) `PROMPT_CRUCIBLE_CONTRIB_LOADER_IN_CONTRACTS.md`. **(2) HOLD the estimand re-aim** — can't validate a component-rate→low-corr×marginal_sharpe flip against a null signal; build+flip once the export has real data. **Layer-2 carries supply meanwhile** (Crucible concurs). **Built now:** a soft `forge healthcheck` line for the export (`check_component_contributions_export` — OK-when-absent, never pollutes OVERALL; no typed read → doesn't pre-empt the contracts loader); live `[OK] component_contributions: present (0.2h old)`, OVERALL=OK; TDD green, ruff+mypy clean. **Daemon UNTOUCHED. Sequenced next:** loader-consumer + the held re-aim (flag-OFF) after the contracts loader lands + the first promotion populates the signal.

**↳ cont.2 — Crucible AGREED loader→contracts + shipped it (contracts `afbe737` = 1.22.0); Forge ADOPTED (pin-only).** Rule-#2 posture won: `crucible_contracts` **1.22.0** hosts `load_component_contributions_from_export -> dict[str, ComponentContribution]` (frozen; `marginal_sharpe` may be NEGATIVE — don't clamp) + `COMPONENT_CONTRIBUTIONS_SCHEMA`; `{}` cold-start on absent/empty/unknown-schema. Bumped `FORGE_EXPECTED_CONTRACT_VERSION` 1.21.0→**1.22.0** (pin-match green). NO-OP (nothing imports the loader yet — consumer+re-aim HELD). **uv.lock UNCHANGED** — Crucible bumped `_version.py` but NOT `crucible_contracts/pyproject.toml` (still 1.21.0) so `uv lock` can't record 1.22.0 (cosmetic for the editable dep; **Crucible should bump pyproject to match**). **Suite 1559 unit + 185 inv/integ GREEN**; the one red — `test_every_command_is_mentioned_in_manpage` → `forge ranker-model eval-rewire` — is **pre-existing at HEAD** (the operator's committed gate-then-tail re-wire command lacks a MANPAGE row; not this work). Commit + `forge.service` restart operator-gated (daemon already runs 1.22.0-in-memory via the editable dep; the pin just aligns the startup guard + healthcheck).

**↳ cont.3 — 1.22.0 PUSHED + `forge check` OK; Crucible wired the SELECTION-side mirror (default-OFF); worldview sharpened to a magnitude×PBO TRADE-OFF (`FORGE_selection_robustness_and_contracts_1_22_0.md`, `a922f5f`/`73d7c2c`).** `forge check` → `crucible_contracts: 1.22.0 OK` + schema OK; D124 non-issue confirmed (new model + new export, no existing model changed → no restart sequencing). **uv.lock still 1.21.0** (pyproject still 1.21.0; re-flag Crucible to bump it). **Selection-side (default-OFF):** `cluster_preselect` (collapse corr≥0.6 near-dupes before selection — assembly-side analogue of Forge's generation-side rebalance) + `shrinkage`; flipping either is a separate operator-gated activation. **Interaction:** Crucible-dedup + Forge-rebalance COMPOUND → don't double-count a future PBO shift. **WORLDVIEW SHARPENED — magnitude×PBO TRADE OFF:** best HONEST book PBO already **0.067** (not binding; hygiene drives it to ~0) but REJECTED on **edge MAGNITUDE** (selected cpcv-p25 ≈ **1.0**, maxDD −0.12). The D212 "cpcv 1.79–1.95" books were the OVERFIT high-PBO ones; anti-overfit hygiene that clears PBO also drops honest magnitude to ~1.0 → the wall is the **joint strong-AND-decorrelated frontier**, and the ONE agreed lever is **more STRONG decorrelated supply** (single-name vol_event = the decorrelated half; magnitude is the harder half). Tempers 06-29's "first promotable book" (n=8 PBO-only). **No new Forge action; re-aim + Layer-2 posture unchanged (HELD/operator-gated).**

**↳ cont.4 — GAPS CLOSED (operator: "close gaps"):** (1) bumped `../crucible_contracts/pyproject.toml` 1.21.0→**1.22.0** to match `_version.py` → `uv lock` now records crucible-contracts **1.22.0**; `forge check` OK. (Contracts-repo pyproject edit uncommitted — operator commits/pushes per their release process.) (2) documented `forge ranker-model eval-rewire` in MANPAGE → `test_every_command_is_mentioned_in_manpage` green → **full suite GREEN (1559 unit + 186 inv/integ)**. (3) marked the two answered relays RESOLVED. No held lever flipped (Layer-2 activation + Layer-1 re-aim stay HELD by design). Daemon untouched; nothing committed in Forge.

---

Older blocks (2026-05-29 → 2026-06-29, incl. the Phase-0/May scaffolding, session log, and original blockers): rotated to `_archive/STATUS_2026-05_2026-06.md` (2026-07-20, D295 — the D242 rotation precedent).
