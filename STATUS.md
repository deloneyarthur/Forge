# Forge — Status

## 2026-08-14 (later) — **CRUCIBLE CLOSED §8 STEP 2: neither generation nor refit triage binds component production. Their `rank_k=20` residue is OUR v48, shipped 2026-07-22 on their own root-cause — 63,171 configs, zero emitted since. No action.** (D393)

- **THEIR ANSWER:** refit ordering WAS quality-blind; they shipped a quality sub-lane 2026-08-06 (the day after our relay). **2.5% of volume → 84% of promotes; 6.30% vs 0.03% = 206×.** Our hypothesis was right and the effect is bigger than we argued. **But more of that lane is INERT** — raised 20%→80%, no effect, reverted same day: it finds 0–3 eligible per pass. **"Refit our best 2,000" was already executed** (1,736 above-floor rows ARE components, 1,460 more have children; un-consumed supply = **3**).
- **THEIR §5 RESIDUE IS OUR v48.** 17 un-convertible configs, all tier-2 `rank_k=20`, refused by their `2 × rank_k` breadth floor at 100% chain coverage — **the exact mechanism they root-caused for us 2026-07-22, which we shipped the same day** (`2160149`, v48, D328: `_RANK_K_CHOICES (5,10,20)→(5,10)`). Snapshot-verified: **63,171 `rank_k=20` ever, last 2026-07-22, ZERO since.** Their 17 are a CLOSED cohort that cannot grow. **Nothing to do.**
- **⚠️ ONE PUSHBACK REGISTERED, not silently accepted.** They ask for `rank_k ≤ 5` two-sided as "genuinely selective". We think that conflates INCLUSION with SELECTION: `long_only` k=20 is degenerate (buy 20 of 20, ranker irrelevant), but `long_short` k=10 includes every name while **the ranking still decides each name's SIDE** — standard dollar-neutral construction where the alpha is the spread. **Returned as a question:** is the floor about inclusion breadth or about dispersion being unmeasurable at n=20? We will not tune generation against a floor whose purpose we inferred (D361).
- **THE FREEZE'S FIRST LIVE TEST.** Their finding landed hours after signature. If they confirm the dispersion reading, the bound is a prereg + bump + D-entry and is a **tightening** (hard rule #4, no loosening path) — D392's hook will demand the prereg first. **The honest test is whether the freeze processes a real change correctly, not whether it blocks one.**
- **BOTH BINDING-CONSTRAINT CANDIDATES ARE NOW CLOSED** — not generation (ours), not refit triage (theirs). Their two withdrawn numbers (3,252 backlog / ~190 expected promotes) were never load-bearing here.

## 2026-08-14 (later) — **BOTH GOVERNANCE GAPS CLOSED. The signed freeze is now structurally enforced; a registered read can no longer come due silently or be taken and lost.** (D392)

- **GAP 1 — `freeze-governance` pre-commit hook (`scripts/check_freeze_governance.py`).** With the declaration SIGNED, a *content* change to `config/grammar.yaml` is **refused** without an open preregistration stating a required n — §6 sequencing, the half a machine can check. **§5 reopeners stay possible but cannot be silent:** `FORGE_FREEZE_REOPENER=D###` works only when the same commit stages that `## D###` Decision Log header. **Verified live** (refused a real staged grammar edit, refused a record-less override, grammar restored byte-clean).
- **GAP 2 — `forge-prereg-watch.timer` ARMED (daily 06:30, `Persistent=true`) + `--resolve` on the read tool.** The watcher reports DUE (exit 1) / waiting-with-count (exit 0) / **UNWATCHABLE (exit 2)** — that last case being the D389 defect itself, *a claim no watcher could ever have checked*. **No `SuccessExitStatus`**, unlike healthcheck: both conditions must mark the unit failed, because a swallowed warning is indistinguishable from no watcher. Smoke-run green: `Result=success`, "no open preregistrations".
- **The watcher never computes the metric** — rows and fingerprints only; peeking to decide whether to page would BE the read. Counts are basis-scoped in SQL so a foreign-basis backlog cannot fake a due read (D387/D391 shape), asserted by test.
- **`--resolve` closes the take-and-lose path**: `3b0cbca7ae17` had to be resolved by hand. Without the flag the tool now prints `[not recorded] ... the registry is UNCHANGED`. An unreadable leg resolves **insufficient, never refuted** — "not enough data" must not be a quieter kind of verdict.
- **VERIFICATION:** 16 new tests TDD red→green; 58 script + 184 unit/invariant tests pass; ruff clean; mypy --strict clean (107 files). Single-read guard re-confirmed live — the within-basis read now aborts with `status is 'confirmed', not 'registered'`.
- **NOTHING OUTSTANDING ON THE FREEZE PROGRAMME.** 31/31 preregs resolved, freeze signed and enforced, watcher armed. Next inbound: Crucible's §8 Step 2 refit-ordering answer — with the grammar frozen, the only live lever either side has identified on component production.

## 2026-08-14 (later) — **CORRECTION: the basis boundary is the 3rd (the RUN), not the first trading day (the LABEL). D386's calendar rule is FALSIFIED — August hid it because Aug 1 was a Saturday. September diverges by two days.** (D391)

- **THE SCHEDULE, from Crucible:** `OnCalendar=*-*-03 06:00, Persistent=true` → next run **2026-09-03 (Thu)**; the snapshot is *labelled* `asof 09-01` (their ranker picks the first trading day inside the month window) but **does not exist until the run**. August: Aug-01 was a Saturday so the first trading day WAS Aug-03 — label and fact coincided **by accident**, and we generalised from that one observation.
- **CONSEQUENCE:** configs generated 09-01 and 09-02 still draw the **AUGUST** universe. Any window cut on "first trading day" puts those two days on the wrong side — the same class as D387's window-grid artifact, and invisible again in any month where the dates align.
- **`Persistent=true` ⇒ the boundary moves LATER, never earlier.** A missed run fires on next boot: the honest phrasing is **"on or after the 3rd"**. Crucible will relay a late catch-up as notifiable, same as an off-cycle re-rank (none planned; schedule firm).
- **THE RULE THAT REPLACES D386's:** the resolved **`enumeration_inputs_hash` universe component is the cut, full stop.** The calendar is demoted to a planning expectation. **Nothing that cuts a window may key on a date** — D387's basis guard already enforces this; D386's heuristic was a weaker parallel instrument that would have disagreed with it in September.
- **THEIR EXPORT WENT DAILY 2026-08-11** (06:05 heartbeat, so QuantIQ can tell "unchanged" from "publisher stopped"; their staleness budget 45d → 5d). They flagged that a consumer keying on file identity/mtime would read a daily basis change that is not one. **VERIFIED IN CODE: we do not.** `_load_underlyings()` returns a sorted ticker union and `universe_fingerprint()` (D078) hashes that content + tier-3 split, so byte-identical republishes fingerprint identically. The two mtime globs in the tree read *other* files (`registry_snapshot_*`, `chain_inception_floors_*`). **No change required.**
- **EFFECT ON THE SIGNED FREEZE (D390): none.** The `3b0cbca7ae17` read sat inside `e1adced727678c8f` on basis-clean windows 12–17 with zero foreign-basis rows; the correction moves that basis's END from 09-01 to on-or-after 09-03 — **more headroom, not less.** The prereg's "ends 2026-09-01" clause is wrong and is corrected in D391; **the registry entry is left as written — a resolved prereg is an immutable record.**

## 2026-08-14 (later) — **THE GRAMMAR FREEZE IS SIGNED.** Operator, 2026-08-14. Frozen at **v55**; §6 governance in force; §5 reopeners live. Programme D328 → D390. (D390)

- **ALL THREE CONDITIONS MET on registered reads:** (A) coverage — `dead_unprotected` empty, 0 of 148,322 flow · (B) multiplicity efficiency — metric B 0.00%, 18 consecutive runs vs a bar of 7 · (C) supply ceiling — **re-founded within-basis** on `3b0cbca7ae17`, both legs BELOW baseline (−0.0222 quality / −0.0092 redundancy, D389).
- **WHAT WAS SIGNED — the narrower of two available claims.** That the **bulk of supply has stopped improving** and the budget belongs on the converting core. **NOT** that a ceiling was proven or that the grammar cannot be improved. Both counter-measurements were read first and are named in the signed header: the ranked lane still sets running-max records at the **unbounded-search rate** (z=+0.13, trail to +1.7397) and the **joint frontier is still advancing** (z=+3.60, p=0.003).
- **⚠️ THE STRONGEST OBJECTION IS UNANSWERED AND SAYS SO.** §4: *the window spans mostly prunes, not expansion — absence of movement is weak evidence about what expansion could do.* Not answered, not claimed to be. It is why §5 reopeners (2) new net-long-vega registry family and (3) Path-C are first-class: they ARE the untested expansion cases, each reopening a bump on the operator's signature alone.
- **WHY NOW, on measured grounds rather than exhaustion (§8 Step 1):** 14 of 23 configs clearing both binding gates were **never refit** — identical stage-one profile to the 9 that were, refit latency p99=2h proving they were passed over. **The binding constraint is Crucible-side refit triage, not our generation.** More grammar search cannot touch it.
- **IN FORCE NOW:** every grammar change is a full increment — **prereg before the edit with required n at registration**, version bump + archive + D-entry, goldens re-pinned, emission proof, funnel attribution, STATUS block.
- **⚠️ TWO KNOWN GAPS, both logged not assumed:** the freeze is **procedural, not code-enforced** (nothing reads the declaration; the pre-commit version-bump scanner would still pass a post-freeze bump — a hook-level guard is the obvious follow-up and is NOT built), and the **D389 read-watcher is still unarmed**.

## 2026-08-14 — **WITHIN-BASIS (C) READ: BOTH LEGS CONFIRMED, both DOWN. `3b0cbca7ae17` resolved — the freeze programme now has ZERO open preregistrations.** (D389)

- **LEG 1 quality:** best-of-new **0.8971** vs baseline 0.9193 = **−0.0222** against bar 0.0536 (falsify above 0.9729) → **CONFIRMED**. **LEG 2 redundancy:** best-of-new **0.4392** vs 0.4484 = **−0.0092** against bar 0.0135 (falsify above 0.4619) → **CONFIRMED**.
- **Both deltas NEGATIVE.** The prereg only asked for "not up by more than the bar"; both legs came in *below* baseline, so neither confirmation rests on its bar — and leg 1's −0.0222 would have cleared the original (C)'s narrower 0.0242 bar too. That is what makes D388's own "this bar is 2.2× wider" caveat survivable rather than fatal.
- **Instrument clean:** basis-scoped to `e1adced727678c8f` (n=22,273, 18 basis-local windows, grid built after filtering → zero straddles); basis guard reports windows 12–17 basis-clean; corr join 84.2%; reference fp `ae47a4749c9d` verified. **Void condition never fired** — zero rows on any other basis since the cut, no grammar bump shipped in the window.
- **⚠️ RECORDED AGAINST US — the read was OVERDUE and nothing fired it.** STATUS described a watcher as armed; there is **no systemd unit and no cron entry** for `freeze_registered_read.py`. The clock (7,490 in-basis rows vs 7,200 required) had already passed when an operator check surfaced it. The prereg forbids extension, so a silent drift here is a live failure mode. **Fix or delete the claim — an unarmed watcher described as armed is worse than none.**
- **⚠️ The registered literals mattered.** Recomputed priors had drifted to 0.9204/0.4485 vs the registered 0.9193/0.4484 as post-stratification re-weighted history. The read used the literals, which made the test marginally *harder*.
- **WHAT IT AUTHORISES:** (C) replicates inside a single generation basis — stronger than the original, because the generator now draws a different universe. The freeze **declaration** may be re-founded on within-basis evidence. **That is a document change and needs the operator's signature. Not taken.** Crucible relay owed.

## 2026-08-10 (later) — **WITHIN-BASIS (C) REGISTERED: `3b0cbca7ae17`.** A replication, not a re-baseline — both original (C) legs were basis-clean and STAND. First non-degenerate drift fit in the programme. Reads ~2026-08-13. (D388)

- **REPLICATION, NOT REPAIR.** Global windows 1–18 are all `877b`; leg 1 read 11–16 and leg 2 read 12–17, so **neither original (C) read crossed the boundary**. They stand as measured. The question is whether (C) *still holds* now the generator draws a different universe — stronger evidence than the original, not a patch on it.
- **REGISTERED NUMBERS** (basis `e1adced727678c8f`, filtered before gridding, 11 prior windows, zero straddles, coverage 0.996–1.000, corr join 100%): leg 1 max **0.9193**, bar **0.0536**, falsified above **0.9729**. Leg 2 max **0.4484**, bar **0.0135**, falsified above **0.4619**.
- **THE DRIFT FIT DOES NOT DEGENERATE — first time ever here.** `b=0.0235`, `b_up=0.0268`. Every prior attempt returned `b=0.0000` exactly ("cannot resolve drift" wearing a decisive number) and forced a `2·sd` fallback three times. Within a single basis, drift is real and resolvable.
- **⚠️ RECORDED BECAUSE IT CUTS AGAINST US:** leg 1's bar is **2.2× the original's 0.0242** — wider *because* the fit now resolves drift. **A wider bar makes "flat" easier to confirm**, so a confirmation here is weaker per-unit than the original's. The rule was held fixed rather than the number; the consequence is in the prereg, not a footnote.
- **VOID CONDITION, instrument-enforced:** if the basis changes before 6 new windows accrue, the prereg is VOID and re-registers inside the new basis. Two triggers: an early Crucible re-rank, and **a grammar bump — also a generation-basis change. No grammar change ships during the window.**
- **INSTRUMENT:** `--read within-basis` (reuses `RegisteredLeg`/`_read` — a replication must not move the rule; only `filter_to_basis` differs upstream) and `--basis` on the exploratory tool. **Two guard bugs found and fixed**: `ref` divided by the pre-filter `n` (cancels in `_tcm`'s ratio, so only the *alarms* broke — coverage read 0.391), and `_basis_guard` saying "UNTAGGED" when it meant "not enough windows yet".
- **CLOCK:** 11 of 17 basis-local windows; 6,431 rows short at ~2,210/day → **~2026-08-13**, comfortably inside the basis (ends 09-01). Watcher armed.

## 2026-08-10 — **RETRACTION + THE BASIS GUARD. Our "13:27Z boundary" was a window-grid artifact; the true cut is 17:15:54Z, our own cache lag. Crucible was right — generation-basis. And within a fixed basis the ceiling is FLAT.** (D387)

- **WHAT WE GOT WRONG.** `13:27:42Z` is where our 1200-row grid broke — an index, not a changepoint. Their 13:00:07Z publish sat 27 min earlier and we read the proximity as corroboration. At hourly resolution there is **no discontinuity there at all**. Every elimination in D386 was cut in the wrong place.
- **THE TRUE BOUNDARY: `2026-08-03T17:15:54Z`** — our `_load_universe_tiers_cached` lru_cache picking up their publish **4h15m late**. Old universe `877b1eddde9864eb` TCM 0.7384/0.7634; new `e1adced727678c8f` TCM **0.8745**. The step is exactly at the fingerprint change.
- **THE DECIDING TEST GOES TO THEM.** Configs generated 08-03 under the OLD universe but **scored after their publish** read **0.7674** — the old level. Ranking-time route REFUTED; **generation-basis CONFIRMED**; our amendment to their sentence withdrawn. Retraction relay sent.
- **LAYER 1 ALREADY EXISTED.** `universe_fingerprint()` (D078) has been folded into `enumeration_inputs_hash` all along and recorded the boundary faithfully on the day. **The gap was consumption, not instrumentation.** Layer 2 relayed back as de-prioritised — only a scoring-basis change (07-19 cache-repair class) is still uncovered.
- **GUARD BUILT (TDD).** `window_bases()` + `_basis_guard()`: a **straddling** window reports BOTH bases (never assigned to a side — that is how a grid boundary became a changepoint), an **untagged** window refuses (a guard that passes on absent data certifies). LEFT JOIN so untagged rows arrive NULL rather than being dropped and moving the grid. 37 tests green, ruff clean. Live map: windows 1–18 `877b`, **19 STRADDLES**, 20–29 `e1ad`.
- **⚠️ THE USEFUL CONSEQUENCE — `74dbbaee89c7` splits in two.** **Leg B is BASIS-CLEAN and STANDS**: threshold from window 23 (`e1ad`), windows 24–29 all `e1ad` → within one basis, **the ceiling is NOT rising**. **Leg A is CROSS-BASIS and is discounted**: its 0.7877 threshold is old-basis, its windows new-basis, so the +0.0798 measures the universe change, not persistence.
- **NET ACROSS D384–D387: within a fixed generation basis the ceiling is FLAT, and the apparent break was a basis change.** That is the pre-break (C) reading re-derived on the far side of the boundary — the strongest agreement available here.
- **NEXT:** register the within-basis (C) on the `e1ad` era (started 08-03 17:15, ends at the 09-01 re-rank); ~10 windows already accrued, ~1.9/day, so a 30-window read lands well inside the era. No grammar bump during the window — a grammar change is a generation-basis change too.

## 2026-08-09 (latest) — **BOUNDARY IDENTIFIED: Crucible's MONTHLY tier-3 universe re-rank, 13:00:07Z, 27 minutes before ours.** Cause accepted; their route refuted on our data. First-trading-day-of-month adopted as a standing basis boundary. (D386)

- **THEY FOUND IT, with receipts.** `crucible-universe-publisher` runs the §3.3.1 tier-3 re-rank **only on the first trading day of each month**. 2026-08-03: `tier3.floor_excluded n=86`, `tier3.refresh_written n=74` at **13:00:07Z**. They cleared every scoring input across the boundary. The 07-01 firing is the same job — our "July universe shrink", which retro-explains the v35→v36 boundary note.
- **THEIR ROUTE DOES NOT REPRODUCE.** They predicted our configs would draw from the new 74-name pool. Measured: **XSECT arm +0.1238** (n=19,101/14,098), **NAMED arm −0.0140** (n=1,252/388). The step is entirely in the arm that **draws no underlying**. Our named arm is ~100% `volatility_event` on index/ETF underlyings — the static tiers they said tier-3 churn does not touch.
- **COMPOSITION IS DEAD AT EVERY LEVEL WE CAN HOLD.** Tier share 4.5%→1.9% (their 86 exclusions, visible) but tier-3's own quality is flat +0.0026 and re-weighting pre-quality to the post mix buys **−0.0031** — the wrong direction. Cell+tier held together: +0.1251 / +0.1293 / +0.1062. Both-era underlyings only: +0.1245. `selector.universe` absent from all 52,952 configs.
- **WHAT OUR DATA FORCES:** the affected population has no underlying, so the route cannot be the drawn name. The remaining construction is **the universe an xsect config is RANKED OVER**, read at run time. That would make their "a config's SCORE cannot see it" true of the price data and false of the ranking universe. **Asked as a question about their runner, not asserted.** Also explains why selection amplified it (+0.2136 vs +0.1178).
- **⚠️ LAYER 1 WILL NOT CATCH THIS.** Their cheap marker fingerprints the snapshot our generator read *at birth*. If the basis attaches at ranking time, birth is the wrong stamp — a config generated pre-boundary and scored post-boundary carries a fingerprint asserting a basis it was never measured under. We escaped only because median queue lag is 0.37h and nothing straddled 13:00Z; a 09-01 backlog won't repeat that. **Layer 2 requested** (data-basis fingerprint on stage-one verdicts, 1.4x dance). Layer 1 we build anyway — free, catches generation-side changes layer 2 won't.
- **ADOPTED REGARDLESS: first trading day of the month is a standing basis boundary** — 09-01, 10-01, monthly. The freeze instrument must REFUSE across it, as leg 2 already refuses on the reference-book fingerprint. Design, not yet built.
- **FREEZE POSITION, now agreed by both sides:** (C) pooled two bases; the **pre-break** reading is the one that speaks about the grammar; re-baselining at ~0.88 would bake their liquidity floor into our grammar criterion. **Re-baseline HELD.** `74dbbaee89c7`'s arithmetic untouched — its level shift is durable *within the new basis*, not evidence the grammar improved.

## 2026-08-09 (later) — **THE BREAK IS NOT OURS.** A global cpcv step at 2026-08-03T13:27Z survives holding grammar version, cell and composition fixed. D384's attribution is refuted by our own data; (C) has been reading across two measurement bases. (D385)

- **THE STEP IS GLOBAL AND LARGER ON THEIR SIDE.** Selected stream median 0.2613 → 0.4749 (**+0.2136**, n=54,306/84,905); our honest arm +0.1178. Not our sampling.
- **IT SURVIVES HOLDING THE GRAMMAR FIXED.** v54 configs only, within cell, shares flat to ±1pp: trend/swing_long **+0.0684**, trend/swing_mid **+0.1089**, MR/swing_mid **+0.0923**. Same generation policy, same mix, better scores.
- **EVERY CANDIDATE ELIMINATED.** v55 — head-to-head in the same windows is +0.0343 pooled and **−0.0020 in MR/swing_mid**, a cell it does not touch that rose +0.13 anyway. ve-floor — lands a day AFTER the break, and it is a between-hypothesis share change on a post-stratification dimension, so `_tcm`'s weighted quantile + weighted mean are blind to it by construction (ve's own within-cell quality: **+0.0066**). Re-gate wave — honest arm is **34,839 rows / 34,839 distinct configs**, zero duplicates, all re-measured pairs pre-boundary. Data volume — `trade_count` **fell**. SAMPLE_N — TCM flat at ~0.75 across both rate regimes, steps after.
- **WE CANNOT TEST SUBMISSION-TIME vs SCORING-TIME.** Median queue lag 0.37h and, excluding refits, **no stage-one row submitted pre-boundary was decided post-boundary** — the discriminating cell is empty. **Relay SENT** with the evidence and four ranked candidates; a rolling evaluation window is the most likely and needs no fix, only a name.
- **⚠️ (C) HAS BEEN POOLING TWO BASES.** Windows 1–17 in one, 18–29 in another. That is D357/D358 in a third costume — and it is the skim rule we adopted from Crucible in **D381** violated by our own instrument within a week of adopting it.
- **WHAT THIS DOES NOT RETRACT:** `74dbbaee89c7`'s arithmetic. Leg A's level shift is durable *within the new basis*. It is **not** evidence the grammar improved and must not be cited as such.
- **RE-BASELINE HELD** pending their answer — baking their environment into our grammar criterion would read as a decline when it moves back. The pre-break (C) reading, entirely inside one basis, may still be the right answer about the grammar.
- **NEXT:** their reply, then a basis-era guard in the freeze instrument — a hard REFUSAL across a detected boundary, as leg 2 already does on the reference-book fingerprint. Asked them for a machine-readable basis marker to key it on.

## 2026-08-09 — **THE CEILING STEPPED ONCE AND RE-PLATEAUED.** Prereg `74dbbaee89c7` read once at its registered n: leg A CONFIRMED, leg B REFUTED. The freeze survives on the merits; its evidence base is stale. (D384)

- **THE READ.** 29 complete n=1200 windows, n=34,839 honest-arm stage-one rows. Windows 24–29 standardised TCM: **0.8759 0.8971 0.8889 0.8675 0.8918 0.8751**. Leg A (persistence): **min 0.8675 vs 0.7877 = +0.0798 → CONFIRMED**. Leg B (continued rise): **max 0.8971 vs 0.9409 = −0.0438 → REFUTED**. Registered literals used; the bar was **not** refit on the judged data — the exploratory instrument still says "flat" today only because it fits `b` over the break itself.
- **WHAT IT MEANS.** The plateau moved ~0.75 → ~0.88 and settled. The new floor clears the old ceiling by 3.0× the bar, and **no window reached even the pre-read peak 0.9145** — window 23 was the top of the excursion, not a rung on a climb.
- **LEG B REFUTING IS THE RESULT THAT KEEPS A FREEZE COHERENT.** A still-climbing ceiling would mean demonstrable headroom and the freeze would be wrong on the merits. It is not climbing. **Leg A confirming is what invalidates the declaration's evidence**: (C) certified flatness at a level the stream has since left. Not wrong about flatness — stale about where.
- **DECLARATION → ⚠️ DO NOT SIGN AS WRITTEN.** Re-baseline first: a fresh (C) registered against the post-break level, drift floor refit on **post-break windows only** (we have six). The prior fit degenerated to `b=0.0000` on 17 windows — "cannot resolve drift", not "no drift" — so the window budget for a non-degenerate refit is the open design question, and reusing the old 2·sd fallback silently is exactly what not to do.
- **ATTRIBUTION IS STILL OPEN AND NOT INFERABLE FROM THIS READ.** v55 (08-03 02:27Z) and the D216 ve-floor retirement (08-04 02:27Z) are one day apart and mutually confounded. The ve-floor change is the larger and more plausible driver; that is an argument, not a measurement.
- **INSTRUMENT.** `freeze_registered_read.py` gained a separate `PersistenceLeg` path — same six windows for both legs, **min** not max for leg A, and CONFIRMED means the prediction *held*, the opposite polarity from the (C) legs. Sharing `_read` would have inverted leg A. Plus `--read {c,persistence}` and a registry-status guard that aborts on any prereg not marked `registered`.
- **NEXT:** design the re-baselined (C). Nothing else in the freeze programme is blocked on data.

## 2026-08-06 — **THE PROMOTE STREAM RECORDS AGAIN: 4 → 29 promote verdicts, and ALL of the recovered ones are OURS.** The loop from our triage relay to a validator fix is closed both sides. (D370/D374/D375/D376)

- **WHAT OUR RELAY SET OFF.** We reported a triage loss (D368: 14 of 23 dual-gate clearers never refit). Crucible reproduced our 23/9/14 **exactly**, and validating it uncovered something far larger: their `PromotionDecision` validator had been **destroying every stage-two promote since 07-23** — §20 made `deflated_sharpe` recorded-but-non-binding while the validator still demanded zero failed gates. **31 configs, 67 children, 12 permanently blacklisted.** Failed runs never export, so it was structurally invisible to us.
- **OUR "ZERO PROMOTES" WAS A MEASUREMENT SHADOW.** We held 4 promote verdicts ever, last on 07-18 — four days before §20. **Now 29.** Every recovered promote **joins to our own `submissions`** — no injections — and all are `mean_reversion/swing_mid`, the one lane our joint-frontier work had independently flagged as still advancing (z=+3.71). Two instruments aimed at different questions agreeing.
- **ADOPTED AND SHIPPED.** Contracts **1.42.0 → 1.44.0** (D374, pin-only; 1.43.0 relaxes a validator we only read, 1.44.0 adds the lane tag we asked for — a **free string, not a Literal**, which is why the D261/D342 reader hazard does not apply). Then **D375: we were dropping that tag.** `refit_selection` arrived on every row and our writer discarded it; one idempotent ALTER + writer field, verified against a copy of the live 617,782-row table. **Working end-to-end: 8 `quality_margin` rows stored, 2 of them promotes.**
- **dsj RE-READ (D376) — the claim STANDS, better founded.** 29,887 dsj configs → 7,901 components → 359 clearing the book floor → **0 promotes, 0 book legs**. The recovery cohort could not have rehabilitated it *even in principle*: dsj is trend-only and all 23 promotes are MR, and because Crucible re-derived the cohort **live from their DB** it is complete — so no dsj config ever crashed. All six dsj book legs remain the single `dsj_veto45` (v22), still theirs. **A strong solo effect (z=+5.44) with zero assembly value, now demonstrable rather than shadowed.**
- **⚠️ THE 23 RECOVERY ROWS ARE UNTAGGED, by our own timing.** Ingested at 00:33; the column shipped minutes later; `INSERT OR IGNORE` does not backfill. **For those rows ONLY, NULL means "arrived early", NOT "newest-first drain"** — identify by `decided_at` 00:32:40–00:33:12 or `fullhist_refit_of`. Recorded in D376 and the index.
- **The freeze premise held all the way down.** Generation was never the constraint: triage was, the stamp was, both are fixed and measured. The declaration (DRAFT, awaiting operator) says so in §4.
- **NEXT, and it is not ours:** the 00:02 selection-PBO restamp over 26+ promotes — how many survive real sibling-family deflation, and whether the first stage-two promote in history crosses into QuantIQ's import stream.

## 2026-08-06 — Step E tranches 1–4 RESOLVED (operator "Let's do 1-4"): winner_prior DELETED (D372), alpha-budget RETIRED (D373), D287 reservation REMOVED (D376), E4 DECLINED (D377 — a waiting instrument), young_explore lane REMOVED (E6, D379, operator "deprecate E6"); D236 BACKFILLED; their dsj entry renumbered D376→D378 (third same-day number race)

- E1–E3 remove ~900 src/test LOC + one CLI command; all behavior-identical (empty pin set / dead flag / zero refs); ruff + mypy --strict + ranking/cli/invariants suites green. `config_cell` moved home to `campaigns.py` beside its dict twin.
- **E4 was the D361 error class caught pre-cut:** the "retired" paired-delta display is the instrument waiting on D284 hygiene-incumbent rows (accruing since 07-16) and `sequential_test.py` serves the live rewire clock — declined with D377; re-propose only after the hygiene-incumbent read.
- **Restart DEFERRED by design:** the contracts pin moved 1.42.0→1.44.0 mid-session (concurrent session, their D374, pin-only); E1–E3 are dead-code removals needing no urgency, so they ride the pending 1.44.0 both-directions restart window. ⚠️ Until then the tree is ahead of the running daemon (normal short-window state; timers spawn fresh processes and are suite-verified consistent).

## 2026-08-06 (earlier) — repo-simplification Steps 0–C EXECUTED (operator: "let's attack the plan") — 11 commits, zero behavior change; D–F stay operator-gated (D371)

- D236 BACKFILLED verbatim into the D201–D300 slice (written on the v23 branch, lost in re-merge `2f2748f` — not a numbering race after all). Root `.md` 34 → 9; `docs/proposals/` 39 → 20; scripts 39 → 17; session ledgers 1.42 MB → ~450 KB (rotations to `_archive/`); DESIGN.md de-fictioned + §3.5 drift banner (commit `9afe042`, **operator review requested**); glossary → `architecture.md` §Terms; 22 spent scripts retired with MANPAGE ledger rows. Full detail: D371.
- **Owed/flagged:** Q62 (QuantIQ six-stream ranker triage — filed to `freeze/relays/`, unanswered); contracts **1.43.0** shipped sibling-side mid-session → `test_expected_contract_version_matches_installed` fails until the operator-gated pin adoption + restart; STATUS-block-length discipline (plan §4.3) awaiting operator word.

## 2026-08-06 (earlier) — repo-simplification plan DRAFTED (operator ask: "too expansive, AI making mistakes") — `docs/proposals/repo-simplification-2026-08.md`, awaiting operator sequencing

- Five-track audit (root/docs/src/scripts/tests) → phased plan: Steps 0–C are docs/records hygiene (no restart, no behavior change), D–F operator-gated; Step F1 = execute `fable-audit/code-complete-retirement/REPORT.md` on freeze declaration.
- Found in passing, needs triage: `PROMPT_QUANTIQ_new_training_signals_for_the_rankers_2026-08-03.md` is an **unhandled inbound relay** (untracked, zero references anywhere in Forge); `scripts/joint_frontier.py` untracked; 4 ACF relays in `_archive/` untracked with stale "NOT YET SENT" banners.
- Nothing executed; no code, config, or service touched.

## 2026-08-06 — **WE HAVE NOT HIT THE CEILING — and the binding constraint is refit TRIAGE, not generation.** 14 of 23 configs clearing BOTH binding gates were never refit. (D368)

- **THE CEILING QUESTION, ANSWERED.** "Flat" and "reached" are different claims and (C) only tests the first. Record progression (distribution-free: records arrive at rate 1/n, so E[count] = ln(n)+γ): ranked lane **13 vs 12.58 expected, z=+0.13** — exactly the unbounded-search rate — still climbing through the gate to **+1.7397 on 08-03**. Better selection reaches a ceiling faster, it cannot exceed one. **(C) reads the top-decile mean at rank ~120-from-top; records live at rank 1** — the bulk tail stopped moving, the extreme tail did not, exactly the blind spot (C) names in its own text.
- **WE HAD BEEN MEASURING ONE AXIS OF A TWO-AXIS WALL.** Promotion needs cpcv **and** `walk_forward_sharpe_median` (stage-one pass 0.00% and 0.49%; `wf_sharpe_p25`/`p10` admit 100% — three WF gates, easily conflated). Joint frontier, permutation null over the fixed point set: **73 advances vs 37.0, z=+3.60, p=0.003 — STILL ADVANCING.** Honest arm stationary, consistent with leg 1.
- **THE REAL FINDING IS ONE STEP PAST THE FRONTIER.** `23` stage-one configs cleared **both** binding gates, **all 23 identical** (`reject`, failing `deflated_sharpe` + `regime_coverage`). **9 refit → 9 components. 14 never refit.** Latency median 0h / **p99 2h** / max 3h over 36,061 pairs, and 13 of the 14 are 1–5 days past decision — **passed over, not queued.** **61% of our best-ever supply never entered the only lane that can make a component.**
- **NOT CLAIMED:** that the 14 are better. 9/9 is consistent with the lane's ~80% base rate (p=0.13). Claimed only: identically eligible.
- **OUR OWN FIRST COUNT WAS WRONG AND WE CAUGHT IT BEFORE RELAYING.** We said 24-of-33; `measurement_basis` is **0% populated before the week of 07-20**, so older refits are invisible to us as stage two. Unverifiable, not un-refit. Same class as the D360 pooled-conversion artifact.
- **PLAN REORDERED.** Probing untested surface (the `rv_rank` blind zone, 19 dark indicators) is **deprioritised** — adding surface while losing 61% of what already clears is the wrong problem. Highest-value item is Crucible-side and **relayed**: is newest-first refit ordering deliberate under the doubled capacity, when stage one has already computed cpcv and WF before the scanner chooses?
- **This STRENGTHENS the freeze** — more evidence the binding constraint is not generation. Declaration §4/§8 updated.

## 2026-08-03 — **the D216 ve floor is RETIRED.** Its founding evidence was retracted by Crucible's ghost close-out, and the v39 repair it was held open for did not work. `young_explore` stays OFF — 87% of its budget would have funded the same family. (D367)

- **FOUNDING EVIDENCE WITHDRAWN.** The floor existed because Crucible validated ve on 06-29 as **the second factor** (PC1 load 0.10, book at CSCV PBO 0.107). Their **07-19 close-out retracted it**: 23/25 stored-cpcv ve components were **ghosts**; clean-cache PBO came back **0.40**, "real-but-MARGINAL, no solo promotion case."
- **THE REPAIR DID NOT WORK — the new fact.** ve runs 2–10% conversion at median cpcv **+0.20…+0.33** through v21 (*that is the ghost era — the retracted evidence*), then 0–1% at median **−0.23…−0.53** from v22 on, and stays there through v39: **v39 0.40%/−0.439, v54 0.10%/−0.413**, indistinguishable from pre-repair v38.
- **ASSEMBLY AXIS CHECKED, NOT ASSUMED.** D361 says a solo statistic cannot condemn a family — our swing_long error the same week. So: **0 ve legs in 7 books / 20 legs, 0 ve rows in 12 contributions.** swing_long is bad-solo/**7-of-7**; ve is bad-solo/**0-of-7**. That contrast is the argument.
- **UN-PROPPING, NOT PRUNING.** The floor was a max-*normalized* weight delivering ~10–12%. Removing it returns ve to its learned weight **plus the D067 5% exploration floor** — still samplable, per-cell question deliberately left open (each ve cell has only **3–24** honest verdicts, the `_UNEVALUATED` state we refuse to prune on). One env line to revert.
- **THE FALSIFIER TESTS HOMOGENEITY, NOT CONVERSION.** Predicting a conversion rise would be predicting arithmetic (0.34% family → 22.5% families). D216's real concern was homogeneity, which the freeze **leg-2 TCM-corr** measures directly. Prereg **`d4b1efd26bb3`**: TCM-corr must not rise >**0.0173** above its pre-cut max **0.4457** — the bar *reused* from `13e4d2cece3f`, not refitted, because refitting on the judged data is the D357 defect. Read ~2 days.
- **COUPLED DECISION: `young_explore` stays OFF.** Of 42 young cells, **22 are ve and 87% of young-cell flow is ve** — they stay young because ve's P(cpcv|submitted) is ~21% vs trend's ~73%. Flipping it would have spent ~576 configs/day exploring the family we just declined to over-supply. The coherent pro-ve alternative (keep floor **and** flip lane under a dated prereg) was considered and not chosen.
- **Deploy:** preflight **2,155 passed / 1 skipped**; stop → commit → daemon-reload → start. **Not a promotion claim** — a cost decision reclaiming ~5–7% of enumeration.

## 2026-08-03 — **v55: the Q46 vix_term_slope conditioner is RETIRED** (share zeroed, not deleted) on Crucible's refutation and our own reproduction of it. And the hot-grammar-read hazard fired a **second** time — zero contamination, by luck again. (D366)

- **THE EVIDENCE, both sides.** Crucible closed Q46: the hurst×vix double-gate was proposed as a **decorrelation** mechanism and measures **more** correlated — median `|corr|` **0.3782 vs 0.3564** (n=1,335 vs 118,189). **We reproduced it before accepting: 0.3682 (n=1,172) vs 0.3520 (n=109,384).** Their pinned pool-entry metric was separately unmeasurable (~1,506 days to ONE expected entry), so decorrelation was the only axis that could decide.
- **ONE CONSTANT: `_VIX_CONDITIONER_SHARE` 0.125 → 0.0.** Zeroed rather than deleted, and both halves matter. *Determinism:* the draw site short-circuits, so keeping the predicate keeps the `rng.random()` consumption exactly in place. *Reversibility:* their reading is correlation to **one** reference book and they said the door is not nailed shut — a constant is a one-line revert, a deleted path is not. The reclaimed **~20 configs/day are REDISTRIBUTED**: the regime-veto branch is now reachable for configs that previously took the conditioner.
- **NO GOLDENS RE-PIN, verified not assumed.** All 210 sampler goldens pass untouched — their minimal fixture never served vix as a trend gate, so the predicate short-circuits and the cold path is byte-identical (hard rule #6), exactly as the v44 design comment predicted.
- **EMISSION PROOF, live registry, 3 seeds × 8,000:** 0 conditioner signals, 0 double-gates, **1,231 vix-as-PRIMARY draws still emitted** — scoped to the conditioner, not the indicator, and that scope is a test.
- **A TEST OF MINE WAS WRONG AND THE SUITE CAUGHT IT.** My first emission guard keyed on the *indicator* id, banning `vix_term_slope` outright and contradicting my own scope guard. Re-keyed onto the conditioner's **signal id**. Three v44 tests **inverted, not deleted** (v52 precedent); the share-rate test's eligibility denominator is now the load-bearing half, proving the zero is a real non-firing rate rather than an empty denominator passing quietly.
- **⚠️ THE HOT-GRAMMAR-READ HAZARD FIRED AGAIN.** CLAUDE.md forbids editing `grammar.yaml` in the live tree while the service runs. I did. The daemon re-read it hot and **6 iterations stamped `grammar_version=v55` while the process still held the v54 sampler** — any batch shipping then would have been stamped v55 but enumerated under v54 semantics, the [[D340]] incident exactly. **ZERO CONTAMINATION (0 v55 rows all-time)** because §7.3 backpressure held the stream at 72.9% gated throughout. **That is luck, not design — the same luck that saved D340.** For `grammar.yaml` the tree IS the live config: the edit must land on a branch, or the stop must come first.
- **Prereg `c14fa12cd4da`** predicts on **surviving** flow, not the removed cell's own rate (the D363 defect): 0 post-cut double-gates AND xsect-trend cpcv median not falling >0.02 below +0.1980. Required n stated at registration — 3,000 rows, ~0.6 days at 4,414/day.
- **Deploy:** preflight **2,155 passed / 1 skipped**; stop → commit → restart. **NOT a promotion unlock.**

## 2026-08-03 — **SOLO cpcv CANNOT RANK BUCKETS.** We argued swing_long was a component factory, not a promotion source — it is in **7 of 7 promoted books** and is the exclusive trend carrier. Correct measurement, wrong quantity. (D361/D362)

- **THE CORRECTION WE TOOK.** Crucible fixed our stage-one "0.0% by construction" identity: true for the 1825d buckets, **false for `swing_long`**, which runs a 2555d window, starts near the data floor, and converts **28.1% (5,726/20,402)** at stage one. We derived the identity from `mean_reversion` — ~96% swing_mid — and promoted a bucket-specific fact to a structural claim, **after they had corrected us on the per-bucket 5y/7y windows two days earlier**, a correction living in our own chain-inception code.
- **THE LARGER ERROR.** We then argued against their "keep swing_long" on a same-basis comparison: stage-two components at cpcv ≥1.0, **0.61% vs swing_mid's 5.62%** (expected ~165, observed 13), and **0 of 8,221 swing_long components ever clearing 1.5**. Those numbers are right. `promoted_portfolios_2026-08-03T230326Z.json` — **in our own exports directory, which we had been reading all week** — shows **7/7 promoted books carry a swing_long leg and 8/8 of those legs are `trend_continuation`**. Both books QuantIQ has traded are swing_long-anchored.
- **WHY THE BAD SOLO NUMBER IS THE REASON IT WORKS.** swing_long is the **decorrelating** leg. Its long window and slow cadence are simultaneously why its solo cpcv is mediocre and why it is what the MR sleeve is measured against under §8.7, where `mean_pairwise_correlation` and the drawdown family dominate. Ranking it by solo cpcv asks it to be a good MR component. **This is [[D186]]/[[D187]] — decorrelation is owned at ASSEMBLY — already a standing rule here, written after a previous version of this same mistake.** Crucible's `write-probe-script.md` uses swing_long as its worked example of the identical error; both principles pre-existed and both sides still needed the other to catch this instance.
- **POSITION: both bucket recommendations withdrawn, theirs and ours. No supply-composition change; staying at ~34,300/day.** Neither side holds a statistic that ranks buckets on the promotion axis — so supply composition is **not currently an actionable lever**, which is worth more than the recommendation we nearly acted on.
- **THE RELAY QUEUE WAS A FICTION (D362).** We tracked outbound relays as "N unsent" and reported it every turn; **every one had already been answered**, several the same day. The shared repo *is* the transport — committing is delivering. Crucible's `INDEX_crucible_answered.md` exposed it; we reciprocated with **`INDEX_forge_answered.md`**, recording what we have handled plus the standing obligations in both directions. Not every relay needs a reply; one with no ask is closed by its D-entry.
- **Transferable:** a correct measurement of the wrong quantity is **worse** than a wrong measurement, because it looks decisive. Before a supply statistic decides anything, name the axis the decision runs on and check whether an artifact already answers it directly.

## 2026-08-03 — **THERE IS NO MR CONVERSION COLLAPSE.** The 27%→11% fall I reported was a pooling error across measurement bases; conditional conversion is FLAT at ~79%. The v52 capitulation retirement STANDS and its prereg is resolved `confirmed`. (D360)

- **WHAT I GOT WRONG.** Investigating the capitulation cell I measured pooled MR conversion falling **27.19% → 11.01%** across the v52 cut and called it a probable prereg falsification. `0a5ddc861aae`'s registered action on a material conversion fall is **REVERT THE RETIREMENT** — so acting on that reading would have undone a correct prune.
- **WHY IT WAS WRONG.** Component admission comes **only** from the `fullhist_refit` lane; the `standard_window` 5-yr screen converts at **0.0% BY CONSTRUCTION** (D330) — `regime_coverage` demands the window start within 30 sessions of the data floor, and a 1825d rolling window starts ~900 sessions after it. Confirmed as an **identity**: the gate's pass rate equals the share starting ≤30 sessions, exactly, every version. **Conditional on stage two, MR conversion is flat: 79.9% → 78.9% → 78.9%.** Only the stage-two *share* moved (37.1% → 14.7% → 11.5%) — the known D330 feed constraint, Crucible-side. **Pooling a 0%-by-construction screen with a 79% refit lane makes the pooled number a function of lane mix.** That is the D337/D338 collider rule I enforced on the freeze legs all day and then missed on conversion.
- **FOUR CONTROLS, all exonerating the retirement.** (1) Our MR supply quality **ROSE** — cpcv_p25 median +0.298 → +0.327, WF +0.847 → +0.853, sharpe_baseline +0.846 → +0.886. (2) The retired cell converted at **0.00%** over 122 ranked configs and was ≤2.1% of MR flow. (3) Control hypothesis `trend_continuation` **held**. (4) Gate mix flat within 1pp, every cell moving together, reject-reason mix unchanged.
- **PREREG `0a5ddc861aae` RESOLVED `confirmed`.** Emission leg confirmed (0 momentum-MR post-deploy; the 3 post-cut rows are v51-stamped and pre-restart; a 20,000-draw v54 probe returns 0). Conversion leg confirmed on the correct basis. **Metric-B leg explicitly recorded as NOT-YET-READABLE** — the census's 14d flow window still holds 312 v51-era rows until ~08-14 — and counted as evidence for neither side.
- **SECOND CORRECTION.** I had reported capitulation as an **open operator decision** blocking condition (A). It was not: **D340 retired it on 2026-07-31**, with Crucible confirming the close-out. **(A) is not blocked by a decision** — it is blocked by the same 14-day window artifact and resolves itself ~08-14. The census-protection fix I proposed would have re-protected an exemption that no longer exists; it was **not made**.
- **NO GRAMMAR CHANGE WAS MADE** in this thread, and none is needed.

## 2026-08-03 — **honest-arm rate REVERTED 150 → 40** — the freeze clock has stopped, so the ramp that existed to run it is closed. Second consecutive ramp returned in the same window its question did. (D359)

- **WHY NOW.** [[D355]] ramped `FORGE_PREFILTER_SAMPLE_N` 40 → 150 for exactly one purpose: the honest arm is the freeze clock, and at 40 the six n=1200 windows take ~10 days against ~4 at 150. **Both legs are read and resolved `confirmed` (D358)** — the clock has stopped, and the ~31% ranked-production cost now buys nothing.
- **THE OBLIGATION WORKED, TWICE.** Both this revert and D351's were triggered by the ⚠️ note written **next to the variable in the unit file**, not by a ledger entry. A standing obligation kept where the operator sees it at the moment it binds is the difference between a ramp that closes and one that quietly becomes permanent.
- **WHAT IT BOUGHT, AND WHAT IT COST THAT WE DID NOT PRICE.** Bought: the reads landed 08-03 instead of ~08-09, for ~110 extra known-rejected submissions per batch for a day. **Cost: it made both preregs' a/b re-derivation caveat UNSATISFIABLE.** The ramp landed 08-02T09:00Z, essentially **at** leg 1's cut (08:24Z), so there is no post-ramp *pre-cut* data to fit on — the post-ramp data is the judged data. D355 deliberately timed the switch to land at 0.3% of post-cut rows **to keep the cohort homogeneous**, which was right for the cohort and exactly wrong for the variance fit. **Now written into the unit file: a rate change for a clocked prereg must land BEFORE the cohort cut, or the caveat cannot be honoured.**
- **Deploy:** preflight suite **2151 passed / 1 skipped**; unit-file change, so `daemon-reload` before `start`.

## 2026-08-03 — **FREEZE CONDITION (C) IS MET.** Both legs read once, on their registered slices, and both survive a bar **3.3× tighter** than the one they were judged against. (A) is now down to **one cell**. (D358)

- **LEG 1 (quality) — CONFIRMED.** Windows 11–16 `0.7483 0.7589 0.7478 0.7552 0.7426 0.7457`; best-of-new **0.7589** vs registered baseline **0.7548** = **+0.0041** against bar **0.0242**. The quality ceiling has **not resumed rising**.
- **LEG 2 (redundancy) — CONFIRMED.** Windows 12–17 `0.4298 0.4202 0.4378 0.4345 0.4310 0.4368`; max-of-new **0.4378** vs baseline **0.4411** = **−0.0033** against bar **0.0173**. The good supply has **not become more redundant**. Corr coverage **100% on all 17 windows**, reference basis fingerprint verified against the pin *before* reading.
- **THE BAR OBLIGATION COULD NOT BE MET AS WRITTEN — reported rather than dropped.** Both preregs required re-deriving a/b on post-ramp data first. The `SAMPLE_N` ramp landed 08-02T09:00Z, essentially **at** leg 1's cut (08:24Z), so there is **no post-ramp pre-cut data to fit on**: the post-ramp data *is* the judged data, and fitting there is exactly the defect D357's registered-bar flag prevents. So the read used the **registered** bar (a prereg forbids extension) and the re-derivation is published as a **sensitivity**: `b_up` 0.0121 → 0.0037, a post-ramp bar of **0.0074, 3.3× tighter**. **Both legs pass it anyway** (+0.0041 < 0.0074; leg 2 trivially at −0.0033 < 0.0130). **The result is not an artifact of a loose bar** — the one unrecoverable direction. Caveat on the sensitivity itself: 7 windows, `b` point estimate exactly 0.0000 (the "cannot resolve drift" signature), so 0.0074 is a **bound, not a measurement**.
- **NEW INSTRUMENT `scripts/freeze_registered_read.py`** — deliberately separate from the exploratory one, which re-fits its own floor every run. It **hard-codes what the preregs fixed and computes none of it**, reads *exactly* the registered windows (leg 1 takes 11–16 though a 17th exists — reading it is an extension chosen after seeing data), and **refuses** on a short or NaN-holed slice instead of rendering a partial verdict. Tests assert its constants against the prereg registry, so script and record cannot drift.
- **WHERE THE FREEZE NOW STANDS.** **(B) MET** — metric B **0.28%** vs a 1.00% bar, **14 consecutive runs** (bar requires 7). **(C) MET** — this entry. **(A) is the only open condition**, and its entire remaining surface is **one cell**: `mean_reversion / swing_mid / named / momentum / (nogate)` — the **capitulation cell**, 312 in 14d flow, 417 all-time, **0 components**. It was exempted at v47 with a defined close-out (*folds into a later prune if it fails its adoption episode **or a better decorrelated third leg surfaces***), and the timer-MR leg has since surfaced **and been promoted**. Whether that fires the close-out is an **operator call** — it is a grammar prune.
- **WHAT (C) IS NOT.** A **supply-ceiling** result. Both legs sit at rank ~120-from-top while promotion-grade events sit at rank ~0.4–5, so **a top-1%-only lift is arithmetically invisible to them.** "Flat" must not be read as "the grammar cannot produce a better component."
- **⚠️ NOW DUE: revert `FORGE_PREFILTER_SAMPLE_N` 150 → 40.** Both preregs are read; the ramp's purpose is spent. Operator-gated (service restart).

## 2026-08-02 — **THE DESIGNATION FLIPPED 08-01, ONE DAY BEFORE THE FREEZE LEG WAS REGISTERED.** The reference book stays pinned to the *retired* champion — and the number says why: switching would have printed a **−0.0458 unearned improvement, 2.6× the leg's own bar.** (D357)

- **WHAT HAPPENED.** The operator retired `aa31532489613849` for `f52a05c8968bdc7a` on **2026-08-01** (QuantIQ D306 — the old champion was infeasible at 11.48% NAV drawdown against an 8% ceiling). Leg 2 of freeze condition (C) measures `|corr to book|` against **`frozen_b36f49a4`**, which is the *retired* book's minted series. The flip landed **before** the leg's prereg cut (08-02T17:26Z), so the registered series was already read against one fixed reference: **nothing needs re-basing.**
- **THE PIN STAYS, AND IT IS PRICED — NOT ARGUED.** On the 2,497 configs carrying correlations to both books: per-config agreement **pearson +0.9582 / spearman +0.9531** (the choice costs essentially no ordering power), but the leg-2 **level** reads **0.4228** against the retired book and **0.3770** against the designated one. The **−0.0458 gap is 2.6× the leg's 0.0173 bar**, and it points **DOWN** — so a silent switch would have printed a large unearned *improvement* and biased the freeze toward being declared MET on a **reference change rather than a supply change**. That is the unrecoverable direction. Crucible independently recommended the same thing: a coverage-chosen yardstick should not chase the designation, or it inherits a re-base on every future flip.
- **GUARD 1 — the yardstick is now self-checking.** Crucible undertook to flag `corr_to_book` basis changes before shipping them. A promise depends on someone remembering, and **this week produced a defect of exactly that shape on each side** (our computed-but-never-passed keyword D352; their computed-but-never-persisted ablation value). So the instrument fingerprints the reference book's identity fields (`spec_note`/`window`/`n_days`/`weights`/`traded_unit`/`minted_at`), pins the hash **`ae47a4749c9d`** — stable across all five exports on disk — and **refuses to run leg 2 on any mismatch**, the same principle as the existing 50%-join refusal. A re-mint can move the level either way, so an unnoticed one could manufacture a **false PASS**.
- **GUARD 2 — the registered read may not set its own bar.** Left alone the instrument re-fits `a`/`b` on every run **including the windows under judgement**: leg 2's bar read **0.0173 at registration and 0.0210 one window later**, purely from refitting. **That is peeking wearing a formula.** `--leg1-bar`/`--leg2-bar` pass the registered values and label the output `REGISTERED`; without them the run prints **`EXPLORATORY — NOT the registered read`**. Resolution uses the prereg's **literal** baseline (0.4411): post-stratification re-weights past windows as the sample grows, moving them ~0.0002 (1.2% of the bar).
- **RELAY OUT (`1ca9451`, then this one).** We told Crucible to **stand down** on the periodic-export work they offered — `corr_to_book_*.json` already carries 116,329 per-run rows and our honest arm joins to it. We asked them to build something before checking whether it was on disk. They accepted, and answered the designation ask by **shipping an export rather than promising a relay**: `designation_history_<ts>.json`, history reconstructed from `config/designated_champion.yaml`'s own git log so it cannot drift from the record it describes.
- **CURRENT READING — EXPLORATORY, NOT THE READ.** Leg 1 **−0.0174** vs 0.0183; leg 2 **−0.0420** vs 0.0173. Both inside their floors. **Neither is a freeze decision**: condition (A) still has to hold, the registered read needs 6 new windows, and **both legs stay arithmetically blind to a top-1%-only lift** (TCM sits at rank ~120-from-top; promotion-grade events at rank ~0.4–5).
- **Ledger:** D355 (the 40 → 150 re-ramp and its bias direction) and D356 (leg 2's specification) were referenced in commit messages but never written — **backfilled here alongside D357.**
- **Standing obligations unchanged:** ⚠️ revert `FORGE_PREFILTER_SAMPLE_N` **150 → 40** once both preregs are read, and **re-derive the a/b fits on post-ramp data** before either read is treated as final.

## 2026-08-02 — **`/tmp` IS A 62 GB tmpfs AND THE LIVE DB IS 6.7 GB** — the house snapshot ritual has been eating RAM-backed storage; it took the shell down twice today and stalled the daily timer on 07-09. Fixed at the source, not detected. (D354)

- **THE MECHANISM.** `docs/tasks/investigate-live.md` said `cp ~/forge_data/forge.db /tmp/forge_snapshot.db`. `/tmp` here is **tmpfs — RAM — 62 GB**, and the live DB is **6.7 GB**. Nine investigation snapshots in one session = ~60 GB = a full tmpfs.
- **IT DOES NOT PRESENT AS "DISK FULL".** Every Bash call — including `true` and `/bin/echo` — returned **exit 1 with no output**, because the harness could not write its own output capture. It reads as broken tooling, which is what makes it expensive: it cost a mid-investigation abort **twice today**, and the second time I had to delegate a file search to an agent to finish a diagnosis.
- **⚠️ IT HAS BILLED PRODUCTION BEFORE, AND WE ONLY BUILT A DETECTOR.** `MANPAGE.md:553` records the **2026-07-09 stall**: `daily_ranker_eval.sh`'s `cp forge.db /tmp/…` failed on a full /tmp, F3 and wf_p25 **silently staled**, and the `model` healthcheck only CRIT'd **~2 days later**. [[D259]] responded with `tmp_headroom` (WARN below 5×, CRIT below 3.5×) — **a detector for a failure we could have removed.** The timer was still copying 6.7 GB into tmpfs every 05:00 until today.
- **FIXED AT THE SOURCE:**
  - **`scripts/live_db_snapshot.sh`** — one reusable snapshot, on real disk, path on stdout. **Refuses tmpfs/ramfs outright** (verified: `FORGE_SNAPSHOT_DIR=/tmp/... → REFUSING`), checks free space before a 6.7 GB copy, reuses anything younger than 15 min (ten queries cost one copy, not ten), tmp-then-rename, and `--clean`.
  - **`daily_ranker_eval.sh` moved off tmpfs** — `$HOME/forge_data/.snapshots`, PID-suffixed, existing cleanup trap untouched. **The 07-09 failure mode is now absent rather than alarmed.** D259's detector stays and now guards a surface that no longer routinely eats tmpfs.
  - **Every stale instance swept** — 5 in `MANPAGE.md`, plus `alpha_budget.py`, `tail_verified_alignment.py`, `search_multiplicity_census.py`, and the ritual doc itself. Zero `cp … /tmp` snapshot instructions remain.
- **THE TRANSFERABLE POINT:** a hazard documented in a *detector's rationale* is still a live hazard. D259 wrote down exactly why /tmp fills and then left the thing that fills it in place, in six documents, where it stayed for three weeks and re-fired today. **When an incident note explains a cause, fix the cause in the same change.**

## 2026-08-02 — **THE FREEZE CRITERION'S BINDING CONDITION IS UNEVALUABLE, NOT UNMET** — one of its two tail clearers was a basis violation, and it is specified on the statistic D341 proved cannot resolve. Plus: threshold-resolution measured, and the coarsening proposal REFUTED by its own feasibility check. (D353)

- **THE COARSENING PITCH IS DEAD — killed by the check that should have preceded it.** The plan was: directional threshold resolution buys nothing ⇒ coarsen the grid ⇒ cut `search_n_trials` ⇒ lower the DSR hurdle. **The first two hold; the third does not.** Rounding the directional threshold to 1dp collapses **1 config out of 95,376**; coarsening *every* continuous parameter aggressively still leaves **74.8%** distinct. **The n_trials treadmill is driven by structural composition, not parameter resolution** — quantising grids cannot touch the DSR hurdle. Found before a grammar bump, not after.
- **DIRECTIONAL THRESHOLD RESOLUTION BUYS NOTHING — a well-powered null.** Honest arm, stage one, within-cell (`scripts/threshold_resolution_value.py`): mean rho **−0.0032**, **0 of 21 cells clear their own MDE** (the largest could detect |rho| ≥ 0.09 and saw 0.02–0.03), top-decile permutation **1/21 against ~1.1 expected by chance**.
- **THE REGIME GATE IS THE REAL FIND — and the collider check is what made it usable.** Within a cell the sampler assigns thresholds at *random*, so this is an experiment, not a fit — **but only if the threshold does not also decide whether a config gets measured**. It does, for one of them:

  | gate | rho (2 independent cells) | observation rate across range | verdict |
  |---|--:|---|---|
  | `adx` | −0.226 / −0.190 (both clear MDE) | 78.2% → 76.1%, **5.1pp** | **clean** |
  | `vix_term_slope` | −0.354 / −0.234 | 75.9% → **50.7%**, **25.2pp** | **CONFOUNDED** |

  **The two largest correlations are the untrustworthy ones.** Both gates use `>`, so on `adx` a *tighter* gate means *worse* quality — replicated across two directional pairings. `vix_term_slope` must not be read causally.
- **⚠️ FREEZE CONDITION (C) — TWO DEFECTS, and the second is structural.** Re-read at n=11,932 (1.6× the recorded 7,484):
  - **(1) BASIS VIOLATION inside the measurement meant to enforce the basis.** `36c8aab4b0f6a360`'s quoted **1.6629 is its `fullhist_refit` (stage-TWO)** value; its stage-one row reads **1.1703, a reject**. The corrected stage-one honest-arm clearer count is **1, not 2** — and was 1 when written. The [[D337]]/[[D338]] error, committed inside the criterion that warns about it.
  - **(2) IT IS SPECIFIED ON AN UNRESOLVABLE STATISTIC.** Corrected series: **1 → 1 clearers** across a 1.6× sample. That is equally consistent with "exhausted" and "produces at a constant 0.008%". **[[D341]], written the same day, measured that detecting a doubling of P(cpcv ≥ 1.5) needs ~183 days/arm** — which is exactly why the A/B was specified on p90 instead.
  - **⇒ Neither "the tail did not converge" NOR "the tail is exhausted" is supported. (C) is UNEVALUABLE AS SPECIFIED, not unmet.** Reading a stall off a count of 1 is the same error we corrected Crucible on today.
  - **What would fix it:** re-specify the tail half onto a bar with observable events — **P(cpcv ≥ 1.0) carries 51 events (0.427%)**, p90 is 0.5944 and moves on ~3 days per D341. **Operator-gated: it changes what the freeze programme turns on.**
- **THE `vix_term_slope` WASTE CASE IS WEAKER THAN IT LOOKED, stated rather than pitched:** the high band (≥1.6) reaches a cpcv 45.1% of the time vs 66.2% below it, ~70 configs/day, so trimming it would save ~0.9% of throughput — **but 45% of it still produces**, unlike the chain-inception class where failure is guaranteed. That is a productivity trade needing a prereg, not a free waste cut.

## 2026-08-02 — earnings-coverage thread **CLOSED** (no asks either side): `uncoverable_upstream` shipped, ABNB/ARM reclassified, and VIXY is the first hole our stopgap has that the manifest closes

- **`uncoverable_upstream` LIVE** in `earnings_covered_symbols_2026-08-02T053208Z.json` (149 covered): **ABNB ARM ASML BRK.B V**. Crucible measured it at the source rather than hardcoding, so a Polygon backfill drops a name automatically. **No Forge code change and none owed** — an uncoverable name is already absent from `covered_symbols`, so the intersection excludes it today; the key is diagnostic. We said so rather than wiring a consumer to justify having asked, and they preferred that answer.
- **OUR OWN CLASSIFICATION WAS WRONG AND IS FIXED:** we had ABNB/ARM filed as *short-history* exclusions. They are **upstream-uncoverable** — Polygon publishes no EPS for either at any timeframe. Right answer (pool 88), firmer reason. ASML/BRK.B confirmed outside our 118-name draw universe.
- **⚠️ DO NOT COLLAPSE THE TWO FILTERS.** ABNB/ARM sit behind chain-inception (**temporary** — ARM frees ~2028 as the window slides) *and* earnings-coverage (**permanent**). Removing one because "the other covers it" reopens a hole on the expiry date. Crucible has written this into `docs/decisions/eps_coverage_upstream_gap.md` because **they publish both exports** and are the party able to make the mistake.
- **VIXY IS ABSENT FROM OUR `_NO_EARNINGS_UNDERLYINGS` STOPGAP** — surfaced by their ingest hygiene (VIXY/SVXY were returning their *issuer's* filings, the `XLF → State Street` hazard). It costs nothing today (not in our universe) but is **one universe-widening away** from reopening the SOXL degenerate-leg pathology, which is exactly what D268's durable fix exists to prevent. **First concrete evidence the v32 manifest wiring closes something the hardcoded list does not** — and it came from their side, not from our list review. Strengthens the eventual `_NO_EARNINGS_UNDERLYINGS` deletion (operator-gated cleanup bump, not now).
- **STANDING CROSS-BOUNDARY RULE, adopted by both sides:** *whoever holds the ground truth does the measuring.* Two wrong predictions today in opposite directions — they predicted into our pool from their manifest ("recover to ~97"); we inferred their window semantics from our failure counts (flat 5y). Each was caught by the other side checking against data the predictor did not hold. Same division that makes the floors export theirs and the emission proof ours.

## 2026-08-02 — **v54 DEPLOYED 04:58:40Z — because v53's filter WAS NEVER LIVE.** One missing keyword argument; every gate we ran was blind to it, and the journal line we wrote is what made it look shipped. (D352)

- **THE DEFECT.** `_run_one_iteration` computed `below_inception`, echoed it to the journal, and **never passed it** to `_run_battery_for_seed`. The parameter took its `None` default → **enumeration ran unfiltered from `fa00daf` through the entire v53 deploy**.
- **WHY IT SURVIVED A FULL DEPLOY RITUAL — keep this, it is the transferable half:**
  - the 13 unit tests exercise `underlyings_below_inception` **directly** → they prove the predicate, never the call graph;
  - the **emission proof passed `below_inception` explicitly into a test harness** → it proved the FUNCTION works, not that production reaches it. A proof built around the path production does not take;
  - the **journal line was the trap**: `chain_inception: excluding 22 underlying(s) (…)` printed the resolved set faithfully, every batch, while nothing was excluded. [[D185]] says verify the FEATURE in the journal; the sharper rule is **verify a filter by its EMISSION through the production path — a log line is not evidence.**
- **CONTAMINATION MEASURED: v53 = 960 submissions, 96 single-name, 10 on excluded names** (LCID ×4, UVXY ×3, SQQQ, RTX, COIN). **Nothing was actually burning cycles** — the 7y-class names appeared in `swing_short` where they are legal, and LCID/COIN sit inside the 183-day margin zone. The only thing lost was the feature.
- **⚠️ v53 IS A VOID COHORT FOR THIS FEATURE** — stamped as the chain-inception version, emitting like v52. **The floors comparison is `funnel --compare v53 v54`, NOT v52-vs-v53**; v53's 960 rows belong with v52 for that purpose. Owed to Crucible.
- **WHY A BUMP, not a versionless fix:** emission changes here, so hard rule #6 (versionless ⇒ cold-start byte-identical) forbids it, and an unversioned fix would split the v53 cohort at an unrecorded timestamp.
- **THE DURABLE GUARD:** `tests/invariants/test_enumeration_inputs_reach_the_battery.py` — any local in `_run_one_iteration` sharing a name with a `_run_battery_for_seed` keyword must be forwarded. Static (`ast`) because the defect is a **missing edge in the call graph**; an emission test would need the whole daemon path to see what parsing proves in milliseconds. **Verified RED against the exact defect, then GREEN**, and its class sweep reports only this one name. It guards every future enumeration input.
- **CRUCIBLE'S FIRST-LOOK §1 IS WRONG, and their §2 curiosity was the real signal** (their `aac80d7`). They read RIVN 38→0 / CEG 19→0 / ARM 27→0 as the loop closing. At their own sample — ~820 arrivals, ~9% single-name ≈ 74 configs over ~118 names — **the per-name expectation is under 1**, so three zeros are noise. **LCID ×3 against an expectation of ~0.6 pointed the other way.** The names they filed as a harmless curiosity were the tell; the names they used as proof could not discriminate.
- **LIVE:** `active/running`, NRestarts=0, `grammar_version=v54`, `registry_loaded_from_export`, no traceback. Suite **2,135 passed / 1 skipped**. Contracts **1.41.0 → 1.42.0** adopted in the same window (`deployment_sizer_modes`; same pin-only class, re-verified additive).
- **✅ EMISSION PROOF LANDED — the filter is LIVE, verified through the production path** (not the log line):

  | | single-name | **bucket-aware** violations |
  |---|--:|--:|
  | v53 (inert) | 125 | **5** — LCID ×4, COIN ×1 |
  | v54 (fixed) | **145** | **0** |

  **CAVEAT NOW CLOSED (re-read at comparable n):** v54 reached 145 single-name with **zero** violations against v53's 5-in-125. Under v53's observed 4% leak rate, P(0 in 145) ≈ 0.0027 — the earlier "0 in 28" reservation is discharged.

  Plus a full `forge run --dry-run --max-iterations 1` against a scratch DB copy: production path, v54, filter resolved 14/14/22 per bucket, 200 ranked, no submit.
  **CAVEAT, stated rather than buried: n=28 single-name is small.** Zero violations is consistent with a working filter but is not by itself overwhelming, and per-name draw weights (D105/D106) make a naive binomial the wrong null. The load-bearing evidence is the **red→green invariant test against the exact defect** plus the `ast` confirmation of the call site; emission is corroboration that will firm up with volume.
- **⚠️ THE BUCKET-vs-UNION CONFUSION BIT A THIRD TIME — mine, in the checker.** My watcher tested submissions against the **union** of all 22 excluded names instead of `for_bucket()`, and duly reported `STILL LEAKING` on SQQQ/swing_short and LYFT/swing_short — both **swing_long-only** exclusions, both legal in swing_short. A false alarm that would have triggered a needless third deploy. Same shape as the flat-5y inference (D351) and as Crucible's underpowered zero-count read: **the per-bucket rule has to be carried by every consumer of the exclusion set, including throwaway verification scripts.**
- **⚠️ THE STAMP-FLIP HAZARD FIRED AGAIN** (grammar.yaml edited in the live tree, second time today). Next bump: worktree, or stop first.

## 2026-08-02 — **v53 DEPLOYED 03:17:49Z** (chain-inception floors + A/B teardown + honest-arm ramp revert, one window) (D351)

- **LIVE AND VERIFIED.** `active/running`, **NRestarts=0**, `grammar_version=v53`, `registry_loaded_from_export` (`registry_hash=03f0119f01df54e7`), reconcile line present, **0 traceback / SchemaVersionMismatch / GrammarVersionError**. Env confirmed IN THE PROCESS, not just the unit file: `FORGE_PREFILTER_SAMPLE_N=40`, `FORGE_GENERATION_ARM_B_SHARE=0`. Full uncontended suite **2,133 passed / 1 skipped**.
- **SHIPPED IN THIS WINDOW:** (1) chain-inception floors, **per-bucket** (1,825d short/mid, 2,555d swing_long, 1,825d default) — emission proof 221 → 0, single-name supply 3,573 → 3,547; (2) `SAMPLE_N` 150 → **40**, the D341 ramp obligation discharged in the same window its experiment closed; (3) `ARM_B_SHARE` 0.5 → **0**, the registered falsifier action for [[D350]]; (4) contracts pin **1.40.0 → 1.41.0**.
- **CONTRACTS 1.41.0 ADOPTED (Crucible `62ed159`) — the preflight forced it, and it was verified additive rather than assumed.** Adds `selector_spread_bind: bool = False` to `PromotedPortfolio`. Pin-only: the diff is models.py + tests + version, the field is a DEFAULTED bool (absent == False == every pre-existing book), and **Forge never CONSTRUCTS a PromotedPortfolio** — we only read the export. Explicitly NOT the [[D261]]/[[D342]] hazard class: that class is a new Literal/enum VALUE, and this bump adds no vocabulary.
- **⚠️ THE STAMP-FLIP HAZARD FIRED, AND COST NOTHING — but only by luck of the limiter.** Editing `config/grammar.yaml` in the live tree while the daemon runs is exactly what `docs/tasks/deploy.md` forbids. The old process re-read it hot and recorded the `grammar_versions` v53 row at **03:01:13Z**, ~14 minutes before the code landed. **Zero configs were submitted under it** (checked, not assumed: `v53 submissions = 0`; its last emissions were v52 at 02:57:59Z) because §7.3 backpressure was blocking at 75.1% gated. `grammar_versions.changed_at` is now stamp-flip time, not deploy time — the documented property, not a defect. **Next grammar bump: edit in a worktree, or stop first.**
- **PRE-COMMIT DEFECT FOUND AND FIXED, and it was load-bearing:** both GRAMMAR hooks — including the **hard-rule-#10 version-bump scanner** — called bare `python`, which does not exist on this box's PATH. They died `Executable 'python' not found` on the first bump that tried to run them, meaning the guard has never actually executed. Both verified passing manually, then repointed at `uv run python` (the fix the mypy hook already carried). **A guard that cannot run is worse than no guard** — it reads as enforcement and blocks at the moment you most want a real answer.
- **Q60 RESIDUAL — PARTLY DISCHARGED, and the rest is not dischargeable by the CLI.** `f59812c7` was ALREADY `rejected` in `grammar_proposals` (no action needed). But **two 07-18 rows remain `pending` in the DB while REJECTED in the file** (`b6ede416` cpcv_sharpe_p25, `a64d0be2` walk_forward_sharpe_median — the D298 fossils). `forge grammar reject-proposal` returns "not found" for both: **its lookup is FILE-scoped and only matches PENDING entries, so an in-file decline permanently orphans the DB row.** Not fixed by hand — an unaudited manual DB mutation is precisely what the proposal flow exists to prevent. Inert (Q58: no auto-apply path), so this is a ledger inconsistency, not a hazard. Logged as the real shape of Q60.
- **NOT SHIPPED, deliberately:** the `young_explore` lane (D316 2d). Its configs stamp `selection_arm=None` and are invisible to the freeze criterion's honest-arm analysis; flipping it against two other mix changes in one restart would make its effect unattributable. Own window.
- **OWED TO CRUCIBLE:** relay the v53 string + deploy timestamp for `funnel --compare v52 v53` (`crucible-handoff.md`), plus the **Visa coverage-manifest gap** ([[D349]]).

## 2026-08-02 (**GENERATION A/B RESOLVED: REFUTED.** The book-usable weighting does not beat the incumbent map — and the favourable interim did not replicate, it **reversed sign**. Reading post-cut is what caught it.) (D350)

- **PREREG `4e369b779ca9` → REFUTED** (`forge prereg resolve`, post-cut evidence only — the tool enforces it). Single read at the registered gate, no peeking, no extension.

  | | baseline (arm A) | book_usable (arm B) |
  |---|--:|--:|
  | decided honest stage-one | 2,193 | 2,209 |
  | cpcv-bearing subset | 1,438 | 1,494 |
  | median cpcv | 0.1678 | 0.1700 |
  | **p90 cpcv (PRIMARY)** | **0.6028** | **0.5988** |
  | P(cpcv ≥ 0.9439) (secondary) | **0.97%** | 0.60% |

  **p90 delta = −0.0039 (arm B WORSE), bootstrap P(delta ≤ 0) = 0.5210** over 2,000 seeded resamples. Registered bar (delta > 0 AND P < 0.05) fails on **both** clauses. The secondary agrees: arm B is worse on the floor rate it was actually built to move (z = +1.14 toward baseline).
- **⚠️ THE INTERIM DID NOT REPLICATE — IT REVERSED.** The disclosed pre-registration peek at n=1,123/arm read p90 0.6348 vs 0.5625, **delta +0.0723, bootstrap 0.001**. On the clean unpeeked cohort it is −0.0039 at p=0.52. **A +0.0723 "highly significant" effect became a coin flip on fresh data.** This is precisely the failure the late registration was flagged for, and the ONLY reason it surfaced is that the operator chose the post-cut basis over the cumulative one — the cumulative read would have folded the peek back in and likely confirmed.
- **FALSIFIER ACTION (registered in advance): revert to a single map.** `FORGE_GENERATION_ARM_B_SHARE=0.5 → 0` at the next restart. **The frozen-weights forward test is NOT triggered** — that was the follow-up for a win, and there is no win to defend.
- **⇒ `FORGE_PREFILTER_SAMPLE_N` 150 → 40 IS NOW UNBLOCKED.** The experiment that time-boxed the ramp is over; the ~31%/day of forgone ranked production has no remaining justification.
- **TOOL FIX, and it was load-bearing:** `scripts/production_by_group.py` had **no cohort-cut filter** and did not compute the prereg's registered statistic at all. Running it as-shipped would have produced the *cumulative* read — the basis we rejected. Added `--since` (cut) + `_bootstrap_p_delta_le_zero` (2,000 resamples, `SeedHierarchy` per hard rule #8) and factored `_percentile` out of `_stats` so the reported p90 and the bootstrapped p90 cannot drift apart. **A resolved prereg nobody can re-run is not evidence.**
- **BASIS NOTE, stated rather than buried:** the registered gate is "2,000 **decided**" and that is what was tested (2,193 / 2,209). The cpcv-bearing subset is ~1,450/arm because rejects without a stored cpcv cannot contribute to a p90 of cpcv. Waiting for 2,000 *cpcv-bearing* would have been extending an experiment whose answer was already visible — explicitly forbidden by the resolution rule.

## 2026-08-02 (**THE MANDATE PICKS `f52a05c8` AND THE CHAMPION IS INFEASIBLE** — QuantIQ's D306 ran same-day; the flip is at the operator, and the champion trades **Monday 08-03**) (D348)

- **QuantIQ D306 RESULT (`d306_scalar_read_2026-08-01.json`), objective sharpe, ceiling `DEFAULT_MAX_DD_CEILING_PCT = 8.0` %-of-NAV:**

  | | split | tandem Sharpe | tandem DD of NAV | verdict |
  |---|--:|--:|--:|:--|
  | champion `aa315324` | 0.9665 | 1.777 | **11.48%** | **INFEASIBLE** |
  | `f52a05c8` | 0.6792 | **1.899** | 6.81% (1.19pp margin) | **FEASIBLE → PICKED** |

  It wins **both** axes — the only feasible configuration, and its quantized-sleeve Sharpe beats even the champion's most favourable measured arm ($25K, 1.863 < 1.899).
- **⚠️ STRIKE FROM OUR RECORD: "the champion holds the mandate 27.86 vs 21.54" (D347) IS WRONG.** That is the retired hand-computed ratio, which Crucible killed as prose three relays ago and we then re-imported. **The coded mandate is a DD BUDGET, not a ratio** — and a *static capital split* maps card-DD through it **linearly**, which is a different object from the vol-target dial. Corrected in place below.
- **OUR ARGUMENT DID NOT WIN THIS AND WE SHOULD NOT CLAIM IT.** The scale-down claim was refuted on the **vol dial** and stays refuted — Crucible's own words: *"if the champion falls, it falls to budget arithmetic — not to any claim you made or we refuted."* What our relays did contribute is narrower and real: surfacing that `c52c1ab3` had **never been compared**, and the **like-for-like unit find** (cpcv-p25 0.0543 → 0.0007 was the overlay, not an axis). The decisive arithmetic is theirs.
- **Their pre-registered lot-floor read** (honest fills, real NBBO, identical rig): `f52a05c8` **1.899** at its $14.9K rail sleeve vs champion **1.777** at $21.3K — equal quantization cost from $25K (−0.084 vs −0.086), more trade breadth, lower same-basis NAV drawdown (6.8% vs 11.5%).
- **NOTHING IS FLIPPED, AND THE SEQUENCING IS AN OPERATOR CALL WITH A CLOCK.** Import, triple-gate, champion retirement and pinning the 0.68 split are QuantIQ-side steps triggered only by the **operator's designation record**. **The champion trades Monday 2026-08-03, both legs due**; flip-vs-that-pass is explicitly the operator's decision.
- **Our §4 handicap worry is ANSWERED and it eases:** the search lane is **PASS-not-BEAT by §20 design** — books promote on §8.7 alone, roster append-only, and no routing ever keyed on the champion gap. The handicap cost **narrative, not routing**. Our projection caveat on "1 of 82" and the recall-label refinement were both adopted at the width we gave them.
- **Open on Crucible's side, not ours:** QuantIQ asks them to persist the four daily curves (`probe_designation_25k_read.py:105` discards `_e`) for curve-level confirmation, which is also their standing **CR-3** from 07-20. No Forge action.

## 2026-08-02 (**7th BOOK — and the champion comparison was never like-for-like: the champion has ALWAYS been tail-OFF while all four search books carried the tail. At matched unit the champion's cpcv-p25 win collapses 0.0543 → 0.0007.** Ask 1 still does not reopen.) (D347)

- **`f52a05c8968bdc7a` PROMOTED 2026-08-01T21:04:19Z — the 7th book, and it is `c52c1ab3`'s exact three legs on the tail-OFF unit** (`91324e5d_dsjv45` + `ad566996` + `04c84871`). Verified in the export ourselves, not taken on trust; their ablation numbers reproduce to the digit.
- **THE FINDING, ours, from their own export: the traded unit was never matched.** `tail_leg` is **None** on `aa315324` (champion) and `664b137e` — the two hand-queued `explicit` books — and **present on all four search books**. So every search-vs-champion comparison ever made, including their 07-29 *"neither new book beats our champion,"* compared a tail-OFF incumbent against tail-ON challengers. Neither side caught it; we argued three relays inside it.
- **AT MATCHED UNIT (both tail-OFF) — champion `aa315324` vs `f52a05c8`:**

  | | champion (n=2) | f52a05c8 (n=3) | |
  |---|--:|--:|---|
  | cpcv_sharpe_p25 | **1.7236** | 1.7229 | **dead heat — was a 0.0543 win** |
  | walk_forward_sharpe_median | 2.3063 | **2.5378** | |
  | wf_sharpe_p25 | 1.2722 | **1.3825** | |
  | wf_sharpe_p10 | −0.1369 | **+0.3658** | sign flip |
  | pbo / deflated_sharpe | 0.156 / 0.9991 | **0.1333 / 1.0000** | |
  | mean_pairwise_correlation | 0.0650 | **0.0127** | |
  | regime_stress_p25 | 9.59 | **40.20** | 4.2× |
  | total_return_vs_spy | 4.46 | **17.73** | 4.0× |
  | walk_forward_calmar_median | 9.19 | **11.45** | |
  | wf_max_drawdown_worst | **0.0828** | 0.1178 | |
  | cpcv_max_drawdown_p75 | **0.0837** | 0.1001 | |
  | Sharpe/DD | **27.86** | 21.54 | |

  **At matched unit the champion's advantage is drawdown and ONLY drawdown.** The cpcv-p25 axis — one of the three we named as champion-favourable — was a unit artifact.
- **ASK 1 STILL DOES NOT REOPEN, and we are not reopening it.** worst-DD is **byte-identical** across the unit (0.1178 tail-ON and tail-OFF), so the tail was never the drawdown story; their 30%-cut experiment measured the book's own path and stands. ~~Champion holds the mandate 27.86 vs 21.54.~~ **STRUCK 08-02 ([[D348]]) — that is the retired ratio, not the mandate. The coded mandate is an 8%-of-NAV DD budget; under it the champion is INFEASIBLE at 11.48% and `f52a05c8` is the pick. Ask 1 still does not reopen: the refutation was of the VOL DIAL, and a static capital split is a different object.**
- **CONSEQUENCE FOR THE WHOLE SEARCH LANE, not just this book:** applying the measured shift, `aa57f9f1` 15.3 → ~16.1 and `7f2a697e` 16.5 → ~17.3. The conclusion (champion holds) is **robust**; the *magnitude* of the gap was overstated for every search-lane book for its entire existence. This is a measurement correction, not a supply one.
- **THEIR RATE ANSWER — measured, and it lands where we hoped.** Of 82 tail-ON-era portfolio rejects, applying the shift converts **exactly one** (`3205c242`, sole failure cpcv 1.457). PBO fails 54/82 and worst-DD 13/82; the unit moves **neither**. So no unit-driven promotion wave (~1%). **One caveat we owe them:** the +0.054/+0.111 shift is a *single-book* delta extrapolated to 82 heterogeneous books — their conclusion survives it anyway, because the PBO/DD failures that dominate are unit-invariant at any shift magnitude.
- **THE RISE THEY DO EXPECT IS THE REACHABILITY REPAIR** (strong-base extensions enter the stream 08-02). Attribution rule, symmetric with ours: **a rate rise from 08-02 is lane repair, not supply.**
- **FLOOR UNCHANGED: 0.9439 stands** — `f52a05c8` admitted no new component (weakest admission-time cpcv **1.163**). **Commitment secured:** if it ever moves they publish **two era-scoped constants** (tail-ON through 08-01, tail-OFF from 08-02), never a blended refresh. The [[D346]] pin holds for the A/B's duration.
- **REFINEMENT TO OUR OWN 08-01 CLAIM:** we told them `promoted_leg_recall.py` reads labels not metrics and we are therefore immune. The *metrics* are immune; the **label population is not** — lane repair grows the promoted-leg set for non-supply reasons, so a recall read spanning 08-02 would misread. Diagnostic only, never in-loop, but do not run it across the boundary un-split.

## 2026-08-01 (THE INBOOK FIX SHIPPED SAME-DAY + **a TRADED-UNIT change lands 08-02 that must not be read as supply** — and the one place it could contaminate us is the LIVE A/B's arm-B definition) (D346)

- **THE UN-CONFOUNDING IS LIVE FROM THE 08-02 05:00 TICK.** Crucible shipped the inbook fix this afternoon (operator-signed): incumbent legs now resolve from the **promoted composition**, pool membership required only of NEW entrants. Dry-run candidate batch **4 → 8**, with `inbook_aa315324_add_*` and `664b137e` extensions in the set — **`664b137e` was ALSO silently inextensible** and nobody knew.
  - **Precision on which of our numbers un-confounds when, and it is not "instantly":** admission-time marginal ranking un-confounds at the 08-02 tick (every entrant scored against every promoted base, champion included). The D213 export marginals we quoted (−0.3315 / −0.0176) are leave-one-out properties of the books they sit in and **do not retroactively change** — the un-confounded dilution read arrives with the first champion-lineage promotions. Our "redundancy stands, dilution magnitude confounded" split was accepted verbatim.
- **MARGIN: KEEP 183 DAYS, UNIFORM — no change, and their reasoning is now on the record.** It also absorbs floor movement (earlier-on-backfill) and the asof-vs-calendar skew; the cost is two swing_long names for a few months in the lane our own converge program says is emitting near-duplicates at corr 0.66–0.79. "Uniform beats a per-lane knob nobody will remember the reason for."
- **⚠️ TRADED-UNIT CHANGE 08-02: the search lane's tail overlay is now OFF.** It was enforcing a 2026-07-09 §20 decision (`portfolio-campaign-tail-off-everywhere`, tail measured at ~−$26k over 8.5y) that the later-built search lane had **drifted past** — all four search-promoted books carry the tail by drift, not decision. Their ablation on `c52c1ab3`:

  | | tail ON | tail OFF | delta |
  |---|--:|--:|--:|
  | wf_max_drawdown_worst | 0.1178 | 0.1178 | **0.0 — byte-identical** |
  | wf_sharpe_median | 2.4267 | 2.5378 | **+0.111** |
  | cpcv_sharpe_p25 | 1.6693 | 1.7229 | +0.054 |
  | wf_sharpe_p10 | 0.4323 | 0.3658 | **−0.067** |

  **The tail bought exactly zero worst-drawdown protection** — the one axis it exists for — at 0.11 of median Sharpe (66 `tail_roll_reopen_unaffordable` events today alone: the carve-out often cannot re-open after rolls, so the hedge is absent when it matters). **Post-08-02 book cards read ~0.05–0.11 higher on the Sharpe family. That is the UNIT, not our supply.**
- **WE TRACK NO BOOK-CARD SERIES — verified, not assumed.** Every yardstick we steer or train on is component-level (`verdicts` cpcv / wf_p25 / p10); the tail is a **portfolio-level** overlay that never touches a component card. `promoted_leg_recall.py` reads the export for `component_run_id` labels only, not `gated_run.run.metrics`. **Nothing to split.**
- **⚠️ THE ONE REAL EXPOSURE IS SECOND-ORDER AND IT TOUCHES A RUNNING EXPERIMENT.** `BOOK_FLOOR = 0.9439` (`feedback/book_usable_weights.py:40`) — the weakest component ever admitted to a promoted book — **defines arm B of the LIVE generation A/B** (prereg `4e369b779ca9`). If tail-OFF lifts book cards, more books clear §8.7, more components become promoted, and the floor can move DOWN for **unit** reasons. It is a **pinned literal in 4 places and is never recomputed from the export**, so it cannot drift on its own — **but do NOT refresh it from a post-08-02 population while the A/B runs.** That would import a unit change into a running experiment mid-flight.
- **`c52c1ab3`'s distinctive property shrinks under the new unit, honestly noted:** its p10 goes 0.4323 → 0.3658. Still the only positive p10 on the board, but ~15% of it was the overlay Crucible just removed. **Ask 1 does not reopen** — the 0.1178 worst-DD is the book's own path, not the tail, and tail-off cpcv_p25 lands 1.7229 vs the champion's 1.7236.

## 2026-08-01 (CRUCIBLE ANSWERED EVERYTHING — **the window is PER-BUCKET (swing_long is SEVEN years), our Ask-4 decorrelation mechanism is REFUTED at n=7,439, the champion criterion turned out not to exist in code, and our scale-down algebra was killed by their measurement. Two of our four asks landed; two of ours were wrong. Filter corrected in-tree.**) (D345)

- **⚠️ STANDING OBLIGATION (unchanged): REVERT `FORGE_PREFILTER_SAMPLE_N` 150 → 40 WHEN THE A/B RESOLVES.** ~31% of ranked production/day until then.
- **THE WINDOW IS PER-BUCKET AND OUR 5y INFERENCE WAS WRONG FOR THE TREND LANE — FIXED (`chain_inception.py`, 13 tests).** Crucible's `runs_repository.py`: swing_short/swing_mid **1,825d**, swing_long **2,555d**, default 1,825d, sliding from `polygon_data_asof`. 18% of our decided volume (7,787 runs) is on the 7y window and that cohort is **100% trend_continuation/swing_long**.
  - **OUR 5-OF-5 VERIFICATION COULD NOT HAVE CAUGHT IT.** All 49 recorded `pre_inception` failures are 1,825-day runs; **zero** are 2,555-day. The 7y trap is latent, not absent — every observable we had was generated by the 5y lane. "Verified on the boundary" was the best-supported wrong answer available.
  - **Live effect, measured on the real export (185 floors):** 5y buckets **14 names**, swing_long **22**. The 8 swing_long-only names: ABNB, DASH, LYFT, PLTR, RTX, SQQQ, UBER, UVXY. **Six of those we had filed as permanently dormant.**
  - **Crucible said 20 for swing_long; we exclude 22** — LYFT (2019-04-04) and UBER (2019-05-16) sit inside the 183-day margin band, not failing yet. That is the fail-safe doing exactly its job, priced and documented. VIX (2019-01-02) is 30 days clear and correctly untouched.
  - **`ChainInceptionExclusions`** (frozen, slots) replaces the flat `frozenset` through sampler/iterator/CLI: the only way to read it is `for_bucket()`, so no call site can silently re-introduce a flat window. **NOT deployed** — daemon started 07-31T23:51 PDT, before `fa00daf`, so the filter has never been live. The bump stays held; this makes the code that a reboot would activate correct rather than wrong.
- **ASK 4 (absolute-market-gate decorrelation) — RUN BY THEM AND REFUTED, n=7,439.** `|Δ mrv_abs|` beta **+0.0057/SD, t=+3.6 — significant, WRONG SIGN, tiny**; whole threshold model R² **0.0018**. A looser gate is a duty-cycle *superset* of a tighter one, so it correlates slightly HIGHER — **backwards for the very leg that inspired the hypothesis.** Dropped, per our own pre-commitment.
  - **THE HALF THAT SURVIVES AND IS ACTIONABLE: percentile-threshold spreads buy ZERO decorrelation** (|Δ rsi_pct| t=+0.4, |Δ ivol_pct| t=−0.6). Confirmed at n=7,439. **Stop spending enumeration multiplicity on percentile spreads inside a converging cell** — that is a converge-program input.
  - **AND THE UNCOMFORTABLE ONE:** IC(cpcv_p25, corr_to_book) = **+0.547** in this population — *stronger* than the +0.336 we quoted, and it **survives controlling for all three threshold deltas**. So anti-alignment is NOT an artifact of percentile-monoculture supply. `5d265cc5`'s decorrelation is **unexplained by any structure we can name**; the honest next instrument is realized trade overlap, not declared thresholds.
- **ASK 2 CONFIRMED WITH TEETH — nothing gates on marginal Sharpe.** In `inbook_*` it is a **rank with a top-N cut, compared against nothing**; in every other generator it is not consulted. The −0.3315 and −0.0176 legs are post-hoc leave-one-out diagnostics on an already-promoted book. **§8.7 on the whole book is the only admission gate.** Their words: *"redundant supply that assembles into a book still clearing §8.7 gets admitted and dilutes"* — and explicitly, *converge-and-freeze protects their books, not just our runner cycles.* **This raises the priority of [[D328]].** Warning attached: marginal Sharpe is **asymmetric** (tracks solo quality, not assembly value; they retired a program on it 07-25) — it ranks, it must never gate as-is.
- **ASK 3 CONFIRMED — membership only, equal weights always.** The queue hard-codes equal; there is no weight-fit step anywhere in search or promotion. Both `explicit` books are **hand-queued operator artifacts**. So part of "quality is monotone-decreasing in n" is **mechanical dilution** (at n=6 equal the champion's legs are held at 16.7%), remainder matches saturation ≈ 3.
- **THE n=6-FROM-THE-WORST-BRANCH HAS A MECHANISM, AND IT IS NOT GREED.** The inbook lane extends only books whose **every leg resolves in the current honest pool**, which carries a metric-era floor (2026-07-02). The champion's trend leg `053ada64` predates it ⇒ **the champion and every descendant (`c52c1ab3`, `5f555d6f`, `664b137e`) are silently ineligible for extension**, leaving `aa57f9f1`/`7f2a697e` as the only extendable incumbents. The search grows what it *can assemble*, not what ranks. Their fix queued, operator-gated.
- **ASK 1 — WE WERE WRONG, AND THEY MEASURED IT RATHER THAN ARGUED.** There is **no champion comparison in any production code path**; the "27.9" was hand-computed for a relay sentence. They accepted the mixed-statistic critique and will stop quoting the ratio as policy. Then they **ran our scale-down**: `c52c1ab3` at vol_target 0.105 on a rig that first reproduced the stored card to **0.0 delta on all five metrics**.

  | | promoted (0.15) | matched (0.105) | our linear prediction |
  |---|--:|--:|--:|
  | wf_max_drawdown_worst | 0.1178 | **0.1129** | 0.0825 |
  | wf_sharpe_median | 2.4267 | **2.2173** | ~unchanged |
  | wf_sharpe_p10 | 0.4323 | **0.2427** | ~unchanged |

  **A 30% size cut moved worst-DD by 4.2%.** DD is path/tail structure the vol target cannot see, and Sharpe was *not* scale-invariant in practice — wf_median fell **below** the champion's. One pre-registered arm, no retry. **Our own "where we would be wrong" clause is exactly what happened**; that pre-commitment is what made it a one-experiment settle instead of an argument, and it is worth keeping as the template.
- **WHAT SURVIVES FOR `c52c1ab3`:** still promoted at its own validated 0.15 unit, still the only **positive wf_p10** on the board. What died is only the claim that it dominates *at an equal drawdown budget*. **The champion stands.** The lever for beating it is risk construction that changes tail shape, not sizing that scales it.

## 2026-08-01 (BOOK READ — the promoted set went 2 books → **6**, 12 distinct legs, all ours) — **breadth is accumulating and the frontier is NOT moving: the two newest books added trend legs at corr-to-incumbent 0.66/0.79 with marginal Sharpe −0.33/−0.02, and they are the worst books on every tail measure. The 07-20 2-leg champion still holds.** (D344)

- **Six promoted portfolios** (07-12 n=3, **07-20 n=2 = champion**, 07-21 n=3 ×2, 07-28 n=5, 07-29 n=6). **All 12 contributing legs are Forge-authored.**
- **CHAMPION UNCHANGED — `aa31532489613849`, gated run `de00e099`:** wf_median **2.3063** on maxDD **0.0828** → **Sharpe/DD 27.9**, cpcv-p25 1.7236, PBO 0.156, DSR 0.9991, pairwise corr 0.065. Crucible's objective is Sharpe *subject to* maxDD, which is why the newer, higher-return books do not displace it.
- **THE BEST CHALLENGER IS ALREADY 11 DAYS OLD AND IS BETTER ON EVERYTHING EXCEPT DRAWDOWN — `c52c1ab377597c9e` (07-21, n=3):** wf_median **2.4267** (>champion), wf_p25 **1.4128** (>1.2722), wf_p10 **+0.4323** (champion is **−0.1369**), PBO **0.1333**, DSR **1.0000**, pairwise corr **0.0077**, regime-stress-p25 31.6, vs-SPY 13.56 (vs 4.46). It loses only on maxDD (0.1178 vs 0.0828) → Sharpe/DD 20.6. **Worth asking Crucible whether the champion criterion should stay pure Sharpe/maxDD when the alternative is positive-p10 and 3× the return.**
- **THE DILUTION IS VISIBLE IN THEIR OWN CONTRIBUTION EXPORT.** Marginal Sharpe of added legs: 5d265cc5 **+2.84** (corr 0.17), cf530d34 +2.31 (0.11), ed41e1ba +2.23 (0.32), b14c937c +1.94, 9ecd9855 +1.34 … then **f34bd056 −0.02 (corr 0.79)** and **5ab11b44 −0.33 (corr 0.66)**. The n=5/n=6 books that took those two are exactly the books with the worst wf_p25 (0.7217 / 1.1168), worst wf_p10 (−0.5271 / −0.029) and highest PBO (0.267 / 0.244). **Our trend supply is now producing near-duplicates of the incumbent trend leg** — a generation-side redundancy signal, and ours to act on.
- **THE GOOD NEWS, AND IT IS THE ANSWER TO "IS THE GRAMMAR WORK CONVERTING":** promoted legs come from **v22, v24, v26, v38, v39, v46, v48, v51** — three of those (v46/v48/v51) landed legs within the last 10 days. Grammar work converts **at the component level** even while the portfolio frontier sits still. **11 of 12 legs are `ranked`; `ed41e1ba697333d5` (v51) is the first and only `tail_lane` leg.**
- **THE COLLIDER IS IN THE PROMOTED SET ITSELF.** The five newest legs (b8e0f90b, cf530d34, f34bd056, 5ab11b44, ed41e1ba) each carry `component` on `fullhist_refit` **and a paired `reject` on `standard_window`**. Stage-two admission IS the refit ([[D337]]/[[D338]]) — the promoted population is a stage-two population, and must never be pooled with stage one.
- **ONE LEG IS NOT OURS TO REPRODUCE: `053ada6400c6af5b`** = `forge_trend_continuation_swing_long_91324e5d_dsjv45`, in **4 of the 6 books including the champion**, appears in **no Forge submission or verdict**. It is Crucible's own overlay (`days_since_jump < 45`) on our v22 config `52d25dc465a9d165` (submitted 07-05, itself promoted in the two newest books at marginal Sharpe +0.19). **Checked before claiming a grammar gap — there is none:** we emit hurst + days_since_jump on trend/swing_long at **2,520 configs/30d** (dsj overall 20,553/30d, v37→v52). The shape is covered; only that 07-05-era base predates it.
- **`de00e099` IS ANSWERED, no open obligation.** It is the champion's gated run_id, cited *informationally* in `CRUCIBLE_Q57…2026-07-29`; we answered that relay 07-30 (`FORGE_your_Q57_mapping_is_wrong_in_TWO_directions…`) and they replied same-day. The book paragraph carried no ask.

## 2026-08-01 (chain-inception floors BUILT, bump HELD) — **~22 configs/day of permanent-failure emission killed at the source (220→0 proven), but the bump is deliberately NOT deployed: Crucible's own `pre_inception` fix already solved the expensive half, leaving ~0.4%.** (D343)

- **⚠️ STANDING OBLIGATION (unchanged, most likely to be forgotten): REVERT `FORGE_PREFILTER_SAMPLE_N` 150 → 40 WHEN THE A/B RESOLVES.** ~31% of ranked production/day until then.
- **BUILT, NOT DEPLOYED** (`fa00daf`): `forge/enumeration/chain_inception.py` + sampler/iterator/CLI wiring, 9 tests. Reads Crucible's `chain_inception_floors_*.json` (live, daily 20:30, 185 names) and drops underlyings whose option chain starts after the implied window. **Emission proof on the live registry: 220 excluded-name configs of 4,000 → 0, single-name supply UNCHANGED (3,594→3,599)** — the draws redistribute.
- **WHY WE READ THEIR EXPORT AND NOT OUR OWN PROXY.** Our prefetch `below_full` (ARM 721, CEG 1126, RIVN 1183 vs max 2155) was the obvious shortcut and is **wrong**: bar count conflates a late LISTING with an INGEST-START floor, which want opposite handling. Their reply proved it — **the class is 29 names, not the 3 we flagged**: 5 biting today (+COIN, +LCID which we missed), 15 dormant under a sliding window, and **9 ingest-start floors that must NOT be frozen** (backfillable; floors move earlier). A proxy would have frozen those nine permanently.
- **REFRESH, NEVER PIN** — unlike the D278/D286 untradeable list. Floors move earlier on backfill; the window slides (their examples: PLTR stopped mattering 2025, ARM stops 2028). Recomputed every batch; **both directions pinned by tests**. We exclude **14** (5 live + 9 ingest-start), leaving the 15 dormant alone.
- **THE ONE INFERENCE, relayed for confirmation (`64088e2`, unsent):** they declared floor semantics but **not the window LENGTH**. Inferred 5y sliding, verified **5-of-5** on the boundary (COIN/LCID inside & clean — LCID by *six days*; RIVN/CEG/ARM outside with 6/2/3 failures). Still an inference about *their* semantics = the D342 shape, so **not shipping on it**. A **183-day fail-safe margin** drops a name shortly BEFORE it fails.
- **WHY THE BUMP IS HELD, and it corrects our own motivation:** we thought these configs pinned §7.3. **They no longer do** — verified: all 11 `pre_inception` configs are already `gated`, because their new `failed_runs` category feeds our existing D240 flush. **Their fix, not ours.** Residue priced honestly: **~16 failing configs/day vs ~4,200/day = 0.4%**, which does not justify a restart two days after one of ours cost 6h. **It rides the next bump with its own reason.**
- **The case that survives:** the class **regenerates** — LCID crosses within days, every new listing joins later. A one-off exclusion needs redoing forever; a filter on their daily export is self-maintaining.
- **LIVE STATE:** v52, A/B at **360/340** on contracts 1.40.0, `SAMPLE_N=150`, prefetch **~200s** (was ~700s — their writer finalize fix, ~4–5×). Daemon `active`, NRestarts=0.
- **CLOCKS — one has RESOLVED (operator, `8b8284b`):** trend-lane prereg **`4d1fa832789f` CONFIRMED at 2.84×**, read on the PRE-COMMITTED pooled statistic at the fixed n=700 gate (reached at 736), single read, no peeking-to-threshold. **The pre-commitment earned its keep:** the registered per-batch form still reads 11/18 with a median finite ratio of **0.16** — exactly the degeneracy predicted on 07-31 *before* the data matured, because merit's trend arm carries ~20 decided/batch at a ~0.7% strong rate. Resolving on the registered form would have read a coin-flip as a null.
- **Generation A/B prereg `4e369b779ca9` REGISTERED (operator, `25ab274`) — deliberately LATE, with the interim disclosed in the claim.** Registered after the arms began accruing; the disclosure is what keeps it honest.
- **REMAINING CLOCK:** generation A/B ~2.5 days to a readable p90 delta → then **revert `SAMPLE_N` to 40**.

---

Older blocks, rotated verbatim (newest archive first):
- 2026-07-01 → 2026-07-31 (180 blocks, D217→D343 era): `_archive/STATUS_2026-07.md` (2026-08-06, simplification Step A3)
- 2026-05-29 → 2026-06-29 (incl. the Phase-0/May scaffolding, session log, and original blockers): `_archive/STATUS_2026-05_2026-06.md` (2026-07-20, D295)
